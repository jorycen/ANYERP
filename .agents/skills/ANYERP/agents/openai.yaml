interface:
  display_name: "ANYERP"
  short_description: "Repo-specific patterns and workflows for ANYERP"
  default_prompt: "Use the ANYERP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true