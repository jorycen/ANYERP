# AGENTS.md

# 必须先阅读

开始任何任务前，必须先阅读：

* PROJECT_CONTEXT.md
* DECISIONS.md
* BUSINESS_RULES.md

理解：

* 企业背景
* 项目目标
* 开发原则
* 经营规则
* 历史决策原因

之后再开始工作。

---

# 工作方式

每次收到新任务：

## 第一步：苏格拉底式提问

先澄清：

* 任务目标
* 目标受众
* 使用场景
* 范围边界
* 输出格式
* 排除项

---

## 第二步：第一性原理分析

分析：

* 问题本质
* 核心变量
* 关键约束
* 最小可行方案

---

## 第三步：奥卡姆剃刀

删除：

* 重复内容
* 无关内容
* 情绪化内容
* 无法验证内容
* 不影响结论的信息

---

## 第四步：输出执行任务包

格式：

任务目标：

目标受众 / 使用对象：

输入材料：

核心变量：

最小可行路径：

输出格式：

验收标准：

不要做：

待确认事项：

建议下一步：

---

## 第五步：等待确认

对于二级任务：

未获得确认前：

不得直接开始开发。

---

# 任务分级

## 一级任务（直接执行）

满足以下条件时：

允许直接执行。

包括：

* 文案修改
* 字段名称修改
* 页面样式调整
* 表格字段调整
* 简单报表调整
* 格式转换
* Bug修复
* 小范围功能优化

执行完成后汇报结果。

无需提前讨论。

---

## 二级任务（先讨论）

满足以下任意条件：

必须先讨论。

包括：

* 新增业务流程
* 修改业务流程
* 修改审批流程
* 修改权限体系
* 修改财务逻辑
* 修改采购逻辑
* 修改库存逻辑
* 修改结算逻辑
* 修改门店运营逻辑
* 修改组织结构相关功能
* 修改核心数据库结构
* 新增核心功能模块
* AI能力设计
* 自动化流程设计

必须先输出执行任务包。

获得确认后再开发。

---

# 待确认原则

涉及以下内容时：

* 数据
* 价格
* 时间
* 软件版本
* 第三方接口能力
* AI能力边界
* 政策规则
* 财税规则

如果无法确认：

必须标记：

【待确认】

禁止编造。

---

# 系统设计原则

优先级：

业务规则

>

系统流程

>

自动化

>

AI增强

---

禁止：

为了AI而AI。

---

正确顺序：

经营经验

→

规则库

→

规则引擎

→

经营驾驶舱

→

AI分析

→

AI决策

---

# 开发原则

系统目标：

* 提升门店经营效率
* 降低管理成本
* 降低经营风险
* 减少人工依赖
* 提高组织透明度

代码要求：

* 简单
* 稳定
* 可维护
* 可扩展

避免过度设计。

优先满足：

10-100家门店规模的发展需求。

---

# 项目边界与 API 单一来源

本项目 `sdxcx` 只负责微信小程序端。网页端位于 `D:\艾诺云\小艾虾\ANY-ERP`，不属于本项目。

业务逻辑和数据统一由 `D:\艾诺云\小艾虾\ANY-ERP` 提供的 API 执行和返回，包括：

* 财务、采购、库存、销售、报表等业务计算
* 金额汇总、状态流转、结算和权限校验
* 数据查询、筛选、分页和统计口径

开发要求：

* 类似功能优先查找并调用 `D:\艾诺云\小艾虾\ANY-ERP` 现有 API，不在本项目重复实现业务逻辑。
* 新需求如果现有 API 不支持，应先在 `ANY-ERP` 增加或调整 API，再由小程序端调用。
* 小程序端只负责页面展示、交互、参数传递、加载状态、错误提示和展示格式化；不得复制财务、库存、采购、结算或权限规则。
* 禁止在本项目新增或扩展 `backend`、`frontend` 等后端/服务端目录，不新增服务端模型、控制器、数据库迁移或本地业务数据层。
* 不得使用模拟数据、前端硬编码业务结果或通过前端计算掩盖 API 缺失；接口能力不明确时标记【待确认】。
* 修改前必须确认对应 API 的路径、参数、返回结构和权限范围；网页端相关改动应在 ANY-ERP 项目内完成。

---

# 行业最佳实践原则

涉及：

* 财务
* 库存
* 采购
* 结算
* 权限
* 连锁门店管理

时：

优先参考行业成熟方案。

不要为了创新而创新。

如果成熟方案已经验证有效：

优先采用成熟方案。

并向我说明：

* 行业常见方案
* 当前推荐方案
* 优缺点
* 实施成本
* 未来扩展性

然后等待确认。

---

# 决策记录规则

发生以下情况：

* 新增核心业务流程
* 修改业务规则
* 修改权限体系
* 修改财务逻辑
* 修改采购逻辑
* 修改库存逻辑
* 修改系统架构
* 修改AI策略

获得最终确认后：

自动更新：

DECISIONS.md

记录内容：

* 决策时间
* 决策内容
* 决策原因
* 备选方案
* 最终选择
* 状态

---

# 文档维护规则

以下文件由AI负责维护：

* DECISIONS.md
* BUSINESS_RULES.md
* CHANGELOG.md

---

规则：

重大业务决策

→ 更新 DECISIONS.md

经营规则新增或调整

→ 更新 BUSINESS_RULES.md

系统功能变更

→ 更新 CHANGELOG.md

---

更新完成后：

向我汇报更新内容。

无需我手工维护文档。

---

# 默认原则

如果无法判断属于一级任务还是二级任务：

默认按二级任务处理。

先讨论。

后开发。

禁止直接修改核心业务逻辑。

---

# Git 发布要求

* 默认直接在 `main` 分支进行修改、提交和推送。
* 不新建 `agent/*`、`codex/*` 等工作分支。
* 提交前必须检查变更范围，只上传用户明确纳入本次任务的文件。
* 用户明确排除的项目、目录或文件不得上传。
