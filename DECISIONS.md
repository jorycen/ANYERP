# 决策记录

## 2026-08-12 用户权限同时配置区域与门店并禁止跨区域调拨

决策时间：2026-08-12

决策内容：
* 用户分配范围同时保存区域权限和精确门店权限；区域用于限制门店候选范围，精确门店作为最终业务数据权限。
* 选择全部有效区域时，可以选择全部有效门店；选择单个或多个区域时，只能选择对应区域下的有效门店。
* 用户管理列表同时展示所属区域和实际已分配门店；历史只有区域权限的账号继续兼容显示该区域全部有效门店，重新保存后转为精确门店权限。
* 服务端校验门店必须属于所选区域及用户所属经销商，前端限制不作为安全边界。
* 调拨只允许同一区域内执行；跨区域商品流转必须走出库及采购流程，并提示具体业务路径。

决策原因：
* 只配置区域无法表达区域内部分门店的实际权限，也会造成用户确认后门店信息不回显。
* 区域筛选与门店最终授权分离，可以同时满足区域管理和门店级数据安全。
* 调拨跨区域会绕过出库及采购流程，不符合库存和采购责任链要求。

备选方案：
* 仅保存区域并默认放开区域内全部门店：无法支持部分门店授权，不采用。
* 仅保存门店并删除区域权限：无法满足区域管理和库存区域权限展示，不采用。

最终选择：采用“区域限定候选 + 门店确定最终权限 + 服务端双重校验 + 调拨禁止跨区域”的方案。

状态：已确认并实施。

## 2026-08-11 库位按门店配置有效范围并禁止带库存停用

决策时间：2026-08-11

决策内容：
* 库位继续采用销售仓、样品仓、铺货仓、不可售仓和占用仓五类标准库位，不新增门店物理库位类型。
* 系统设置支持为每类标准库位选择适用门店；编辑库位时可以增加或移除门店范围。
* 停用前检查该门店库位的数量库存和在库 SN，只要存在库存就拒绝停用并提示具体门店及库存信息。
* 停用只修改库位状态，不删除库位记录，也不改变历史库存、SN和业务单据引用；本次不支持批量操作。

决策原因：
* 当前业务需要表达“同一类库位在不同门店是否有效”，而不是维护柜台或展台等物理位置。
* 带库存停用会导致后续库存无法正常选择和处理，因此必须先清空库存再停用。
* 保留现有五类库存类型可以兼容库存汇总、采购入库、库存调整和 SN 库位引用，降低迁移风险。

备选方案：
* 继续将标准库位强制同步到全部门店：无法表达门店差异，不采用。
* 允许带库存停用：会产生无法继续处理的存量库存，不采用。

最终选择：采用“标准库位 + 门店适用范围 + 带库存禁止停用”的方案。

状态：已确认并实施。

## 2026-08-10 经销商级库存汇总按账号直接区域权限导出

决策时间：2026-08-10

决策内容：
* 经销商级账号增加直接配置的管理区域；账号可管理一个或多个区域时，只能访问这些区域下的有效门店库存。
* 账号配置全部区域时，可查看并导出所属经销商全部区域的门店库存；BOSS 继续保持全局范围。
* 库存汇总页面增加“门店”字段；库存汇总导出沿用现有筛选条件，在同一工作表中按“门店 → 商品”展开库存信息。
* 经销商级账号的库存区域范围由服务端按账号区域权限和门店区域二次校验，不能通过前端参数扩大范围。
* 历史经销商级账号从既有门店授权一次性补齐区域权限；后续不再运行时从门店授权反推区域。

决策原因：
* 经销商账号可能只负责一个区域，也可能负责多个区域，单纯按经销商或门店授权无法准确表达经营边界。
* 导出按门店展开可以支持区域内库存核对，避免多个门店商品库存合并后无法追溯来源。
* 服务端区域与经销商双重校验可以防止经销商账号通过修改 storeId 或导出参数读取其他区域数据。

备选方案：
* 继续按账号已分配门店反推区域：无法表达区域级管理范围，且门店新增后需要重复维护账号权限，不采用。
* 仅在前端增加区域筛选：无法防止直接调用接口越权，不采用。

最终选择：采用“账号直接区域权限 + 所属经销商门店区域过滤 + 单工作表按门店展开导出”的方案。

状态：已确认并实施。

## 2026-08-09 批量出库限定为纯库存调整

决策时间：2026-08-09

决策内容：
* 批量管理继续支持 SN 商品和非 SN 商品批量出库，并保留申请、审批、后台执行及整批事务机制。
* 批量出库统一定义为纯库存调整：SN 更新为 `out_stock` 并扣减库存，非 SN 按数量扣减库存。
* 批量出库不生成普通出库单、结算单或待结算记录，不核销资源权益，也不产生返利、应付等财务结果。
* 页面移除批量出库的“出库权益”和“资源权益”入口；服务端同时强制忽略旧客户端传入的触发参数。
* Excel 导入逐行预校验，异常行排除并支持下载，正确行生成批量维护申请；审批执行任一申请明细失败时整批回滚。

决策原因：
* 本功能用于集中修正库存事实，不代表销售、调拨、退货或对供应商结算业务。
* 库存调整与财务结算职责分离，可以避免批量维护误生成待结算、返利或应付数据。
* 服务端强制限定业务边界，能够避免旧前端或直接调用接口绕过页面限制。

备选方案：
* 保留“出库权益”开关并由操作人决定是否结算：容易将库存调整误用为结算入口，不采用。
* 批量出库同时生成普通出库单：会与批量维护申请形成重复业务凭证，不采用。

最终选择：批量出库仅调整库存和 SN 状态，保留批量维护申请及操作日志作为追溯凭证，完全不产生结算结果。

状态：已确认并实施。

补充调整：批量文件允许“正确行生成申请、异常行排除并下载”，避免大批量文件因少量错误无法继续；审批后的正式执行仍保持申请内整批事务。

## 2026-08-09 统一单一经销商并按区域展示用户归属

决策内容：
* 企业当前统一使用 `DIST001` 作为唯一经销商编码，将历史 `DEFAULT` 数据迁移为 `DIST001`。
* 保留成都区域、重庆区域、地市区域三类区域，门店区域以 `T_STORE.region_id` 为准。
* 用户管理列表将“所属经销商”列调整为“所属区域”，按用户已分配的启用门店汇总并去重显示区域。

决策原因：
* 当前实际经营主体为单一经销商，`DEFAULT` 与 `DIST001` 是历史组织编码，不应继续作为两个经销商展示。
* 用户实际业务权限由已分配门店决定；同一用户可能跨多个区域，区域应从已分配门店推导，不能依赖员工表中的单一 `region_id`。

备选方案：
* 保留 `DEFAULT` 与 `DIST001` 两个经销商：不符合当前经营组织，继续造成账号和门店归属混淆。
* 直接把用户绑定为单一区域：无法表达跨区域门店权限，且会覆盖真实门店范围。

最终选择：统一 `DIST001`，按已分配门店汇总区域展示；服务端仍保留经销商字段用于兼容权限和历史业务关联。

状态：已确认并实施。

## 2026-08-04 全量校正账号经销商归属与门店权限

核查发现问题并非邓红梅单个账号，而是历史数据中 41 个有效非 BOSS 账号存在 `T_STAFF.distributor_id` 与已分配门店归属不一致。已按有效分配门店归属统一校正：41 个账号更新所属经销商，移除 11 条跨经销商历史门店权限，主门店同步为有效分配门店。

经销商级账号的订单、导出、国补照片和 SN 追踪均以已分配门店为实际权限范围；BOSS 仍为全局范围。

状态：已实施并完成全量一致性复核。

## 2026-08-04 所属经销商与分配门店职责分离

决策时间：2026-08-04

决策内容：
* 保留“所属经销商”字段，用于组织归属和门店分配时的经销商边界校验。
* 将“分配门店”作为订单、导出、国补照片和门店选择的实际权限范围。
* BOSS 可切换账号所属经销商；普通管理员不可跨经销商修改。切换归属时清空旧门店权限，避免旧范围残留。
* 邓红梅（14780834570）归属修正为 `DIST001`，保留已勾选的 11 家 `DIST001` 门店，移除 3 家 `DEFAULT` 门店权限。

决策原因：原账号归属为 `DEFAULT`，但分配门店包含 `DIST001`，订单查询按所属经销商过滤导致只能看到重庆礼嘉天街；组织归属和实际运营门店范围必须分离。

最终选择：所属经销商负责组织边界，分配门店负责实际数据权限；服务端统一校验，前端仅作选择和展示。

状态：已确认并已实施。

## 2026-08-04 经销商级订单查询、明细导出与国补照片批量下载优化
决策时间：2026-08-04

决策内容：
* 经销商级角色查询范围以 `distributor_id` 为最终边界；门店下拉、订单列表和导出均按本经销商全部有效门店处理，不再依赖是否配置逐门店权限。
* 订单导出按附件“小艾导出.xlsx”的 58 列结构生成，按“订单 × 商品明细”展开，并汇总订单支付方式、补贴、补录和退货信息。
* 国补照片新增“当前查询结果批量下载”，服务端重新执行当前筛选条件和权限校验，ZIP 内按订单编号分目录保存照片。

决策原因：
* 原实现只放开了订单查询控制器，门店下拉接口仍按 `accessibleStoreIds` 过滤，导致经销商级账号看不到完整门店范围。
* 附件已有稳定的导出字段口径，复用现有订单、商品、收款和补录数据即可满足财务核对，避免新增业务表。
* 国补照片属于敏感资料，批量下载必须由后端重查权限和筛选结果，并保留业务操作日志。

最终选择：经销商范围按经销商归属校验；导出使用附件 58 列明细；照片批量下载使用查询结果 ZIP。

状态：已确认并已实施。

## 2026-08-04 经销商级订单查询与导出权限

决策时间：2026-08-04

决策内容：
* 经销商级角色包括财务、出纳、商务及其他非店员/店长角色，可查看所属经销商全部门店、全部人员创建的销售订单。
* BOSS 查看全局订单；店员、店长继续按原有本人或授权门店范围查看。
* 销售订单导出仅开放给经销商级角色和 BOSS，导出当前筛选条件下的全部可见订单，不受列表分页限制。
* 后端以订单所属门店的经销商归属作为最终数据范围校验，前端按钮隐藏不作为安全边界。

决策原因：
* 财务、出纳和商务需要按经销商维度对账、核查和汇总订单，按创建人限制会造成业务数据缺失。
* 复用现有订单列表字段和 Excel 导出工具可以降低实施成本，不增加订单业务表或改变订单流程。
* 导出属于批量读取能力，必须与订单查询使用相同的经销商范围校验，避免通过接口参数越权读取其他经销商数据。

备选方案：
* 仅放开指定角色的门店授权范围：改动较小，但无法满足经销商级岗位的跨店对账需求。
* 仅在前端增加导出按钮：实现简单，但无法防止直接调用接口越权，不采用。

最终选择：采用“经销商级角色按经销商范围查询 + BOSS 全局查询 + 服务端范围校验 + 当前筛选结果 Excel 导出”的方案。

状态：已确认并已实施。

## 2026-08-04 已实现功能统一迁移到左侧二级菜单

决策时间：2026-08-04

决策内容：

* 销售、库存、采购、审批、财务、商品、报表和系统设置模块中已经实现的页面功能统一改为左侧二级菜单入口。
* 原页面顶部功能页签保留内部内容实现，但由路由 `meta.tab` 驱动并隐藏页签栏；二级菜单路径成为正式访问入口。
* 定金管理继续作为销售订单页面内的操作，不单独拆分菜单。
* 当前尚未实现的退货管理、采购单、采购入库、库存预警等菜单不新增页面，并停用旧的无对应页面菜单项。
* 保留现有角色、门店范围和后端业务权限；菜单权限按原模块权限映射到已实现的二级功能。
* `/sales`、`/inventory`、`/purchase`、`/finance`、`/products`、`/reports`、`/approval` 和 `/system` 等旧模块入口继续重定向到各自默认二级页面。

决策原因：

* 功能入口统一放在左侧菜单后，用户可以直接定位业务功能，页面 URL、菜单权限和浏览器历史保持一致。
* 继续复用现有页面和接口，可以降低迁移风险，不改变销售、采购、库存、审批或财务业务事实。
* 未实现功能不应通过菜单暴露，避免用户进入空页面或误以为功能已经可用。

备选方案：

* 保留模块页签：改动最小，但功能入口分散且不利于权限与 URL 管理。
* 为每个功能重新拆分独立 Vue 页面：结构更彻底，但会重复迁移现有业务逻辑，实施成本和回归风险更高。

最终选择：采用“已实现功能二级菜单 + 路由驱动现有页面内容 + 旧入口兼容重定向 + 不新增未实现功能”的方案。

状态：已确认并已实施。

## 2026-08-01 采购入库单区分采购发起人与入库操作人

决策内容：

* 采购入库单的采购发起人统一取关联采购申请的 `T_PURCHASE_REQUEST.APPLY_USER`。
* 入库单 `T_INBOUND.CREATE_USER` 保留为入库单创建/操作审计字段，不用于展示采购申请人。
* 入库接口按 `purchase_request_id` 优先、`source_no=request_no` 兜底关联采购申请，兼容历史入库数据。

决策原因：

* 采购审批人可能代为生成待入库单，不能因此改变原采购申请的责任人。
* 采购发起人和入库操作人属于不同业务角色，必须分别保留并展示。

## 2026-07-31 SN修改申请、admin/boss审批与SN追踪

决策时间：2026-07-31

决策内容：
* 后台库存查询的序列号修改不再允许直接更新正式SN，改为一张申请单包含多条SN明细的申请审批流程。
* 仅允许修改 `in_stock` 状态的SN；申请原因必填。
* 审批人固定为经销商总权限账号 `admin` 或系统账号 `boss`，申请人不得审批自己的申请。
* 复用通用审批中心保存申请、审批任务、审批意见和审批日志；审批最终通过后由库存模块在同一事务中更新 `T_PRODUCT_SN` 并追加 `T_SN_LOG`。
* `T_SN_LOG` 保存原SN、新SN、申请人、审批生效人、原因和时间，SN跟踪通过原SN和新SN均可查到修改记录。

决策原因：
* 直接修改SN会绕过经销商组织层面的库存风险控制，无法保证申请内容与实际生效内容一致。
* SN属于库存事实，必须保留原值、新值和审批责任链，不能覆盖历史。
* 现有通用审批中心已经具备角色审批、拒绝意见、重提和审批日志能力，复用可以减少重复流程实现。

备选方案：
* 继续直接修改并追加日志：操作成本低，但不能满足审批和责任分离要求。
* 新建独立审批系统：隔离性强，但会重复建设现有审批中心。

最终选择：采用“SN修改申请 + 通用审批中心 + 审批通过事务生效 + T_SN_LOG追踪”的方案。

状态：已确认并已实施。

## 2026-07-28 用户修改密码校验与展示方式

决策时间：2026-07-28

决策内容：

* 登录后用户可从账号菜单打开修改密码弹窗，只能修改自己的密码。
* 修改密码必须提交旧密码、新密码和确认密码；新密码至少 6 位，且不得与旧密码相同。
* 前端表单按明文显示当前输入内容；服务端不返回、不反查已保存的原始密码，数据库继续保存 BCrypt 哈希。
* 修改成功后保留当前登录会话。

决策原因：

* 现有后端修改密码接口已经具备旧密码校验，主要缺少可用的前端入口和完整的服务端密码规则校验。
* 前端明文输入满足当前操作要求，但不能扩展为展示已保存密码，否则会违反密码安全规则。
* 保留当前会话可以避免普通密码修改后的额外登录中断；后续如引入会话撤销机制再单独评估。

备选方案：

* 密码输入框使用掩码：安全性更高，但不符合当前确认的交互要求。
* 修改成功后强制退出并重新登录：会话安全边界更严格，但增加操作中断。
* 仅在前端校验密码长度：实施成本低，但可被绕过，不采用。

最终选择：采用“前端明文输入 + 前后端双重校验 + 服务端哈希存储 + 保留当前会话”的方案。

状态：已确认并已实施。

## 2026-07-25 外部 Windows 图形心跳客户端自动保活 API

决策内容：

* 新增独立 Windows 图形心跳客户端，运行在 API 服务之外的其他电脑上。
* 用户双击启动后自动执行首次只读 GET 查询，此后默认每 5 分钟执行一次。
* 查询使用 `/api/v1/health/db`，不携带登录凭证、不连接数据库、不修改业务数据。
* 页面持续显示运行状态、HTTP 状态、响应耗时、返回信息和历史执行记录；允许停止心跳和保存配置。

决策原因：

* API 所在云服务可能因空闲自动暂停，外部电脑发起请求可以避免心跳进程与被暂停的 API 运行在同一环境。
* 只读健康查询的副作用和成本较低，适合作为最小保活动作。
* 图形界面可让非技术人员确认每次执行是否成功，不依赖命令行窗口。

最终选择：采用双击即自动运行的 Windows EXE；默认每 5 分钟访问一次 API 健康接口。

状态：已确认并已实施；生产 API 域名及 `/api/*` 回源配置仍需以云平台当前有效配置为准。

## 2026-07-25 销售管理国补照片查询、下载与重新上传

决策时间：2026-07-25

决策内容：

* 在销售管理下新增“国补照片”二级页面。
* 查询条件使用订单时间、补贴人姓名、补贴人手机号和开票信息中的云闪付订单号。
* 页面展示新建销售订单时已经上传的全部国补照片，不新增照片业务表，继续使用销售订单 `SUBSIDY_PHOTOS` 字段。
* 允许有权限的账号对订单国补照片进行二次整体替换上传，并保留销售订单业务操作日志。
* 页面和服务端开放给 BOSS、admin、财务和店长；店长只能访问授权门店范围内订单。
* 重新上传的图片保存到受保护的服务端文件目录，下载接口必须重新校验账号和门店权限。

决策原因：

* 国补照片已经作为销售订单正式字段持久化，复用现有订单数据可以避免重复建表和历史数据迁移。
* 补贴人姓名和手机号比客户姓名更符合国补资料查询场景；云闪付订单号已经沿用开票信息字段保存。
* 资料照片属于敏感信息，查询、修改和下载都必须由服务端控制权限，并保留操作记录。
* 店长需要处理本门店资料补录和纠错，因此纳入授权门店范围，而不是仅开放给总部角色。

备选方案：

* 新增国补照片独立业务表：追踪能力更强，但会增加数据迁移和订单关联复杂度。
* 仅允许总部账号处理：权限风险较低，但无法满足门店现场补录和纠错。
* 只追加新照片：可以保留旧文件，但容易产生重复和错误资料；本次采用整体替换并记录前后数量。

最终选择：采用“复用订单 `SUBSIDY_PHOTOS` + 销售管理二级查询页面 + 服务端门店权限 + 整体替换上传 + 下载审计日志”的方案。

状态：已确认并已实施。

## 2026-07-21 应付待结算清单来源筛选与同供应商批量申请

决策内容：

* 应付待结算清单增加来源类型筛选，采购、采购调整、费用和报销使用不同颜色标签区分。
* 清单同时展示当前筛选结果和已勾选记录的条数、剩余待结算金额汇总。
* 每条应付款记录支持多选；批量生成供应商结算单时，所选记录必须属于同一供应商。
* 勾选结果带入现有结算申请界面，继续使用结算草稿、提交审批、审批通过后待付款和付款管理流程，不在清单页直接付款。

决策原因：

* 来源类型和颜色区分可以帮助财务快速识别采购、调整和费用类应付款，降低误选风险。
* 筛选汇总和勾选汇总可以在批量结算前明确核对处理范围与金额。
* 供应商结算单需要以同一供应商为收款主体；复用现有结算申请及审批链路可以保持现有财务职责分离和审计追溯。

备选方案：

* 逐条进入结算申请：校验简单，但无法满足批量处理和金额核对需求。
* 清单页直接确认付款：操作更短，但会绕过现有结算审批和付款职责分离。

最终选择：采用“来源类型筛选与颜色标识 + 筛选/勾选汇总 + 同供应商多选 + 复用结算申请及审批流程”的方案。

状态：已确认并已实施。

## 2026-07-20 SN跟踪关联原始单据跳转与权限

决策内容：

* SN 跟踪沿用现有采购申请、销售订单、调拨单和入库单数据，通过已有详情页面跳转，不新增数据表，不改变采购、销售、库存或审批流程。
* 追踪接口只返回当前账号有权查看的关联单据节点，并返回 `ref_type`、`ref_id`、`ref_no` 和 `can_view_order` 供前端生成按钮。
* 员工/店员仅能查看本人发起的原始单据；店长按授权门店查看；经销商级账号（admin、boss、财务、商务、采购等非店员/店长角色）查看本经销商全部门店；BOSS 查看全局。
* 原始订单详情接口在追踪跳转场景再次校验权限，前端按钮隐藏不作为安全边界；跳转页面仅用于查看，不授予编辑、审批、删除权限。

决策原因：

* 现有单据已经保存 SN 与采购、销售、调拨、入库的关联事实，复用详情页可修复跳转并避免重复建表和数据迁移。
* 追踪页和详情页双重校验可以避免通过猜测单据 ID 绕过页面按钮限制。

最终选择：采用“现有单据关联 + `ref_type/ref_id` 跳转 + 服务端分角色校验”的方案。

状态：已确认并已实施。

## 2026-07-19 调拨全来源查询与SN入库追溯权限

决策内容：

* Web 调拨管理统一查询调拨主表，不区分 Web 或小程序发起来源；经销商级账号按本经销商门店范围查询，并兼容使用调拨记录上的经销商字段补齐历史记录，BOSS 继续查看全局记录。
* 经销商账号包括 `admin`、`boss`、采购、财务、商务及其他非店员/店长角色；`clerk`、`staff`、`manager`、`store_manager` 不属于经销商级账号。
* SN 跟踪的关联入库详情单独设置后端权限，仅经销商级账号可以查询；查询结果显示采购申请、供应商、采购价格、门店名称和商品 SN。
* SN 信息中的门店统一返回门店名称；入库单详情按入库明细展示 SN，非 SN 商品显示为空值占位。

决策原因：

* 调拨记录的业务事实与发起端无关，Web 查询应覆盖统一调拨数据集；同时保留店员、店长既有数据范围，避免权限扩大。
* 采购来源和价格属于经营敏感信息，需要服务端权限控制，不能只依靠前端隐藏按钮。
* 入库明细已保存采购价格、SN 和采购明细关联，直接复用现有事实可以避免重复建表和历史数据迁移。

最终选择：采用“统一调拨查询 + 服务端经销商角色校验 + 门店名称及入库 SN 展示”的方案。

状态：已确认并已实施。

## 2026-07-19 采购申请、销售订单和结算单草稿删除规则

决策内容：

* 采购申请、销售订单和结算单均允许删除草稿，但仅限从未提交过的草稿。
* 已提交、审批中、已审批、已付款或已作废单据不可删除；结算单审批退回草稿时仍保留“曾经提交过”的事实，因此不可删除。
* 结算单删除采用 `is_deleted=1` 软删除，保留结算单、结算明细和不可变业务操作日志。
* 结算单删除与关联应付款、费用状态恢复必须使用同一数据库事务。

决策原因：

* 草稿删除可以清理错误或重复录入，避免正常业务列表被无效草稿干扰。
* 已提交单据已经进入业务责任链，必须保留原单据和提交历史，不能通过删除绕过审批与审计。
* 结算草稿会影响应付款分配，删除时必须同步释放占用金额，避免应付款被错误锁定。

备选方案：

* 物理删除结算单及明细：实现简单，但不符合财务追溯和审计要求。
* 使用“作废”代替删除：会混淆“未提交草稿删除”和“已生效单据作废”两个业务动作。

最终选择：三类单据统一限制为“从未提交草稿可删除”；采购申请沿用 `status=deleted`，销售订单沿用 `is_deleted=1`，结算单新增 `is_deleted=1` 并同步恢复应付款状态。

状态：已确认并已实施。

## 2026-07-18 应付结算单增加审批及按金额部分付款

决策内容：

* 采购结算单创建后保留为 `draft`；提交后进入 `pending_approval`，审批通过后进入 `confirmed` 待付款。
* 审批拒绝不删除结算单，退回草稿并保留审批人、审批时间和审批意见，允许重新提交。
* 应付结算单管理独立为 `/finance/settlement` 页面，集中处理详情、审批、作废和付款。
* 按金额部分付款复用付款流水，允许本次付款金额小于等于剩余应付金额；原始结算金额不被覆盖，累计付款和剩余金额持续保留。
* 结算创建页面增加备注；采购明细列名称统一为“结算数量”，数量展示去除无意义尾数。

决策原因：

* 结算单提交和实际付款需要职责分离，避免未复核的应付数据直接进入付款。
* 按金额部分付款需要保留原结算单及每次付款流水，满足财务追溯和并发安全要求。
* 独立管理页面便于审批人员集中处理待审批结算单，不与待付款源数据混在同一列表。

备选方案：

* 提交后直接进入待付款：改动小，但缺少应付审批控制。
* 修改结算单原金额实现部分付款：实现简单，但会覆盖原始业务金额，无法完整追溯。

最终选择：新增待审批状态和审批字段，部分付款使用现有付款记录及事务锁机制。

状态：已确认并已实施。

---

## 2026-07-18 国补 POS 首部分到账金额扣除税费

决策内容：

* “首部分”定义为 `国补POS（电脑）-客户实收`。
* 国补金额取国补 POS 客户实收与政策补贴应收两部分金额合计。
* 补贴金额取国补应收单的应收金额；国补补贴金额取订单 `national_subsidy`。
* 首部分日结到账金额按 `国补金额 - 补贴金额 - (国补金额 - 国补补贴金额) × 0.006` 计算。
* 仅调整日结到账金额，订单收款原始记录和国补应收原始记录不覆盖。

决策原因：

* 国补 POS 首部分实际到账需要扣除 0.6% 税费。
* 保留原始收款和应收金额，便于业务追溯和财务对账。

最终选择：在销售订单同步日结明细时统一计算首部分净到账金额，并由后端测试保障公式。

状态：已确认并已实施。

## 2026-07-18 费用单保存生成持久化草稿

决策内容：

* 费用单保存时复用 `T_EXPENSE` 生成 `draft` 状态记录，并立即进入费用列表。
* 草稿编辑和重复保存沿用原费用单 ID/单号；创建人、店长、管理员或 BOSS 可以编辑、提交和删除草稿。
* 草稿阶段不生成对公应付、不进入私人垫付审批、不进入付款或结算流程。
* 草稿提交时，对公费用生成待付款应付记录并进入 `pending_payment`；私人垫付进入 `pending_approval`。
* 草稿删除使用 `is_deleted=1` 软删除，保留费用单历史记录。

决策原因：

* 当前前端保存草稿仅存在浏览器本地，无法跨会话查询和统一执行权限控制。
* 费用单如果保存即生成应付，会把未完成的费用信息提前带入付款流程，增加财务风险。
* 复用现有费用单可以保持费用字段、审批、应付和结算链路的一致性，避免草稿转正式单据时重复建单。

最终选择：在现有费用单表中增加 `draft` 状态语义，提交时再按支付方式进入原有财务流程。

状态：已确认并已实施。

---

## 2026-07-18 采购申请与销售订单统一持久化草稿

决策内容：

* 采购申请和销售订单保存时都生成正式业务记录，状态分别使用 `draft`；保存后立即进入对应列表。
* 草稿编辑和重复保存沿用原单据 ID/单号更新，不重复生成业务单据。
* 采购申请草稿删除使用 `status=deleted` 标记；销售订单草稿删除使用 `is_deleted=1` 软删除，均保留历史记录。
* 采购申请草稿提交后进入现有采购审批；销售订单草稿提交后进入现有售价审批、库存、定金、资源权益和归档链路。
* 草稿阶段不产生审批、库存扣减、SN 绑定、资源权益核销或定金占用。

决策原因：

* 仅保存在浏览器本地无法跨设备、跨会话查询，也无法满足后续列表管理和权限控制。
* 复用原业务单据可保持订单明细、提交状态和后续审批的同一条追踪链路，避免重复建单。
* 草稿删除保留记录，满足审计和问题追踪要求，同时不让已删除草稿干扰正常列表。

备选方案：

* 继续使用前端本地草稿：实现成本低，但无法跨设备使用，且服务端无法统一控制权限。
* 为草稿新增独立业务表：隔离性更强，但会增加数据模型、迁移和提交转换成本。

最终选择：复用采购申请和销售订单现有业务表，以状态字段区分草稿，并使用软删除。

状态：已确认并已实施。

---

## 2026-07-17 销售退库必须生成重新入库单

决策内容：
* 销售退库审批通过后，自动生成 `SALES_RETURN` 类型的待入库单，进入现有重新入库/入库执行页面。
* SN 商品审批通过后先变更为 `return_pending`（退库待入库），不直接恢复为可销售状态。
* 执行重新入库时复用原 SN，恢复为 `in_stock`，增加门店库存并写入 SN 入库日志；入库完成后销售退库申请改为 `completed`，原销售订单改为 `returned`。
* 使用销售退库单号作为来源幂等键，禁止重复生成待入库单；执行入库时必须匹配原 SN，禁止替换为其他 SN。

决策原因：
* 原销售退库接口只有申请和审批，没有库存执行、入库单和 SN 状态回写，导致商品仍是 `sold`，重新入库列表为空且再次销售被拦截。
* 退库和重新入库分成两个库存事实，便于门店确认实物后再恢复销售库存，并保留完整追踪链路。

最终选择：采用“审批通过自动生成待重新入库单，执行入库后恢复库存”的流程。

状态：已确认并已实施。

---

## 2026-07-17 同门店 SN 库位调整

决策时间：2026-07-17

决策内容：

* 在库存管理的 SN 库存清单中增加“调整库位”操作。
* 仅允许将当前在库 SN 调整到同一门店的启用标准库位。
* 调整只修改 `T_PRODUCT_SN.LOCATION_ID`，不改变门店、库存数量和 SN 状态。
* 每次调整写入 `T_SN_LOG`，记录原库位、目标库位、操作人和操作时间。
* 本次不支持非 SN 商品按数量进行库位调整，也不支持跨门店移动。

决策原因：

* 现场需要快速纠正商品实际存放库位，使用完整库存批量维护流程成本过高。
* SN 商品以单台记录保存库位，直接修改库位不会改变库存数量事实。
* 跨门店移动属于调拨业务，非 SN 商品库位调整需要数量台账，均不纳入本次最小范围。

备选方案：

* 方案一：所有库位变更统一走批量维护审批，审计完整但操作成本较高。
* 方案二：直接开放门店和数量调整，范围过大且容易形成跨门店或库存数量误改。
* 方案三：仅开放在库 SN 的同门店库位调整，并记录 SN 日志。采用。

最终选择：方案三。

状态：已确认并已实施

---

## 2026-07-16 调拨历史查询与分级数据权限

决策时间：2026-07-16

决策内容：

* 调拨管理新增历史调拨记录查询，包含全部调拨状态，并支持按调拨单号、状态和日期筛选。
* 店员查询本人参与的调拨记录；参与范围包括申请、调出确认和调入确认。
* 店长可以查看授权门店涉及的全部调拨记录；除店长、店员以外的经销商角色可以查看本经销商全部门店调拨记录；BOSS 查看全部调拨记录。
* 历史数据沿用已有申请人、出库确认人和入库确认人姓名，不新增申请人 ID 回填要求。
* 后端统一执行数据范围过滤，禁止通过显式传入调出门店或调入门店参数绕过权限。

决策原因：

* 调拨属于库存事实，历史记录需要同时满足业务追溯和最小权限原则。
* 当前调拨表已经保存申请人及确认人姓名，按姓名兼容历史记录可以避免为存量数据强制补录无法可靠确认的员工 ID。
* 查询权限必须在服务端执行，避免仅依赖前端列表过滤造成越权。

备选方案：

* 方案一：所有账号查看授权门店调拨记录。
* 方案二：店员按申请人、店长按门店、经销商角色按经销商、BOSS 按全局分级查询。
* 方案三：新增申请人 ID 并只允许按 ID 查询，历史姓名记录不展示给店员。

最终选择：

* 采用方案二；历史参与人使用已保存姓名匹配，保留全部状态查询。

状态：已确认

---

## 2026-07-18 销售、采购、调拨全流程审计展示

决策时间：2026-07-18

决策内容：

* 销售订单、采购申请、库存调拨均在列表展示关键提交/审批/确认人员。
* 三类单据详情增加按时间排序的流程记录时间线。
* 新增通用业务操作审计日志，记录单据、动作、前后状态、操作人、时间、意见及必要的附件数量信息。
* 销售补充提交人、提交时间、审批人、审批时间和审批意见字段；采购补充提交人、提交时间和审批时间字段。
* 调拨沿用申请、调出确认、调入确认职责，不将出库或入库确认误称为审批。
* 本次不迁移三类业务到通用审批引擎，保留现有业务流和权限规则。

决策原因：

* 现有三类单据的直接字段不完整，无法满足拒绝、重提和出入库协作的完整追溯。
* 独立追加不可变操作日志可以低风险补齐审计能力，避免改变库存、采购入库和销售归档事务。
* 后续若统一审批流程成熟，可继续将业务日志与审批实例关联。

备选方案：

* 方案一：只增加列表字段，不保存历史操作。
* 方案二：本次直接将销售、采购、调拨全部迁移到通用审批引擎。
* 方案三：保留现有流程，新增通用业务操作日志和详情时间线。

最终选择：

* 采用方案三。

状态：已确认

---

## 2026-07-17 采购申请草稿与提交流程

决策时间：2026-07-17

决策内容：

* 新建采购申请的商品明细区域增加字段提示，明确填写商品名称、采购单价和数量；不新增采购申请标题字段。
* 保存草稿后生成正式采购申请记录，状态为“草稿”，不进入审批流程。
* 草稿支持编辑，编辑后可再次保存。
* 草稿可由创建人、店长、管理员或 BOSS 提交；提交后进入现有待审批流程。
* 采购申请列表增加提交人字段，并支持按提交人、商品名称/PN/商品编码、供应商和状态查询。

决策原因：

* 采购申请需要先保留录入结果，再由申请人确认提交，避免未完成信息进入审批队列。
* 复用现有采购申请和审批状态，减少新增业务对象及审批分支。

最终选择：

* 采用“草稿 → 编辑/保存 → 提交 → 现有审批”的流程。

状态：已确认

---

## 2026-07-17 采购申请门店与库位分配

决策时间：2026-07-17

决策内容：

* 采购申请沿用现有门店库位主数据，不新增独立仓库模型。
* 新采购申请必须按“门店 → 启用库位”分配数量。
* 门店分配数量必须等于该门店下库位分配数量，所有门店分配数量必须等于商品采购数量。
* 新增门店分配时默认使用该门店的“销售仓”库位。
* 审批通过后，采购入库单按门店和库位生成入库明细，执行入库时自动承接指定库位。
* 历史未指定库位的采购申请继续兼容原有入库流程，不进行存量数据迁移。

决策原因：

* 采购申请应在源头明确实物到店后的存放位置，减少入库人员二次判断和错放。
* 复用现有库位主数据和入库明细库位字段，可以保持库存、SN和入库追溯的一致性。

最终选择：采用“门店分配 + 启用库位分配 + 审批入库继承库位”的方案。

状态：已确认

---

## 2026-07-16 商品 PN 主数据与销售归档校验一致性

决策时间：2026-07-16

决策内容：

* `T_PRODUCT_PN` 作为销售归档使用的 PN 主数据来源；`manufacturer_code` 和厂商条码只能作为商品创建、导入、编辑时同步 PN 的来源，不能在归档时替代 PN 主数据放行。
* PN 比对统一转字符串、去首尾空格并移除内部空格、统一小写；归档时优先使用订单明细 `product_id` 查商品，再校验 PN 与商品的关联关系。
* 商品库存存在不能绕过 PN 主数据校验；库存记录的商品 ID 只能用于确认 PN 与商品的匹配关系。
* 商品创建、批量导入、商品编辑、厂商条码新增和手工新增 PN，必须在同一数据库事务中同步有效 PN 主数据；已被其他商品占用的 PN 不得转移绑定。
* 存量修复仅自动处理“有效厂商条码明确证明商品与 PN 关联”的记录；同一 PN 关联多个商品、仅有历史删除记录的记录保留人工复核。

决策原因：

* 历史商品导入流程只写入 `T_PRODUCT` 和 `T_PRODUCT_BARCODE`，未写入 `T_PRODUCT_PN`，导致商品搜索和库存可见，但归档接口按 PN 主数据校验时误报“PN 不存在”。
* 归档继续使用 manufacturer_code 兜底会掩盖主数据缺失，也可能放行 PN 与订单商品不匹配的数据，破坏库存和销售追溯。

备选方案：

* 方案一：归档继续使用 manufacturer_code 兜底。实现简单，但无法保证 PN 主数据完整性，放弃。
* 方案二：只修复当前商品。风险低但会继续影响其他历史导入商品，放弃。
* 方案三：统一写入链路、严格归档校验并进行可审计的存量修复。采用。

最终选择：方案三。

状态：已确认并已实施

## 2026-07-15 采购结算与个人报销支持部分处理

决策时间：2026-07-15

决策内容：

* 采购结算按采购明细和数量选择，使用采购申请明细作为应付款来源；采购结算单保存商品、数量、单价和金额快照。
* 个人报销审批通过后，财务可以输入本次报销金额，允许同一报销单多次生成报销结算单。
* 原采购应付款和原报销单不被覆盖，已结算金额、实际付款金额和剩余可结算金额分别累计。
* 部分采购结算显示“部分结算”，部分报销显示“部分报销”；只有累计完成且全部付款后才显示完成。
* 已结算明细不得再次选择；部分结算、作废恢复和状态更新必须在同一数据库事务中完成并锁定相关记录。
* 采购结算单价沿用扣除返利后的实际应付单价；个人垫付不生成供应商待付款。

决策原因：

* 保留原始采购和报销事实，避免直接修改历史单据造成审计和追溯断裂。
* 采购明细级分摊可以准确控制数量和金额，避免整张采购单重复结算。
* 结算占用与实际付款分开，能够支持结算单未付款、部分付款和多次分次处理。

备选方案：

* 方案一：直接修改原采购单或报销单金额。
* 方案二：每次部分结算都拆分生成新的完整应付款。
* 方案三：原单保留，新增明细级结算分摊记录并累计剩余金额。

最终选择：

* 采用方案三；审批仍以完整报销单为单位，财务按剩余金额分次生成报销结算。

状态：已确认

## 2026-07-14 采购退单采用独立调整单并仅调整未入库数量

决策时间：2026-07-14

决策内容：

* 采购订单页面增加“退单”入口，实际采购订单由现有 `PurchaseRequest` 业务单据承载。
* 退单采用独立采购调整单及明细记录，不直接覆盖原采购订单、原入库单或原始应付款。
* 调整范围限定为未入库数量，支持增加和减少；已入库数量必须通过现有库存退库流程处理。
* 页面按采购明细、门店和待入库单明细展示调整数量，避免多门店分配时无法确定减少对象。
* 调整后待入库数量为 0 时保留入库单和明细历史，并将待入库单标记为已取消；调整后大于 0 时继续保留待入库状态。
* 公司账期采购按扣除单位返利后的实际应付单价计算调整金额，生成独立的正数或负数待付款记录；负数调整不得单独生成付款金额不大于 0 的正式结算单，只能和正数应付款合并抵扣。
* 减少数量按比例退回已使用的返利抵扣，增加数量按比例追加返利抵扣并校验余额。
* 个人垫付采购不生成供应商待付款调整，而是同步调整关联采购垫付报销单；已经付款的报销单禁止继续调整。
* 退单操作沿用采购管理权限，不新增独立审批流程；所有变化在同一数据库事务内完成并保留操作原因。

决策原因：

* 原采购单代表真实外部采购事实，直接改写会破坏采购、入库、应付和审计追溯关系。
* 未入库数量调整本质是采购承诺变化，已入库数量调整本质是库存退库，两个业务事实必须分开处理。
* 独立正负待付款记录可以兼容当前“待付款清单 → 应付结算单 → 付款管理”的流程，并避免直接冲销历史付款。
* 采购当前存在多门店分配和供应商返利抵扣，明细级记录能够保留数量、金额和返利的可解释性。

备选方案：

* 方案一：直接修改原采购订单数量和金额。
* 方案二：删除或重建待入库单和原始应付款。
* 方案三：新增独立采购调整单，调整未入库数量并生成正负待付款记录。
* 方案四：所有退单统一走已入库库存退库流程。

最终选择：

* 采用方案三；已入库部分继续使用现有退库流程。

状态：已确认

## 2026-07-14 生产环境前端 History 路由与静态托管回源

决策时间：2026-07-14

决策内容：

* 正式前端继续使用 Vue Router History 模式，Vite 构建 base 与 Router base 统一为域名根路径 `/`。
* CloudBase 静态托管使用 `index.html` 作为首页和 4xx 错误文档，使前端页面路由由 Vue Router 解析。
* `/api/*` 必须在 CDN/网关层单独回源到后端服务，不得回源 COS 或被 SPA fallback 返回 `index.html`。
* 后端备用静态入口仅对浏览器导航路径回退到 `index.html`，API 请求和带扩展名的静态资源缺失时保持 404。

决策原因：

* 线上 `jorycen.fun` 响应头表明请求进入 Tencent-COS；直接访问 `/login` 时 COS Key 为 `%2F%2Flogin`，说明双斜杠产生于云端回源路径拼接，而非前端登录跳转代码。
* History 模式的前端路由不是 COS 中的真实对象，必须通过静态托管错误文档回到 `index.html`。
* API 和静态资源不能与 SPA 页面 fallback 混用，否则 API 错误会被伪装为 HTML，缺失资源也会被错误缓存。

备选方案：

* 方案一：切换 Hash 路由，绕过服务器路由回源问题，但会改变现有 URL 结构。
* 方案二：所有请求无条件回退到 `index.html`，实现简单但会破坏 API 和静态资源错误语义。
* 方案三：保留 History 路由，配置静态托管 fallback，并在 CDN/网关层分离 `/api/*` 回源。

最终选择：

* 采用方案三，保留可读的业务页面 URL，并明确区分页面路由、静态资源和 API 回源。

状态：已确认

## 2026-07-14 商品与定价批量上传异步处理及失败清单

决策时间：2026-07-14

决策内容：

* 商品批量上传保持现有匹配口径：厂商编码命中已有商品时更新，不存在时新增。
* 商品和定价文件提交后，先校验文件格式、表头和数据行；校验失败的行进入失败清单，正确行继续处理。
* 商品和定价批量上传改为后台异步任务，前端展示排队中、处理中、已完成、部分失败和失败状态，不再将请求超时提示为导入失败。
* 定价导入预先批量加载商品、厂商编码和现有价格，并按批次写入价格及价格变更记录，减少大批量文件的逐行数据库往返。
* 异步任务保留原始上传文件、任务状态、处理结果和失败行快照，支持任务恢复和失败清单下载后重新导入。

决策原因：

* 当前商品导入和定价导入的校验与导入过程耦合，格式或数据错误不便于统一返回和修正。
* 定价导入逐行查询和写入，数据量增大后容易超过前端请求等待时间，造成“导入失败”的错误认知。
* 按库存批量维护的部分成功模式处理，可以减少整批返工并保留错误行的可追溯性。

备选方案：

* 方案一：继续同步处理并仅延长请求超时时间。
* 方案二：任一行失败时整批拒绝。
* 方案三：文件提交后创建异步任务，正确行继续处理，失败行形成失败清单。

最终选择：

* 采用方案三，商品导入保持原有新增/更新业务口径，定价导入采用批量查询和批量写入优化。

状态：已确认

## 2026-07-13 通用审批中心与组织审批关系

决策时间：2026-07-13

决策内容：

* 建设通用审批中心，流程由流程定义、审批实例、审批任务和审批操作日志组成。
* 审批主题人作为组织范围解析依据；新增员工直属上级和门店明确店长字段。
* 审批节点支持串行签批和或签；或签任一人通过即可，全部审批人拒绝后节点才拒绝。
* 审批拒绝后由申请人修改后重新提交，原审批轮次和操作日志保留。
* 已发布流程采用版本快照，进行中的审批不受新版本影响。
* 通用审批引擎不直接修改库存、财务和商品数据，审批通过后的业务动作由业务模块在事务中执行。

决策原因：

* 审批人和级数经常变化，路由规则应配置化；库存和财务结果又必须保留业务模块的强校验和审计能力。

最终选择：

* 采用“通用审批引擎 + 动态审批人解析 + 业务模块适配”的方案。

状态：已确认

## 2026-07-12 供应商服务商属性与非服务商毛利上浮

决策时间：2026-07-12

决策内容：

* 供应商新增“是否服务商”和“非服务商每件毛利上浮金额”。
* 服务商毛利商品成本使用商品产品定价。
* 非服务商毛利商品成本使用采购价加每件固定上浮金额。
* 新规则只作用于未归档订单；已归档订单继续使用已有毛利快照，不因供应商配置变化自动调整。
* SN 商品在入库时保存采购来源供应商；非 SN 商品当前按门店最近完成入库的采购供应商识别来源。

决策原因：

* 服务商的合作价值已经体现在产品定价口径，非服务商需要以实际采购价加固定上浮额作为毛利成本。
* 归档订单是经营和业绩历史事实，不能因主数据后续调整发生漂移。

备选方案：

* 方案一：所有供应商统一使用产品定价。
* 方案二：非服务商使用产品定价加上浮金额。
* 方案三：服务商使用产品定价，非服务商使用采购价加每件固定上浮金额。

最终选择：

* 采用方案三，并按未归档读取最新配置、归档保存计算快照的方式实施。

状态：已确认

## 2026-07-10 库存批量导入部分成功与库存成本口径调整

决策时间：2026-07-10

决策内容：
* 库存批量维护导入从“任一行失败整批拒绝”调整为“单条校验正确即可生成申请，失败行返回异常记录并支持下载后重导”。
* 批量维护导入页采用二级导入页面，批次级统一选择操作类型、SN/非SN导入类型、标准仓位和资源权益，Excel 只保留门店ID、商品编码、PN、SN或数量、提货价和备注等行级字段。
* SN 商品导入按“一行一台 SN”处理，数量固定为 1；非 SN 商品导入不填写 SN，按数量处理。
* 商品成本不在批量维护导入页直接写入商品价格表；导入页只记录本次入库的 SN 单台成本或非 SN 入库成本来源。
* 商品管理-价格管理中的库存平均成本由刷新成本逻辑计算：SN 商品按当前在库 SN 个别成本平均，非 SN 商品按当前剩余 FIFO 成本层加权平均。
* 商品价格批量导入只维护定价、零售价和最低售价；空价格按 0 导入，不再设置生效日期，导入后立即生效。

决策原因：
* SN 商品需要能追溯到具体单台采购/提货成本，符合序列号商品个别计价的行业做法。
* 非 SN 商品适合按 FIFO 成本层管理，当前商品库存平均成本应来自剩余库存成本层，而不是简单历史入库平均。
* 导入页承担成本计算会混淆“库存事实录入”和“成本汇总口径”，应由价格管理/成本刷新统一计算和展示。
* 部分成功导入能减少批量维护返工成本，失败行下载后重导更符合日常运营效率。

备选方案：
* 方案一：继续整批预校验，任一行失败则不生成任何申请。
* 方案二：按行校验，正确行生成申请，错误行下载后修正重导。
* 方案三：批量维护导入时直接覆盖商品成本价。
* 方案四：导入只记录库存成本来源，商品成本由价格管理统一刷新。

最终选择：
* 采用方案二和方案四。

状态：已确认

---

## 2026-07-09 全局标准仓位替代物理库位管理
决策时间：2026-07-09

决策内容：
* 库位管理从“按门店维护物理库位”调整为“全局标准仓位配置”。
* 标准仓位固定为销售仓、样品仓、铺货仓、不可售仓、占用仓。
* 库位类型不再作为独立业务字段展示；系统内部使用仓位编码，业务人员维护仓位名称。
* 仓位设置后必须同步到权限范围内全部启用门店；新建门店自动生成标准仓位。
* 库存汇总展示必须与标准仓位关联，按销售仓、样品仓、铺货仓、不可售仓、占用仓展示结果。

决策原因：
* 当前经营管理需要的是库存业务仓位，不是门店内柜台、展台等物理位置。
* “库位类型”和“库位名称”同时由业务人员填写会造成重复和误填。
* 全局标准仓位能保证多门店库存口径一致，便于批量维护、库存汇总、审批和后续经营分析。

备选方案：
* 方案一：继续按门店维护柜台、展台、售后区等物理库位。
* 方案二：保留库存类型字段，同时让库位管理只维护全局标准仓位。
* 方案三：彻底重构库存表，将库存类型字段全部迁移为库位维度。

最终选择：
* 采用方案二。当前阶段保留库存数量字段，库位管理改为全局标准仓位，并同步到每个门店。

状态：已确认
---

## 2026-07-09 库位管理与占用库存类型
状态说明：已被“2026-07-09 全局标准仓位替代物理库位管理”替代，以下记录仅保留历史决策过程。

决策时间：2026-07-09

决策内容：
* 在系统设置中新增“库位管理”，用于维护门店下的物理库位。
* 库位属于门店主数据，支持库位ID、库位名称、库位类型、是否可销售和启停状态。
* 库位停用不做物理删除，历史库存、SN、入库、出库和批量维护记录继续保留原库位引用。
* “现有库存、铺货仓、样机、不可售、占用”定义为库存类型，不作为物理库位维护。
* `pending_qty` 统一命名为“占用库存”，用于订单或业务流程对库存的预占用。

决策原因：
* 库位描述货品在门店内的物理位置，库存类型描述货品的业务状态，两者混用会导致库存统计和业务流转混乱。
* 库位需要由经销商 admin 或 BOSS 统一维护，门店业务人员在入库、出库、调拨、SN清单和批量维护中引用。
* 历史库存追溯必须保留原库位，即使库位后续停用也不得删除历史事实。

备选方案：
* 方案一：将现有库存、铺货仓、样机、不可售、占用都作为库位维护。
* 方案二：将现有库存、铺货仓、样机、不可售、占用作为库存类型，另行维护门店物理库位。
* 方案三：不建设库位管理页面，仅通过数据库或导入时手工填写库位。

最终选择：
* 采用方案二，并在系统设置新增库位管理页面。

状态：已确认
---

## 2026-07-09 库存批量维护与经销商审批

决策时间：2026-07-09

决策内容：

* 库存批量维护统一采用 Excel 导入方式，导入后先生成批量维护申请单，审批通过前不得直接改变库存事实。
* 门店权限范围内发起的所有批量库存操作均必须由经销商总权限账号审批；审批人只允许通过或拒绝，不在审批环节修改申请内容。
* 审批拒绝后，由店长或申请人修改原始内容后重新提交申请；申请人不得审批自己的申请。
* Excel 导入必须整批预校验，任一行校验失败则整批拒绝，不生成库存变更、SN 变更或资源权益变更。
* SN 商品的批量维护以 SN 清单为准，每行对应一台 SN；不得用数量方式维护 SN 商品库存。
* 非 SN 商品的批量维护以门店、商品、库位和数量为核心，每行对应一组库存数量调整，并通过库存调整记录生效。
* 入库操作必须维护资源权益；SN 商品权益落到 SN 维度，非 SN 商品权益按本次入库批次管理。
* 出库操作可对同一批货品统一勾选是否按规则产生资源权益结果，勾选后整批按同一标准执行，不允许逐行使用不同标准。
* 批量操作审批通过后必须在同一事务中执行库存、SN 清单、非 SN 数量、资源权益和操作日志变更。
* 批量维护必须保留导入文件、申请人、审批人、审批时间、执行时间、调整前后值、失败原因和批次号，支持后续追溯。

决策原因：

* 库存数量、SN 清单和资源权益均属于库存事实，直接通过 Excel 覆盖会破坏库存、财务和经营追溯。
* 店长掌握门店现场情况，适合发起批量维护；经销商总权限账号承担组织内库存风险控制，适合作为审批责任人。
* SN 商品和非 SN 商品的库存口径不同，SN 商品必须逐台追溯，非 SN 商品才适合按数量聚合调整。
* 资源权益影响销售、返利、下账和经营核算，入库必须明确权益来源；出库是否触发权益结果应由整批统一规则控制。
* 审批人不修改申请内容可以保持责任清晰，避免审批环节变成二次录入并模糊申请人与审批人的责任边界。

备选方案：

* 方案一：Excel 导入后直接修改库存数据。
* 方案二：Excel 导入后生成批量维护申请单，审批通过后生效。
* 方案三：门店店长可直接执行批量库存维护。
* 方案四：门店发起的批量库存维护必须经经销商总权限账号审批。
* 方案五：非 SN 商品不管理资源权益。
* 方案六：非 SN 商品按入库批次管理资源权益。
* 方案七：出库资源权益逐行选择不同规则。
* 方案八：出库资源权益按同一批次统一勾选并统一执行。

最终选择：

* 采用方案二、方案四、方案六和方案八。

状态：已确认

---

## 2026-07-07 商品零售价与采购/费用报销闭环

决策时间：2026-07-07

决策内容：

* 商品价格管理中新增“零售价”，零售价作为小程序销售开单默认带入价。
* 原“产品定价”继续用于订单经营毛利中的商品成本，不与销售默认价混用。
* 最低售价继续作为低价审批边界，且必须小于或等于零售价。
* 采购申请新增付款方式：公司账期和个人垫付。
* 采购申请选择个人垫付时自动生成报销单，待领导审批后生成个人报销结算单。
* 费用管理按区域、报销类型、费用发生方、支付方式、发票信息等财务字段建模。
* 报销类型进入系统设置单独维护，不再写死在页面中。
* 费用或采购选择“财务对公/公司账期”时进入应付管理待付款清单；选择“私人垫付/个人垫付”时进入报销审批及报销结算流程。
* 个人垫付审批通过后生成报销结算单，报销人不得自审。
* 报销结算单提交后进入付款管理统一付款。

决策原因：

* 销售默认价、毛利成本价和最低售价代表不同业务含义，混用会导致销售开单、低价审批和经营毛利互相污染。
* 公司对公费用本质是外部待付款，应进入应付管理；员工个人垫付本质是内部报销，应先经过审批。
* 报销类型需要由业务自行维护，避免每次新增费用类型都改代码。

最终选择：

* 采用“零售价作为销售默认价、产品定价作为毛利成本、最低售价作为审批底线”的三价口径。
* 采用“公司对公进入应付、个人垫付进入审批后报销结算”的费用闭环。

状态：已确认

---

## 2026-07-06 供应商返利账户后台化与收款账户隔离

决策时间：2026-07-06

决策内容：

* 供应商返利账户定义为系统内部受限额度账户，不属于客户收款方式，也不由用户手工创建、编辑、删除或排序。
* 返利上账时后台按供应商自动查找并创建唯一返利内部账户；已停用的历史返利账户自动恢复使用。
* 供应商返利内部账户不在“系统设置－结算账号管理”和“财务管理－账户中心”展示。
* 返利余额继续在“返利管理”和采购返利抵扣场景展示，账户内部流水仍完整保留。
* 收款方式的客户实收或默认结算账户只能绑定启用的资金账户；政策补贴应收只能绑定启用的政策补贴应收账户。
* Care 可用金不得绑定到收款方式，但继续在账户中心展示并按现有受限用途管理。
* 前端下拉过滤与后端接口校验同时执行，禁止通过接口绕过账户类型限制。
* 历史误绑定到收款方式的内部账户由自动迁移解除绑定，不删除账户或历史流水。

决策原因：

* 供应商返利是后台产生并用于采购抵扣的额度，不是客户实际支付渠道或银行到账账户。
* 要求用户先配置返利结算账号会暴露内部实现并阻断正常返利上账。
* 将内部额度账户与真实资金账户隔离，可以避免收款、付款和日结错误选择返利或 Care 额度。

备选方案：

* 方案一：继续在收款方式弹窗中由用户维护供应商返利账户。
* 方案二：把返利账户移到独立配置页，仍要求用户手工维护。
* 方案三：返利账户完全后台化，用户只查看返利业务余额和流水。

最终选择：

* 采用方案三。

状态：已确认

---

## 2026-07-06 返利预上账与返利下账并行核销

决策时间：2026-07-06

决策内容：

* “返利管理”恢复“返利上账”功能，但不再直接写一条无来源单据的流水，而是生成正式返利上账单。
* 返利上账单生效后立即增加对应供应商返利可用余额和账户流水，采购可以随即使用该余额。
* 返利下账与返利上账并行运行；返利下账只确认厂商实际返利并与返利上账单核销，不再增加供应商返利余额。
* 返利必须先上账才能使用或核销，不允许未上账返利通过返利下账直接增加余额。
* 一张返利上账单可以分多次核销，一张返利下账单也可以关联多张返利上账单；每笔核销保存独立分配金额。
* 上账单和下账单均展示原金额、已核销金额及剩余待核销金额，支持待核销、部分核销和已核销状态。
* 核销金额不得超过上账单或下账单的剩余待核销金额，且只能核销同一供应商的单据。
* 撤销返利下账核销只释放双方核销金额，不生成供应商返利或账户反向流水。
* 返利上账单已有核销记录时必须先撤销下账核销；供应商仍有采购占用返利时必须先完成采购退单，之后才能冲销上账单。
* 返利余额流水不提供直接冲销入口，所有上账纠错必须从返利上账单发起并保留反向流水。
* finance、admin 和 BOSS 均可新增、查询、核销、撤销核销及冲销返利单据。
* 新增返利上账单表和上账/下账核销分配表；返利下账记录增加已核销金额。

决策原因：

* 返利可用于采购的时间与厂家完成对账下账的时间可能相隔较长，要求等到下账完成才可使用会阻塞正常采购。
* 预上账和实际下账代表两个不同业务事实，必须使用独立单据并通过核销关系勾稽。
* 下账核销不再重复增加余额，可以避免同一笔返利在预上账和实际下账时被计算两次。
* 多对多金额分配能够覆盖一次上账分批下账、一次下账覆盖多笔上账的实际对账场景。
* 采购占用未解除时禁止冲销上账单，可以避免返利余额成为负数或破坏已经发生的采购抵扣。

备选方案：

* 方案一：返利实际下账后才形成可用余额。
* 方案二：返利预上账立即可用，下账时再重复增加余额。
* 方案三：预上账立即可用，下账只做金额核销。
* 方案四：上账单与下账单一对一关联。
* 方案五：使用多对多核销分配并支持部分核销。

最终选择：

* 采用方案三和方案五。

状态：已确认；替代同日“返利下账独立页面与手工待下账返利”中关于停用返利上账及下账增加余额的决策。

---

## 2026-07-06 返利下账独立页面与手工待下账返利

决策时间：2026-07-06

决策内容：

* 将“资源下账”从“资源权益核销与成本调整”中移出，在财务管理“国补应收单”后新增独立“返利下账”页签。
* 返利下账页统一展示销售使用、公司套回、厂家返利和手工返利形成的待下账及历史下账记录。
* 手工返利作为独立类型管理，不要求关联 SN 或商品；新增时必须选择供应商、填写大于零的金额和备注。
* 手工返利新增后只生成待下账记录，不增加供应商返利余额，不生成账户流水。
* 手工返利实际到账后，由授权账号点击下账，金额进入该供应商唯一启用的供应商返利账户，并同步生成供应商返利流水。
* finance、admin 和 BOSS 均可查询、新增、下账、取消和冲销返利下账记录。
* 手工待下账返利可以填写原因后取消；已下账记录发生错误时必须通过冲销生成反向账户及返利流水，不允许物理删除。
* 页面支持按创建时间范围、SN、备注模糊查询、返利或资源类型、来源类型、状态和供应商组合筛选。
* 待下账记录继续按待处理优先、创建时间倒序和唯一 ID 倒序排序，并在数据库层完成筛选、排序和分页。
* 移除“返利管理”中的直接“返利上账”入口；普通手工返利必须统一经过“新增待下账－实际到账后下账”的两阶段流程。
* 原直接返利上账接口停止使用，防止绕过待下账流程直接增加供应商返利余额。
* `T_RESOURCE_SETTLEMENT` 的 SN 和商品字段调整为可空，并增加创建、取消、冲销操作人及时间字段；历史资源下账数据保持不变。

决策原因：

* 厂商承诺返利与实际到账是两个不同业务事实，承诺金额不能提前形成可用返利余额。
* 活动返利可能按整场活动或期间承诺，不一定能分摊到具体 SN，因此手工返利不能强制绑定 SN。
* 复用现有待下账表和账户流水可以统一自动返利与手工返利的到账口径，避免建立两套重复账。
* 取消和冲销保留原记录及反向流水，能够满足财务追溯和余额一致性要求。

备选方案：

* 方案一：继续使用“返利上账”直接增加余额，手工备注厂商尚未实际到账。
* 方案二：手工返利先进入待下账，实际到账后才增加对应供应商返利余额。
* 方案三：所有手工返利必须绑定 SN。
* 方案四：活动级手工返利不绑定 SN，只绑定供应商。
* 方案五：错误记录直接删除或修改余额。
* 方案六：待下账记录取消、已下账记录冲销并保留完整流水。

最终选择：

* 采用方案二、方案四和方案六。

状态：已被“返利预上账与返利下账并行核销”决策部分替代；独立返利下账页面、手工返利不绑定 SN、查询及权限规则继续有效。

---

## 2026-07-06 SN库存清单与经销商SN销售特价

决策时间：2026-07-06

决策内容：

* 在“库存管理－库存汇总”后新增“SN库存清单”，默认只展示当前在库 SN。
* 清单按 SN 展示 PN、商品名称、门店、库位、资源权益当前状态、商品统一售价、经销商 SN 特价、当前适用售价、库龄和备注。
* SN 特价属于销售售价，不改变商品“产品定价”、库存成本、订单经营毛利成本口径或历史订单。
* SN 特价按 `SN + 经销商` 独立保存；当前适用售价优先使用 SN 所在门店所属经销商的有效 SN 特价，没有特价时回退到最新商品统一售价。
* 商品统一调价不得覆盖已有 SN 特价；取消 SN 特价后立即回退到最新商品统一售价。
* SN 跨经销商门店调拨后，只读取目标门店所属经销商配置的价格，不沿用其他经销商价格；各经销商历史配置继续保留。
* 经销商 admin 只能维护本经销商当前在库 SN 的特价，BOSS 可以按 SN 当前门店所属经销商维护；其他账号只允许查看授权门店范围内的库存清单。
* 销售选择 SN 后自动带入当前适用售价，销售人员仍可修改实际成交价。
* SN 特价低于商品最低售价时，销售订单继续进入现有低价审批，不因设置 SN 特价而绕过审批。
* SN 特价的设置、修改和取消必须生成不可删除的变更记录；已归档订单不随 SN 特价或统一售价变化自动改写。

决策原因：

* 同一商品的单台库存可能因库龄、外观、资源状态或清库策略需要不同售价，商品级统一价格无法表达单台差异。
* 经销商经营策略相互独立，价格必须同时绑定 SN 和经销商，避免跨组织调拨或查看时误用其他经销商价格。
* 使用独立覆盖价而不是复制统一价格，可以保证统一调价时只影响未设置特价的 SN，并保留明确的价格优先级。
* 保留最低售价审批和完整调价记录，可以在支持清库定价的同时控制低价销售风险。

备选方案：

* 方案一：直接把特价字段写入 SN 主表，全系统共用一个 SN 特价。
* 方案二：按 `SN + 经销商` 保存独立特价，统一价格作为回退值。
* 方案三：SN 特价低于最低售价时视为预审批，销售不再进入低价审批。
* 方案四：SN 特价只作为默认销售价，实际成交价仍按最低售价规则校验。
* 方案五：库存清单同时默认展示已售和退库 SN。
* 方案六：库存清单默认只展示在库 SN，历史状态通过 SN 追踪查询。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-07-05 订单经营毛利统一口径与计算快照

决策时间：2026-07-05

决策内容：

* 订单经营毛利统一调整为：用户应收－产品定价－应收税率费用－增值税＋补录净额。
* 用户应收按订单商品总额扣除普通折扣计算；国补和教育补贴分别由国家及厂商承担，均保留在毛利收入中，不从用户应收口径扣除。
* 应收税率费用按用户应收在各收款方式中的分配额乘对应默认税率计算；默认税率在系统设置的收款方式管理中维护。
* 增值税统一按 `max(开票金额－产品定价, 0) × 13%` 计算。
* 产品定价默认采用商品采购价；尚未定价的商品首次采购入库时自动写入，之后可以在“商品管理－价格管理”调整，后续入库不得覆盖非零定价。
* 补录项目在字典中配置增加或减少方向，订单录入时保存方向快照。
* 每张订单单独保存毛利计算快照、公式版本、收款税率明细、产品定价明细和补录明细。
* 订单归档时生成最终毛利快照；未归档订单允许试算；归档快照不因后续收款方式税率修改自动变化。
* 新毛利快照替代原“销售额－销售结算成本”口径，作为经营看板经营毛利及员工业绩基础毛利。
* 商品、产品线毛利按订单商品销售小计占订单销售总额的比例分摊；员工仍按主销售人和辅助销售人平均拆分订单基础毛利。
* 历史订单未生成新毛利快照时暂按原归档毛利兼容展示；首次查看、毛利调整申请或授权重算后生成新快照。

决策原因：

* 收款渠道费用、开票增值税和补录费用会实际影响订单经营收益，仅使用销售额减产品定价会高估或低估毛利。
* 订单级快照可以保证历史报表不随税率字典、补录字典或当前商品成本变化；公式升级时统一重算旧版本快照。
* 明细快照能解释每张订单毛利，并为后续门店、收款方式、商品和补录项目分析提供稳定事实表。

备选方案：

* 方案一：只在小程序前端临时计算，不保存数据库。
* 方案二：继续保留原毛利作为看板口径，新公式仅供订单查看。
* 方案三：新公式形成订单快照，并统一替换经营看板和员工业绩基础毛利。

最终选择：

* 采用方案三。

状态：已确认

---

## 2026-07-05 经营数据看板实时口径、辅助销售拆分与重点产品标记

决策时间：2026-07-05

决策内容：

* 在现有 `/reports` 模块建设“经营数据看板（结果层）”，保持 ERP 全局导航不变，当前不新增看板左侧六个子菜单。
* V1 使用 `T_ORDER`、`T_ORDER_ITEM`、`T_PRODUCT`、库存和组织主数据进行实时 SQL 聚合，不建设日汇总表或 BI 缓存表。
* 后端建立统一看板数据源接口，V1 使用实时 SQL 数据源，预留未来切换门店、员工和商品日汇总表的数据源实现。
* 看板只统计已归档销售订单，经营日期按订单 `CREATE_TIME` 归属，统一使用 `Asia/Shanghai` 时区。
* 销售额按订单明细 `SUBTOTAL` 汇总；经营毛利使用订单归档时保存的订单经营毛利快照，历史缺少新快照的订单沿用既有兼容毛利口径。
* 员工业绩毛利使用订单归档基础毛利加已审批业绩毛利调整，不改写订单经营毛利。
* 一个订单存在辅助销售人时，主销售人与全部辅助销售人平均拆分销售额、基础毛利和已审批业绩毛利调整；参与单量对每名参与人记一笔。
* 员工业绩明细必须展示订单、主销售或辅助销售身份、参与人数、分摊结果、毛利调整金额及调整原因。
* 商品一级分类作为产品线统计口径。
* 商品主数据新增“重点产品”标记，由商品管理维护；重点产品看板只展示已勾选商品的实际经营结果。
* 毛利和库存金额仅对 BOSS、admin、财务及店长开放；店长继续受授权门店范围限制，普通店员和采购角色不显示成本与毛利。
* 所有看板查询继续使用账号精确门店权限；前端不计算核心经营指标、同比或环比。
* 当前没有库存历史快照，库存金额同比和环比显示为不可用，不使用当前库存反推历史库存。

决策原因：

* 订单归档经营毛利快照是已经确认的经营事实，使用当前商品成本、当前收款税率或临时 `cost_price` 重算会导致历史经营结果漂移。
* 辅助销售只保存参与人员、没有比例字段，平均拆分是当前最简单、可解释且可审计的最小方案。
* 经营毛利与员工业绩毛利服务于不同管理目标，独立展示可以避免业绩奖励污染公司经营利润。
* 重点产品属于经营策略标记，必须来源于明确配置，不能用销售排名临时替代。
* 实时 SQL 足以支持当前数据规模；通过数据源接口隔离查询实现，可以在数据量增长后替换为日汇总表而不修改前端协议。
* 没有库存历史事实时生成库存同比、环比会制造无法验证的数据，因此明确展示不可用。

备选方案：

* 方案一：前端分别计算卡片、图表和排名指标。
* 方案二：后端统一实时聚合并预留日汇总数据源。
* 方案三：本期立即建设日汇总表、定时任务和缓存层。
* 方案四：辅助销售只展示订单，不拆分金额和毛利。
* 方案五：主销售和辅助销售平均拆分。
* 方案六：新增自定义分摊比例字段和审批流程。

最终选择：

* 采用方案二和方案五；自定义比例、日汇总表及缓存层作为后续扩展。

状态：已确认

---

## 2026-07-04 国补应收分次到账、区域账户路由与差额审批

决策时间：2026-07-04

决策内容：

* 国补政策补贴应收与实际银行到账分开管理；政策补贴应收账户不得直接作为银行资金账户使用。
* 国补到账账户按区域配置并允许切换；成都区域默认到账四川银行（艾诺云），重庆区域默认到账兴业银行（艾诺志兴），地市区域暂不配置。
* 区域账户配置变更只影响后续到账登记，历史到账记录保存实际账户快照，不随配置变化。
* 地市区域未配置到账账户时不得自动使用成都、重庆或其他账户，必须先完成配置再登记到账。
* 一笔银行到账可以核销多张国补应收单，一张国补应收单也可以被多次到账核销。
* 国补应收状态按累计核销结果区分为待回款、部分到账、已到账和差额结清。
* 银行到账超过本次分配金额的部分保留为未分配到账款，可后续继续分配或退款。
* 手续费和确认无法收回的差额按财务配置记录；任何差额核销均必须审批。
* 差额审批由财务提交、admin 或 BOSS 审批，申请人不得审批自己的申请。
* 错误到账、核销、差额及退款记录通过冲销生成反向记录，不允许物理删除。
* 历史已下账国补应收记录保持原样，不迁移为新的到账单。

决策原因：

* 应收资产和银行资金属于不同业务事实，直接把应收下账到应收账户无法表达实际资金进入哪个对公账户。
* 国补回款存在分批到账、一笔回款覆盖多张应收、手续费、尾差和超额到账等常态场景，整笔下账无法准确对账。
* 区域账户配置化可以适应公司对公账户变化，同时通过到账快照保证历史可追溯。
* 差额会直接影响应收资产和经营损益，必须审批并保留原因、凭证和操作链路。

备选方案：

* 方案一：继续按应收单整笔下账，不区分应收账户和实际银行账户。
* 方案二：仅在应收单上累计填写到账金额，不建立银行到账单和分配明细。
* 方案三：建立银行到账单、应收核销明细、未分配款和差额审批，并按区域配置默认到账账户。

最终选择：

* 采用方案三。

状态：已确认

---

## 2026-07-03 销售订单定金预占与详情扩展信息持久化

决策时间：2026-07-03

决策内容：

* 正式订单提交时不再立即核销所选定金，改为生成预占记录并把定金状态设为“已占用”。
* 订单归档时才把该订单的预占金额转为已核销金额，并把定金状态设为“已核销”。
* 订单作废时，无论订单是否已经归档，都撤销该订单对应的预占或核销记录，并把定金恢复为“可使用”。
* 二级来源、辅助销售人、云闪付订单号、开票金额、国补照片、商品照片、教育补贴凭证和个人资料照片作为订单正式字段持久化到 MySQL，并由订单详情接口返回。
* 定金状态变化与订单创建、归档、作废使用同一个数据库事务，不由小程序发起独立的补偿请求。

决策原因：

* 未归档订单仍可能作废，创建时直接核销会过早确认定金使用事实。
* 定金状态和订单状态分开请求会产生一边成功、一边失败的数据不一致。
* 订单扩展信息此前只存在于前端请求中，后端模型未定义对应字段，导致保存后详情为空。

备选方案：

* 方案一：继续在创建订单时立即核销，取消时再反向恢复。
* 方案二：提交时预占、归档时核销、作废时释放，并在订单事务内完成。
* 方案三：把缺失字段临时保存在小程序本地缓存。

最终选择：

* 采用方案二；订单扩展信息直接进入订单主表，不采用仅本机可见的本地缓存方案。

状态：已确认

---

## 2026-07-02 采购货型模板与资源子内容配置

决策时间：2026-07-02

决策内容：

* “货型”作为采购时快速选择多个资源子内容的组合模板，不恢复单一货型覆盖库存权益的旧模型。
* 货型和资源子内容均在系统设置中维护；新增、编辑和删除采用独立接口，删除按停用处理。
* WEB 和小程序采购申请选择货型后，自动把模板中的采购可选资源带到每个商品明细，并允许逐项调整。
* 商品资源成本定义、SN权益和其他权益选择场景统一读取资源子内容配置，不维护独立的硬编码下拉列表。
* 供应商属于全员可读的基础资料；所有已登录账号均可查询，新增、修改、删除仍要求采购管理权限。
* 采购申请查询和提交对所有已登录且已分配门店范围的账号开放；审批、撤销和供应商维护仍要求采购管理权限。

决策原因：

* 操作人员需要用“服务商全资源货”等业务名称快速录入，但同一 SN 的国补、教育补贴、返利等权益仍可能独立核销或套回。
* 组合模板兼顾录入效率与多权益状态模型，避免货型切换覆盖单项权益。
* 供应商查询不涉及门店业务数据，采购申请中的基础资料读取不应被采购角色或门店范围阻断。
* 采购申请是门店提出采购需求的入口，不应要求申请人同时具备采购审批职责；门店范围隔离足以约束申请数据。

最终选择：

* 采用“货型模板 + 可配置资源子内容 + SN独立权益”的模型，并只放开供应商读取权限。

状态：已确认

---

## 2026-07-02 腾讯云数据库营业时段心跳保活

决策时间：2026-07-02

决策内容：

* 保留腾讯云数据库自动暂停能力，不在云平台关闭自动暂停。
* 后端在中国标准时间 `Asia/Shanghai` 每日 09:30（含）至 23:00（不含）之间，每 7 分钟执行一次 `SELECT 1`。
* 数据库心跳复用现有 Sequelize/mysql2 连接池，不创建独立 Sequelize 实例或重复连接池。
* 后端在营业时段内启动或重启时立即执行一次心跳，此后按配置间隔继续执行。
* 非营业时间不执行数据库心跳，让数据库按腾讯云策略自然休眠。
* 心跳失败最多重试 3 次，每次间隔 2 秒；最终失败只记录日志，不终止或阻塞 HTTP 主服务。
* 心跳开关、起止时间、时区、执行间隔和重试参数均通过环境配置维护。
* 本次不增加数据库 warmup 接口，不修改小程序启动流程，也不增加前端预热提示。

决策原因：

* 当前主要问题是营业时间内数据库因空闲自动暂停，导致小程序首次业务访问出现 20 至 30 秒唤醒等待。
* 营业时段低成本执行 `SELECT 1` 可以维持数据库活跃，同时保留非营业时间自动休眠带来的资源节省。
* 复用现有连接池可以避免重复连接、额外资源占用和连接生命周期不一致。
* 心跳属于辅助保活能力，失败不能改变主服务可用性或替代现有请求触发恢复机制。

备选方案：

* 方案一：继续只使用请求触发和后台恢复，不增加营业时段心跳。
* 方案二：关闭腾讯云数据库自动暂停，数据库全天保持运行。
* 方案三：保留自动暂停，仅在营业时段使用后端连接池执行心跳。
* 方案四：增加小程序 warmup 接口和启动预热提示。

最终选择：

* 采用方案三；本次不采用方案四。
* 本决策替代 2026-06-26 数据库恢复决策中“不使用高频心跳”的部分，其余后台恢复、请求触发激活和健康检查机制继续保留。

状态：已确认

---

## 2026-07-01 国补 POS 收款拆分与定金抵扣展示

决策时间：2026-07-01

决策内容：

* 国补 POS 参与补贴的商品金额拆分为“客户实收”和“政策补贴应收”两类订单收款记录。
* 客户实收 = 国补 POS 参与补贴金额 - 政策补贴金额，表示订单当下实际收到的客户款。
* 政策补贴应收 = 国补优惠金额，表示后续待政策结算到账的应收款。
* 当前实收校验只统计客户实收、普通收款和定金抵扣，不把政策补贴应收重复计入客户实付。
* 两类国补资金均保存到订单收款表，并在订单归档时分别生成日结明细。
* 客户实收沿用国补 POS 收款方式配置的资金账户；政策补贴应收使用独立的政策补贴应收账户。
* 政策补贴应收账户属于应收资产账户，不得与客户实收资金账户混用。
* 定金抵扣作为订单资金来源显示在订单详情收款信息中，同时继续保留独立的定金核销记录，避免重复形成现金收入。
* 历史仅保存国补 POS 净收款的订单，在详情展示时兼容拆分为客户实收和政策补贴应收。

决策原因：

* “立即到账/后到账”不能准确表达资金性质，容易把客户支付、政策应收和实际到账时点混为一体。
* “客户实收/政策补贴应收”符合订单资金来源与应收管理口径，也便于后续对账和到账核销。
* 将两类款项下账到不同账户，可避免政策待回款被误记为银行或 POS 已到账资金。
* 定金抵扣虽然不是本次新增现金，但属于订单实际使用的资金来源，应在订单详情完整展示。

备选方案：

* 方案一：使用“立即到账”和“后到账”作为收款方式名称。
* 方案二：使用“客户实收”和“政策补贴应收”，并保留原国补 POS 品类前缀。

最终选择：

* 采用方案二。

状态：已确认

---

## 2026-07-01 销售订单 PN/SN 校验延后至归档

决策时间：2026-07-01

决策内容：

* 新建未归档销售订单时，PN、SN 只作为订单明细文本保存，不校验对应商品、库存或 SN 是否存在。
* 新建订单不得因商品未建档、PN/SN 未录入或 PN/SN 暂不存在而拒绝提交。
* 新建订单不绑定 SN 库存记录、不占用库存、不锁定 SN 资源权益。
* 订单归档时必须校验 PN 对应商品存在；需要 SN 管理的商品必须校验 SN 存在、属于对应 PN 和当前门店且状态可售。
* 非 SN 管理商品如果填写了 SN，归档时同样校验该 SN 与 PN、门店和库存状态。
* PN/SN 校验通过后才扣减库存、绑定 SN、核销所选资源权益并生成归档相关成本和下账数据。

决策原因：

* 开单时可能先记录销售事实，商品、PN 或 SN 主数据尚未完成维护；创建阶段强校验会阻断订单录入。
* 归档是库存、权益和财务事实正式生效的时点，必须在该时点完成严格校验。
* 将文本录入与库存绑定分离，可以允许先开单，同时避免无效 PN/SN 进入已归档业务数据。

备选方案：

* 方案一：新建订单时立即校验并占用 PN/SN 库存。
* 方案二：新建订单只保存文本，归档时统一校验并扣减库存。

最终选择：

* 采用方案二。

状态：已确认

---

## 2026-06-28 定金提交即生效与部分抵扣

决策时间：2026-06-28

决策内容：

* 定金提交成功后立即进入收款员工名下的客户定金库，不再要求二次归档生效。
* 历史仍为“已提交”且存在可用余额的定金，在启动迁移时自动转为“可使用”。
* 正式订单允许填写本次定金抵扣金额，可部分抵扣；剩余余额继续保留在原定金单中。
* 本次抵扣金额必须大于零，且不得超过定金可用余额或订单扣除其他优惠后的应付金额。
* 一张正式订单仍最多绑定一张定金单；店员仍只能使用自己收取、且客户会员ID/电话匹配的定金。
* 订单取消或审批拒绝时，只释放该订单实际抵扣的金额。

决策原因：

* 二次归档增加重复操作，不能为定金可用性提供额外业务价值。
* 全额一次核销无法覆盖定金余额大于订单金额或客户分次消费的实际场景。
* 保留员工归属、客户匹配、余额上限及事务锁校验，可以在简化流程的同时控制越权和超额抵扣风险。

备选方案：

* 方案一：继续提交后归档、全额一次核销。
* 方案二：提交即生效，但仍只允许全额核销。
* 方案三：提交即生效并支持部分抵扣，剩余余额继续使用。

最终选择：

* 采用方案三。

状态：已确认

---

## 2026-06-27 SN级商品资源权益、PO奖励与销售个人Care可用金

决策时间：2026-06-27

决策内容：

* 商品资源权益统一绑定到 SN，而不是只绑定商品、采购单或客户。
* 采购申请明细允许勾选多种商品资源权益；采购审批通过并入库后，入库 SN 按采购明细权益生成对应 SN 权益。
* 系统设置新增资源权益配置能力，权益可配置为销售使用、内部标记、PO奖励、Care可用金、返利或其他类型，并配置是否采购可选、是否销售触发、是否生成下账、是否生成销售个人 Care 可用金、是否计入销售者业绩毛利。
* PO奖励来源按商品、供应商和权益类型配置规则，支持固定金额、按库存成本比例、按销售金额比例等算法，并预留生效时间和触发条件。
* PO奖励可选择是否按配置比例计入销售者业绩毛利；计入后影响员工提成口径，但不改写订单原始毛利、库存成本或财务利润。
* 套装资格只作为内部标记，不参与销售校验；销售完成后可作为后续组合销售提醒依据。
* Care卡可用金属于销售个人可用金，不属于客户；销售具备可佣金属性的商品或权益后，可为销售人员生成个人 Care 可用金流水。
* 采购后允许对未销售在库 SN 批量调整权益，也允许按最新规则批量刷新未销售在库 SN 的权益金额和规则引用。
* 规则修改后的批量刷新不得影响已归档销售单，不得自动改写历史销售单、已触发的权益、已确认的毛利调整或已归档业绩口径。

决策原因：

* 电脑零售经营中，同一商品不同批次、不同供应商、不同政策期可拥有不同权益，SN 级记录才能准确追溯到单台机器。
* 采购时确定权益符合真实进货批次管理，后续批量调整和规则刷新用于处理政策补录、供应商变更和金额修正。
* PO奖励、公司返利、销售提成和 Care 可用金属于不同核算目标，必须可配置、可追溯且不能混入库存成本。
* 已归档销售单是历史经营事实，规则变化不能导致历史订单和员工提成口径漂移。

备选方案：

* 方案一：权益只按商品统一配置，不落到 SN。
* 方案二：权益按 SN 保存，采购入库时继承采购明细权益。
* 方案三：PO奖励固定写死在商品字段中。
* 方案四：PO奖励按商品、供应商和权益类型配置规则。
* 方案五：规则修改后自动重算全部历史销售单。
* 方案六：规则修改只刷新未销售在库 SN，已归档销售单不自动改写。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-26 Recoverable database activation for sleeping cloud MySQL

Decision time: 2026-06-26

Decision:
* The backend HTTP service must start even when cloud MySQL is sleeping or temporarily unreachable.
* Startup database activation and migrations run in a bounded background recovery loop instead of forcing the process to exit.
* Protected API requests trigger database activation before entering business routes when the database is marked unavailable.
* Transient database connection errors mark the database unhealthy and start background recovery.
* Recovery uses bounded exponential retry and a low-frequency startup retry interval, not a high-frequency heartbeat.
* Add an unauthenticated database readiness endpoint for operational diagnosis.

Reason:
* If the backend process exits during database sleep, mini-program traffic can no longer trigger the database resume path.
* Tencent cloud database wake-up can take longer than a single short connection timeout.
* The API layer must remain alive so the first business request can act as the activation trigger.
* Manual database login should not be part of the production recovery path.

Alternatives:
* Keep failing backend startup when database warmup fails.
* Use frequent ping heartbeats to keep the database awake.
* Start HTTP first and recover database availability through request-triggered and background activation.

Final choice:
* Start HTTP first, run database initialization in recoverable background mode, gate API routes on `ensureDatabaseReady`, and expose `GET /api/v1/health/db`.

Status: confirmed

---

## 2026-06-26 Database access stability for mini program and cloud MySQL

Decision time: 2026-06-26

Decision:
* The standard data access path is mini program or web client -> backend HTTP API -> Tencent Cloud MySQL.
* Clients must not connect to MySQL directly or carry database credentials.
* The backend owns database connection lifecycle management through the Sequelize/mysql2 pool.
* Backend startup must warm up the database connection before exposing the API service.
* Transient database connection failures use bounded retries with exponential backoff, not high-frequency heartbeat pings.
* Client HTTP retry is limited to idempotent requests to avoid duplicate business writes.

Reason:
* Tencent Cloud serverless and lightweight database environments can close idle resources or introduce cold-start latency.
* Database connection recovery belongs in the backend because it has the credentials, pool, timeout, and permission context.
* A connection pool reuses stable connections and avoids per-request connection churn.
* Bounded retry handles cold-start and connection-lost cases without masking persistent database failures.
* Blindly retrying write requests from the client can duplicate orders, payments, inventory movements, or approvals.

Alternatives:
* Keep the client or cloud function layer directly accessing the database.
* Add frequent heartbeat pings to keep the database awake.
* Create a new database connection for every request.
* Centralize database access in the backend API with pooled connections, startup warmup, and bounded retry.

Final choice:
* Use backend-only database access with Sequelize pooling, startup warmup, query retry, transient DB errors reported as 503, and idempotent client HTTP retry.

Status: confirmed

---

## 2026-06-25 Production launch cleanup includes account center balances

Decision time: 2026-06-25

Decision:
* The production launch cleanup must clear sales orders, inventory data, inbound documents, inventory resource rights, transfers, split/assembly documents, purchase records, daily statements, expenses, pending payables, payables, rebates, and account center balance sources.
* Settlement account definitions and supplier payment account definitions are kept as master data.
* Account center balances are cleared by removing settlement account transactions and supplier rebate records.
* If preserved account master tables contain stored balance columns in the future, the cleanup script must back them up and reset those balance columns to zero.

Reason:
* Formal launch requires all test operational and finance balances to be removed so reports, inventory, payables, rebates, and account balances start from clean production data.
* Account definitions are reusable configuration; deleting them would create avoidable setup work and risk inconsistent payment method mapping.
* Balances in the current design are derived from transaction and rebate records, so clearing those sources is the correct minimal cleanup path.

Alternatives:
* Delete all account definitions and recreate them before launch.
* Keep account definitions and clear only the balance source records.
* Add stored-balance reset support for future schema changes.

Final choice:
* Keep account definitions, clear balance source records, and reset future stored balance columns if they exist.

Status: confirmed

---

## 2026-06-25 正式启用前测试数据清理范围

决策时间：2026-06-25

决策内容：

* 正式启用系统前，清理测试业务数据但保留系统配置、组织权限、门店、用户、供应商和商品基础资料。
* 保留商品主数据、PN、条码、商品分类、商品字段配置和商品当前价格。
* 不保留商品价格导入批次和商品价格变更历史。
* 不保留厂家返利政策和厂家价格历史。
* 不保留库存、SN、资源权益当前状态、库存单据、采购记录、销售记录、定金记录、审批申请、财务流水、结算流水、返利流水和资源下账记录。
* 供应商资料和供应商付款账户保留；供应商返利流水清空。
* 结算账户配置保留；结算账户流水清空，余额后续按流水重新计算。
* 清理脚本执行前必须统计目标表数据量并备份将被清理的表。
* 清理脚本必须要求输入明确确认词后才执行删除。

决策原因：

* 正式启用需要去除测试业务单据和库存流水，避免影响库存、财务、报表和审批结果。
* 商品、供应商、门店、账号、角色和系统配置属于上线基础资料，重建成本高且可直接复用。
* 厂家政策、厂家价格历史和价格导入历史多为测试验证数据，正式运营应重新录入或导入真实口径。
* 删除前备份目标表可以在误操作时恢复清理前状态。

备选方案：

* 方案一：重建数据库并重新录入全部基础资料。
* 方案二：只清理业务流水，保留基础资料和配置。
* 方案三：保留厂家政策、价格历史和价格导入批次。
* 方案四：清空厂家政策、价格历史和价格导入批次，正式启用后重新维护。
* 方案五：直接执行删除，不做备份。
* 方案六：先备份目标表，再强确认删除。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-25 用户账号停用与登录拦截

决策时间：2026-06-25

决策内容：

* 用户管理支持将账号状态调整为启用或停用。
* 新建账号默认启用；停用账号保留历史业务记录和权限配置，但不得登录系统。
* 登录接口必须拒绝停用账号。
* 服务端鉴权中间件必须在每次受保护接口请求时重新校验账号状态；账号被停用后，已有 token 在下一次接口请求时立即失效。
* 经销商管理员只能停用或启用自己管理范围内的账号，不得管理 BOSS 账号或其他经销商账号。
* 系统 BOSS 可按现有用户管理范围管理账号状态。
* 允许操作人停用自己的账号；停用原因当前不强制填写。
* 停用账号不删除、不清理历史订单、审批、库存、财务及操作归属。

决策原因：

* 账号停用属于账号生命周期管理，和删除账号不同，应保留完整审计链路。
* 离职、调岗和账号异常场景需要快速阻断登录和接口访问。
* 只在登录时校验无法处理已登录 token，因此必须在服务端鉴权层再次校验状态。
* 停用原因和审批流可以后续扩展，当前先满足门店账号风险控制的最小可行能力。

备选方案：

* 方案一：删除账号并清理权限。
* 方案二：停用账号，保留历史业务记录和权限配置。
* 方案三：只在登录时检查账号状态。
* 方案四：登录和每次服务端鉴权均检查账号状态。
* 方案五：停用必须填写原因并走审批。
* 方案六：当前停用原因不强制，后续按需要扩展操作日志和审批。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-21 用户门店精确分配与小程序切换范围

决策时间：2026-06-21

决策内容：

* 普通账号与门店采用多对多关系，由系统设置的用户管理逐个分配。
* 新建账号默认不分配门店；现有账号不自动迁移，由管理员逐个配置。
* 经销商管理员只能为账号分配该账号所属经销商的启用门店。
* 系统 BOSS 为普通账号分配门店时显示全部启用门店，并允许跨经销商分配；经销商管理员仍不得跨经销商分配。
* 系统 BOSS 始终拥有全部门店，不参与手工门店分配。
* 无门店账号允许登录，但门店列表为空，所有门店业务操作均禁止并提示联系管理员分配门店。
* 微信小程序只显示账号已分配门店；当前门店被取消授权后，清除本地选择并要求重新选择。
* 服务端按账号门店关系过滤数据并校验请求，禁止通过伪造门店 ID 越权访问。

决策原因：

* 区域权限无法准确表达同一区域内多家门店的差异化授权。
* 独立的账号门店关系符合最小权限原则，也适合员工跨店工作和后续门店扩张。
* 服务端统一校验可以避免仅依赖小程序或网页端选择范围造成越权。

备选方案：

* 方案一：账号默认拥有所属经销商全部门店。
* 方案二：继续使用区域权限近似表达门店权限。
* 方案三：建立账号与门店多对多关系并逐个分配。

最终选择：

* 采用方案三；新账号默认无门店，现有账号由管理员手工配置，BOSS 保持全部门店权限并可跨经销商为普通账号分配门店。

状态：已确认

---

## 2026-06-21 资源类别配置、统一账户中心与权益下账

决策时间：2026-06-21

决策内容：

* 采用统一账户中心、按账户类型分账的方案；真实资金、供应商返利和 Care 可用金统一展示，但分别维护账户类型和使用规则。
* 资源类别由系统设置维护，不再在程序中固定国补、教育补贴和销量报号三类；初始保留三类配置，并增加厂商返利用于下账管理。
* 资源类别可配置是否允许销售使用、是否允许公司套回、默认到账账户、排序和启停状态。
* 销售订单只能选择对应 SN 当前可用的权益；SN 没有该权益或权益不可用时，前后端均禁止使用。
* 销售使用权益和公司套回审批通过都会形成待下账记录，不直接增加任何账户余额。
* 国补、教育补贴、销量报号、厂商返利等只有财务点击下账后，才进入类别配置的目标账户。
* 目标账户可为资金账户、供应商返利账户或 Care 可用金账户；供应商返利账户必须绑定供应商。
* 厂商返利选择供应商返利账户类型时，系统按返利来源供应商自动匹配该供应商的唯一启用返利账户。
* 厂商返利下账到供应商返利账户后，继续沿用采购订单手工填写返利抵扣金额的方式；本次不处理抵扣比例。
* Care 可用金由指定资源权益下账形成，可在采购延保等特定商品时作为受限额度账户使用，账户用途通过配置说明明确。
* 返利手工增加功能保留；原“删除”调整为冲销并生成反向记录，禁止物理删除财务流水。
* 当前下账不要求上传凭证；数据结构预留批次号，未来扩展批量上传和批量下账。

决策原因：

* 权益被使用、形成应收和真实到账是不同业务时点，直接进入银行卡会虚增可用资金。
* 银行卡、供应商返利和 Care 可用金使用限制不同，必须分类型核算，但统一入口可以降低操作复杂度。
* 资源类别配置化后，新增政策项目不需要再次修改销售、库存和财务程序。
* 返利冲销保留原记录和修正原因，比直接删除更符合财务追溯要求。

备选方案：

* 方案一：所有金额都作为普通结算账户余额。
* 方案二：资金账户、应收权益和受限额度完全分离为不同入口。
* 方案三：统一账户中心展示，底层按资金、供应商返利和 Care 可用金分类分账。

最终选择：

* 采用方案三，并使用“业务产生待下账、财务确认后入账户”的两阶段流程。

状态：已确认

---

## 2026-06-20 用户多角色与权限管理

决策时间：2026-06-20

决策内容：

* 一个账号可以绑定多个角色，菜单、按钮、审批资格、数据范围和门店范围按全部角色权限取并集。
* 用户管理开放给经销商总权限账号和系统 BOSS；经销商只能管理本经销商及下属门店用户，BOSS 可以管理全部经销商用户。
* 用户门店范围原暂按所属经销商下全部门店处理；该临时方案已由 2026-06-21“用户门店精确分配与小程序切换范围”决策替代。
* 角色代码仅作为系统内部稳定标识，由系统自动生成，所有管理界面完全隐藏；用户侧只展示角色名称。
* 继续使用员工角色关联表保存多角色关系，并保留员工表原角色字段作为历史兼容主角色，不再作为权限判断的唯一依据。

决策原因：

* 同一员工可能同时承担店长、财务、采购或审批职责，单角色模型无法表达真实岗位组合。
* 权限取并集符合“承担该角色即拥有对应职责”的管理原则，也能避免在业务代码中维护大量组合角色。
* 经销商数据隔离可以防止跨组织查看和修改账号，BOSS 全局权限满足总部管理需要。
* 角色代码属于技术实现细节，展示角色名称可以减少操作人员理解和配置成本。

备选方案：

* 方案一：继续保持单账号单角色，为组合岗位新增大量复合角色。
* 方案二：账号绑定多个角色，各类权限统一取并集。
* 方案三：经销商和 BOSS 均可查看全部组织用户。
* 方案四：经销商仅管理本组织，BOSS 管理全部组织。
* 方案五：继续由管理员手工维护并展示角色代码。
* 方案六：角色代码由系统生成并在界面隐藏。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-20 库存资源权益管理

决策时间：2026-06-20

决策内容：

* SN库存本体与国补、教育补贴、销量报号等资源权益分离保存，每项权益独立维护状态。
* 未税属于SN税务属性，不作为资源权益，也不再与国补等权益合并为单一货型。
* 销售订单提交时只锁定订单明细实际选择的权益；归档时核销为已使用，取消或审批拒绝时释放。
* 公司资源套回采用运营提交、财务单级审批，申请人不得审批自己的申请。
* 套回申请提交后先锁定权益，财务通过后转为已套回，拒绝后恢复可用。
* 按商品和资源类型定义资源成本金额；财务确认套回后生成产品资源成本流水。
* 产品资源成本不自动修改销售结算成本，销售成本仍通过现有手工调整口径处理。
* 资源套回与厂家政策分别记账，通过来源类型和来源单号防止重复生成成本流水。
* 产品资源成本调整后不得小于零。
* 未税库存开票采用明确提示并由操作人确认继续，不强制拦截。
* 销售标签由系统根据SN税务属性和权益状态自动生成，销售人员不得手工修改。
* 首期只处理国补、教育补贴、销量报号及税务属性，不迁移现有测试数据。

决策原因：

* 同一台机器可同时具备多项资源，且各项资源可能分别被占用、核销或公司提前套回，单一货型字段无法表达真实状态。
* 权益状态、审批单和成本流水分离后，可以防止并发重复使用并保留完整审计链路。
* 产品资源成本与销售结算成本服务于不同核算目标，自动混算会改变销售毛利口径。

备选方案：

* 方案一：继续使用全资源货、国补货、未税货等单选货型。
* 方案二：库存本体、税务属性、资源权益、变更审批和成本流水分离。
* 方案三：套回审批后自动降低销售结算成本。
* 方案四：套回只生成产品资源成本流水，销售结算成本保持手工调整。

最终选择：

* 采用方案二和方案四。

状态：已确认

---

## 2026-06-20 新建商品审批流程

决策时间：2026-06-20

决策内容：

* 手工新建商品不再直接写入正式商品库，先生成待审批的商品申请。
* 审批角色为经销商级财务、采购、admin，以及系统 boss。
* 申请人不得审批自己的商品申请。
* 审批通过后，系统在同一事务中创建正式商品并回写商品ID；审批拒绝时不创建商品。
* 未审批及已拒绝的商品不能用于采购申请；审批通过后才会出现在正式商品搜索结果中。
* 小程序负责提交商品申请和查看状态，ANY-ERP 网页端提供审批操作。

决策原因：

* 商品属于采购、库存、销售和财务共用的核心主数据，错误商品会持续影响后续业务。
* 先审批再进入正式商品库，可以避免未确认的名称、分类和厂商编码被采购引用。
* 使用独立申请记录可完整保留申请人、审批人、审批意见和最终生成商品的追溯关系。

备选方案：

* 方案一：小程序直接创建正式商品。
* 方案二：先创建停用商品，审批后启用。
* 方案三：独立保存商品申请，审批通过后创建正式商品。

最终选择：

* 采用方案三，并禁止申请人自审。

状态：已确认

---

## 2026-06-20 采购申请列表按待审批优先排序

决策时间：2026-06-20

决策内容：

* 采购申请列表中，待审批申请统一排在最前。
* 待审批组内按申请时间倒序排列，最新的申请在最前。
* 已通过、已拒绝以及其他非待审批状态不再按状态分组，统一按申请时间倒序排列。
* 当前采购申请没有独立可靠的提交时间字段，排序时使用创建时间；同一时间的记录按申请 ID 倒序保持稳定顺序。

决策原因：

* 列表首先服务于待办审批，待审批申请需要优先暴露。
* 非待审批记录已完成当前审批处理，按时间统一查看比按审批结果分组更符合追溯需求。

备选方案：

* 方案一：已通过、已拒绝和其他状态分组排序。
* 方案二：仅待审批置顶，其他状态按时间统一倒序排列。

最终选择：

* 采用方案二。

状态：已确认

---

## 2026-06-20 员工业绩毛利调整两级审批

决策时间：2026-06-20

决策内容：

* 所有启用账号都可以针对已归档销售订单提交毛利调整申请。
* 调整支持增加或减少毛利，必须填写调整金额和原因，并支持上传证明附件。
* 审批固定为两级：财务账号初审，admin 经销商总权限账号复审。
* 申请人不得审核自己的申请；财务或 admin 提交申请时，需由同角色其他账号完成对应审核。
* 同一订单允许多次提交调整申请，仅审批通过的调整计入业绩。
* 业绩毛利 = 订单经营毛利快照 + 已审批增加金额 - 已审批减少金额。
* 审批通过的调整按原订单日期和原订单员工归属，因此历史期间统计可随后续审批结果变化。
* 毛利调整单独立保存，不修改订单原始毛利、销售结算成本或厂家返利记录。
* 员工业绩基础毛利统一使用订单归档时保存的订单经营毛利快照，不再根据 SN 入库价、当前商品成本或当前收款税率动态重算。
* 对于未保存归档毛利的存量订单，报表临时按原有成本口径兼容展示并标记；授权账号单笔重算后转为归档快照口径，不自动批量改写历史数据。
* 员工业绩汇总按当前全部筛选结果计算，展示基础毛利、已审批调整和最终业绩毛利。
* 当前先使用固定两级审批，后续建立通用审批管理流程后再迁移配置。

决策原因：

* 业绩毛利存在订单级特殊奖励、扣减和事后确认场景，需要在不污染原订单财务事实的前提下调整业绩。
* 独立调整单可以保留申请、附件、两级审批和最终生效记录，降低毛利被直接改写的审计风险。
* 基础毛利必须使用归档快照，否则当前成本变化会导致历史员工业绩漂移。

备选方案：

* 方案一：直接修改订单原始毛利。
* 方案二：建立独立毛利调整单，审批后叠加到业绩口径。
* 方案三：只由 admin 单级审批。
* 方案四：财务初审后由 admin 复审。
* 方案五：调整计入审批日期所属期间。
* 方案六：调整回归原订单日期所属期间。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-19 待付款清单支持单笔立即付款登记

决策时间：2026-06-19

决策内容：

* 财务管理 → 付款管理 → 待付款清单增加“立即付款”入口。
* 立即付款仅登记系统内的付款事实，不连接银行或第三方支付接口。
* 付款登记支持支付全部剩余应付金额，也支持部分付款。
* 付款时必须选择一个启用的结算账户，并从该账户余额中扣减本次付款金额。
* 结算账户余额不足时不阻止付款；系统必须明确提示付款后余额为负数，由用户确认后继续登记。
* 单笔立即付款复用现有付款批次、付款记录、结算账户流水和撤销机制，以单笔付款批次保存。
* 付款登记必须在同一数据库事务内完成状态校验、付款记录、账户流水、账户余额和结算单付款状态更新。

决策原因：

* 当前仅有批量导出和导入付款结果，无法高效处理日常单笔付款登记。
* 单笔付款和批量付款属于同一资金登记业务，应共用可追溯、可撤销的账务链路。
* 当前结算账户余额是内部金额记录，不代表银行实时可用余额，因此允许出现负数，但必须向操作人明确提示。
* 支持部分付款符合供应商分期支付、临时支付和余额分配等实际业务场景。

备选方案：

* 方案一：只支持全额立即付款。
* 方案二：同时支持全额和部分立即付款。
* 方案三：账户余额不足时禁止付款。
* 方案四：账户余额不足时提示并允许继续登记为负数。
* 方案五：单笔付款建设独立记录和撤销逻辑。
* 方案六：单笔付款复用现有付款批次、付款记录、账户流水和撤销逻辑。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-13 商品价格批量导入与价格历史追溯

决策时间：2026-06-13

决策内容：

* 商品管理 → 价格管理 → 批量导入定价模板不再使用商品名称，将原商品名称列调整为厂商编码。
* 本页面批量导入只更新定价和最低售价。
* 导入时商品编码和厂商编码至少填写一个。
* 只填写商品编码时，按商品编码更新对应商品价格。
* 只填写厂商编码时，按厂商编码匹配所有关联商品，并全部更新。
* 商品编码和厂商编码都填写时，商品编码优先，同时校验厂商编码必须属于该商品。
* 最低售价必须小于或等于定价，否则对应行标记为异常，不参与导入。
* 价格导入不在上传文件后立即校验；用户点击“立即导入”时再执行校验和导入。
* 导入时存在异常记录时，跳过异常记录，正常记录继续入库；异常记录不入库并支持下载 Excel 后修改重提。
* 导入支持预约生效时间，生效时间为空时立即生效。
* 调价原因非必填。
* 暂不支持不同门店不同价格。
* 价格调整纳入现有商品价格管理权限，不新增独立审批权限。
* 商品价格调整必须记录价格变更历史，用于后续结算回溯、审计和价格变化分析。
* 已发生的历史销售订单不因商品调价自动重算，如需处理历史订单，应后续通过授权重算流程单独处理。

决策原因：

* 商品名称容易重复、变更和出现录入差异，不适合作为价格导入匹配字段。
* 厂商编码在实际业务中可对应多个商品编码，批量调价需要支持一对多展开。
* 商品价格会影响销售开单、毛利、结算回溯和后续调价分析，不能只覆盖当前价格而不留历史。
* 当前阶段优先建立“当前价格 + 价格历史 + 批次追溯”的稳定基础，不引入复杂价格策略、门店差异价和审批流。
* 价格导入不涉及资金账户扣款，和财务付款导入不同，允许正常价格记录先导入可以减少运营维护成本。

备选方案：

* 方案一：继续使用商品名称辅助匹配价格导入。
* 方案二：删除商品名称匹配，仅支持商品编码和厂商编码。
* 方案三：价格导入只覆盖当前价格，不保存历史。
* 方案四：价格导入同时保存批次和商品级价格变更记录。
* 方案五：价格导入任一行异常时整批拒绝。
* 方案六：价格导入允许部分成功，正常行导入，异常行提示并支持下载。
* 方案七：本次同步建设门店差异价和调价审批。
* 方案八：本次暂不建设门店差异价和调价审批，沿用现有权限。

最终选择：

* 采用方案二、方案四、方案六和方案八。

状态：已确认

---

## 2026-06-07 商品拆分部分成本剥离规则

决策时间：2026-06-07

决策内容：

* 拆分单支持从被拆商品当前成本中拆出部分金额，不再要求拆出商品成本合计必须等于被拆商品成本。
* 被拆商品拆分后继续销售，不额外标记为已拆分。
* 被拆商品如有 SN，原 SN 保留原有 SN 和在库状态。
* 拆分后原商品成本 = 拆分前原商品成本 - 拆出商品成本合计。
* 拆出商品成本默认带出目标商品成本价，允许用户手工修改，界面提示为“请输入商品价格”。
* 被拆商品栏展示 PN，并自动带出当前商品成本。
* 拆出商品成本合计不得超过被拆商品当前成本。
* 拆分单和组装单的目标商品区域支持新建商品，创建成功后自动选中新商品。
* 本规则只影响新拆分单，不处理历史拆分单。

决策原因：

* 实际业务中存在从整机或原商品中拆出配件单独出售，但原商品仍继续销售的场景。
* 该场景本质是部分成本剥离，不是完整库存形态转换。
* 保留原 SN 和在库状态，可以避免销售端和库房端因额外状态标记产生操作负担。
* 通过成本上限校验保证成本不会凭空增加，同时保留拆分来源追溯。
* 在拆装现场新建目标商品，可以减少退出当前单据维护商品资料的操作成本。

备选方案：

* 方案一：继续按完整拆分处理，拆出商品成本合计必须等于被拆商品成本。
* 方案二：按部分成本剥离处理，原商品继续销售并扣减已拆出金额。
* 方案三：拆分后强制标记原商品为已拆分或降配商品。
* 方案四：拆分后不额外标记原商品，仅保留单据追溯。
* 方案五：拆装时必须先到商品管理维护目标商品。
* 方案六：拆装单内允许按商品管理流程新建目标商品。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-05 应付结算单管理基础流程

决策时间：2026-06-05

决策内容：

* 应付管理保留“待付款清单”。
* 由待付款记录生成“应付结算单”，生成后默认状态为“草稿”。
* 结算单状态先支持：草稿、待确认、已确认、已作废。
* 已确认结算单允许作废。
* 结算单作废后，不回到待付款清单。
* 暂不实现付款功能，仅预留付款状态字段：未付款、部分付款、已付款。
* 应付结算单需要支持详情查看。

决策原因：

* 先把应付结算流程和付款流程分离，降低财务逻辑复杂度。
* 当前阶段优先建立可追踪、可确认、可作废的结算单据管理能力。
* 付款功能涉及资金账户、审批、核销和财务风险，暂不纳入本次最小可行范围。

备选方案：

* 方案一：生成结算单后直接进入待付款或已确认状态。
* 方案二：生成结算单后默认草稿，再提交确认。
* 方案三：作废后退回待付款清单。
* 方案四：作废后不退回待付款清单。

最终选择：

* 采用方案二和方案四。

状态：已确认

---

## 2026-06-06 商品拆分与组装销售基础规则

决策时间：2026-06-06

决策内容：

* 电脑行业拆分与组装不直接修改原采购单和原入库单。
* 拆分与组装统一作为库存转换业务处理，后续通过拆分单和组装单记录。
* 当前系统已有 PN 和 SN 管理，拆分与组装必须保持 PN、SN、单台成本和采购来源可追溯。
* 库存成本按单台成本作为经营和销售毛利主口径，财务报表中的移动加权成本仅作为统计口径。
* 所有商品默认允许拆分和组装，暂不按商品类别限制。
* 拆分后或组装后的结果是否新建商品，取决于是否需要长期销售、独立库存、独立定价、独立售后和独立毛利核算。
* 老商品采购仍对应原始采购商品，不因后续拆分或组装改写采购明细。
* 拆分时由原商品库存减少，拆出商品库存增加，拆出商品成本由人工录入，成本合计必须与被拆商品单台成本一致。
* 组装时由组件商品库存减少，组装成品库存增加，成品成本由人工录入并校验，原则上应等于组件成本合计加组装服务成本。
* 组装服务费计入组装成品库存成本。
* 拆分单和组装单允许作废或反向冲销，但不得删除原记录，必须保留追溯链路。

决策原因：

* 采购记录代表真实外部采购事实，后续库存形态变化不应反向污染采购数据。
* 拆分和组装本质是库存形态转换，不是采购重做或销售备注。
* 电脑行业依赖 PN、SN 和单台成本追溯，拆分/组装后仍需解释毛利、库存来源和售后责任。
* 人工录入成本更符合当前拆分/组装场景差异较大的实际情况，也避免过早建设复杂 BOM 和自动分摊规则。
* 允许反向冲销可以处理门店误操作，但通过保留原记录降低库存和成本审计风险。

备选方案：

* 方案一：拆分/组装直接修改原采购商品或原入库明细。
* 方案二：拆分/组装通过库存转换单据处理，采购记录保持不变。
* 方案三：按商品类别限制哪些商品允许拆分或组装。
* 方案四：所有商品默认允许拆分和组装。
* 方案五：拆分/组装成本由系统自动按比例分摊。
* 方案六：拆分/组装成本由人工录入，系统校验成本守恒。
* 方案七：组装服务费只作为销售附加服务费，不进入库存成本。
* 方案八：组装服务费计入组装成品库存成本。

最终选择：

* 采用方案二、方案四、方案六和方案八。

状态：已确认

---

## 2026-06-06 定金管理与正式订单核销流程

决策时间：2026-06-06

决策内容：

* 新增定金单管理，定金单提交后状态为已提交，仍需归档后才允许被正式销售订单核销。
* 定金不计算销售毛利，不生成销售结算成本调整。
* 正式订单选择“定金”收款方式时，必须绑定具体定金单。
* 只允许核销当前店员自己收取的定金单，不做定金转让。
* 定金核销只支持全额一次性抵扣，不支持手工填写抵扣金额，不支持部分核销。
* 定金单客户电话必须与正式订单客户电话一致。
* 定金退款本次仅作为业务记录，不处理收付款账户、不生成账户流水、不做审批。
* 正式订单未归档前如被取消或审批拒绝，已绑定定金恢复为可核销状态，原核销记录标记作废。
* 该能力同步预留给小程序，通过销售模块定金接口提交定金单和查询可核销定金。

决策原因：

* 定金本质是预收性质的业务单据，不能直接等同正式销售收入或毛利。
* 通过定金单与正式订单核销记录绑定，可以防止重复核销和手工改金额。
* “自己只能用自己收的定金”可以降低门店和店员之间的责任混淆。
* 当前系统不涉及线上收付款，退款先记录业务事实，避免过早引入资金账户、审批和对账复杂度。

备选方案：

* 方案一：把定金作为普通收款方式，仅在订单支付记录里填写金额。
* 方案二：新增定金单、退款记录和核销记录，正式订单必须绑定定金单。
* 方案三：支持部分核销和跨店员转让。
* 方案四：只支持自己收取的定金全额一次性核销。
* 方案五：退款进入资金账户和审批流程。
* 方案六：退款仅记录业务事实，暂不处理资金。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-06 应付提交与付款管理流程调整

决策时间：2026-06-06

决策内容：

* 删除应付结算单“待确认”状态。
* 草稿结算单提交后直接进入待付款状态。
* 付款审批人和审批流后续通过独立审批流程管理建设，本次不在应付付款中固化。
* 付款管理调整为财务管理页面内的顶部标签页，与日结单、费用管理、应付管理、返利管理同级，不进入左侧菜单树。
* 付款管理继续保留批量模式，通过导出付款清单、线下付款、导入付款结果回写系统。
* 支持部分付款。
* 导入付款成功后生成付款批次、付款记录和账户流水，并从所选账户扣减余额。

决策原因：

* “提交后再确认”的含义不清，容易与付款审批、付款完成混淆。
* 应付确认债务和实际支付资金是两个不同职责，当前最小流程中先让提交后的结算单直接进入待付款。
* 出纳的主要工作应集中在财务管理页面的付款管理标签，而不是嵌在应付管理页签内部。
* 付款审批属于通用审批能力，适合后续独立配置，不宜在本次应付付款流程中写死。
* 批量导出、线下付款、导入回写更符合当前不对接支付接口的实施阶段。

备选方案：

* 方案一：保留草稿、待确认、已确认、已作废状态。
* 方案二：删除待确认，草稿提交后直接进入待付款。
* 方案三：付款管理作为财务管理页面内的顶部标签页。
* 方案四：付款管理作为独立一级菜单。
* 方案五：本次直接实现固定付款审批人。
* 方案六：审批流程后续由独立审批流程管理统一配置。

最终选择：

* 采用方案二、方案三和方案六。

状态：已确认

---

## 2026-06-05 厂家返利与销售结算成本解耦

决策时间：2026-06-05

决策内容：

* 厂家政策产生两类结果：后台返利预估、销售结算成本调整。
* 厂家返利不直接等同于销售奖励或销售提成。
* 销售端只展示商品售价、政策调整金额、销售结算成本和销售毛利，不展示厂家返利细则。
* 销售结算成本 = 原始库存成本 - 成本调整金额。
* 厂家政策新增是否影响销售结算成本、成本调整方式、调整值、调整上限和调整说明。
* P差按“库存原始提货价 - 销售时厂家当前提货价”计算，仅当原始提货价大于销售时当前提货价时生成。
* 厂家主体沿用现有供应商资料，不新增独立厂家主数据。
* 新增厂家价格历史，用于按销售日期、PN和供应商匹配销售当日有效提货价。
* 销售归档时自动生成返利预估和销售结算成本调整记录。
* 历史已归档销售单不自动重复计算，如需处理由授权角色手工重算。
* 自定义成本调整规则本次仅预留字段，不实现规则编辑和执行。

决策原因：

* 销售只需要理解当前开单毛利口径，复杂厂家政策应由后台财务和运营核算。
* 返利回款、厂家对账和销售提成属于不同账务目标，必须解耦。
* P差来自厂家提货价变化，不应按销售价格或进货价差额误算。
* 沿用供应商主数据可以减少重复资料维护，符合当前系统结构。

备选方案：

* 方案一：把厂家返利直接作为销售奖励。
* 方案二：返利后台核算，另行计算销售结算成本。
* 方案三：新建独立厂家主数据。
* 方案四：沿用供应商作为厂家来源。
* 方案五：历史销售单自动批量重算。
* 方案六：仅对指定订单手工重算。

最终选择：

* 采用方案二、方案四和方案六。

状态：已确认

---

## 2026-06-05 应付实际付款导入流程

决策时间：2026-06-05

决策内容：

* 应付结算单确认环节纳入现有财务角色权限控制。
* 暂不新增独立权限管理页面，后续再扩展按钮级权限管理。
* 实际付款页面放在应付管理内部。
* 已确认且未付清的结算单可导出付款清单。
* 付款结果通过 Excel 导入系统。
* 导入时页面统一选择一个付款账户，整批从该账户扣款。
* 导入前必须进行整批预校验，任一行异常则整批拒绝，不处理任何付款。
* 支持部分付款。
* 导入成功后生成付款批次、付款记录和结算账户流水。
* 付款批次支持整批撤销，撤销时生成反向账户流水并回退结算单付款状态。
* 导入模板尽量沿用导出模板，减少人工编辑工作量。

决策原因：

* 财务付款需要可追溯、可撤销、可防重复扣款。
* 当前阶段不接银行接口，通过导出、线下付款、导入回写的方式降低实施复杂度。
* 统一账户批量导入便于财务按账户对账和追责。
* 整批拒绝比部分成功更适合财务批量入账场景，避免批次状态复杂化。

备选方案：

* 方案一：导入时按行部分成功，失败行跳过。
* 方案二：导入前整批校验，任一异常则整批拒绝。
* 方案三：导入时每行选择付款账户。
* 方案四：页面统一选择一个付款账户，整批导入。

最终选择：

* 采用方案二和方案四。

状态：已确认
