# BUSINESS_RULES.md

## 店长商场上报查询账号规则（2026-09-02）

* 目标账号由启用中的 `manager`、`store_manager`、`store_admin` 店长账号生成，登录标识为原手机号追加大写 `D`。
* 目标账号归属原经销商，并复制原店长的有效门店权限；没有有效门店权限的店长不生成目标账号。
* 目标账号仅允许调用销售订单列表和详情查询接口；服务端强制追加 `MALL_REPORT_STATUS=reported` 条件，不能通过参数取消。
* 目标账号可以查看授权门店内所有已上报订单，不按订单创建人限制；未上报订单详情返回不可见。
* 目标账号不得新建、编辑、归档、审批、退单、上报订单，也不得访问库存、采购、财务、报表、导出和其他业务模块。
* 商场上报状态、上报时间和上报人员由 ANY-ERP API 统一保存；小程序的入口隐藏只负责交互，不作为安全边界。

## 费用经营归属与员工绩效分摊规则（2026-09-01）

* 费用单同时保存费用发生日期和经营归属月份；费用报表及门店经营利润按经营归属月份统计，付款日期不决定费用所属月份。
* 房租、电话费、运费、手续费、历史亏损等费用可以在本月录入并指定本月体现，用于管理会计口径的真实门店经营结果。
* 费用单的“是否计入门店经营利润”由财务侧明确选择；计入时，门店经营利润 = 销售经营毛利 - 归属月份已确认经营费用。该口径不等同于法定财务报表净利润。
* 已提交或审批通过的费用才进入经营费用统计；草稿、待审批、已拒绝和已撤销费用不进入经营费用。
* 费用可以部分分摊到一个或多个员工的指定绩效月份；分摊合计不得超过费用金额，待审核和已审批分摊均占用可分摊额度。
* 费用绩效分摊先由财务初审，再由 admin/BOSS 复审；只有复审通过后才扣减员工绩效毛利。
* 员工最终业绩毛利 = 原订单业绩毛利 + 已审批订单毛利调整 - 已审批费用绩效扣减；费用绩效扣减不修改销售订单原始毛利、产品端毛利、库存成本或销售结算成本。
* 费用经营月份和员工绩效月份均支持锁定；锁定后不得直接新增、编辑或分摊，只能由授权账号重新打开或通过后续冲销/调整流程处理。
* 费用经营金额、员工绩效扣减和审批状态由 ANY-ERP API 统一计算，前端只负责录入、展示和提交操作。

## 产品端结算与厂家政策收益规则（2026-08-31）

* 销售订单最终归档后，由 ANY-ERP 在同一事务内幂等生成一张产品端结算单；产品端结算单不构成第二次销售，不重复扣减库存。
* 产品端毛利只计算提货差：产品端结算价减实际采购成本。
* SN 商品的产品端结算价沿用销售订单已冻结的产品定价/SN 特价快照，成本使用该 SN 的实际采购或提货成本。
* 非 SN 商品的成本使用销售归档时的库存加权平均成本，并将单价和成本方法冻结在产品端结算明细中。
* 采购及调拨运费已经摊分到销售商品明细，不再单独计入产品端毛利。
* 产品端结算单缺少有效价格或成本时进入待补成本状态，不得用零成本生成虚假毛利；待补单不计入已实现产品端毛利。
* 销售退货入库完成后生成关联的产品端负向调整单；原产品端结算单不得改写，整单和部分退货均通过正负业务事实抵销。
* 联想返利、价保、活动补贴及其他厂家政策收益不计入产品端基础毛利，也不回写已冻结的产品端结算单。
* 厂家政策收益沿用现有厂家政策、返利预估和返利上账链路，至少区分预估、确认、到账和冲销状态；第一期仅将实际到账收益纳入公司已实现政策收益。
* 财务总览同时展示门店经营毛利、产品端毛利和公司商品综合收益；公司商品综合收益不等同于扣除期间费用后的净利润。
* 产品端成本、产品端毛利和厂家政策收益属于财务敏感指标，仅经销商级账号、财务和 BOSS 可查看，所有金额由 ANY-ERP API 统一计算。

## 采购申请人员筛选规则（2026-08-31）

* 提交人筛选同时查询 `submit_user`、`apply_user` 和 `create_user`，并支持通过员工姓名关联 `applicant_staff_id`。
* 经手人优先按 `operator_staff_id` 或 `operator_name` 查询；历史数据缺少经手人字段时，兼容申请人和制单人员工ID。
* 新采购申请保存当前操作员工的经手人和制单人快照，草稿提交不会覆盖已有经手人快照。

## 采购申请生命周期状态筛选规则（2026-08-31）

* “待入库”指采购申请为已审批状态，且关联入库单中至少存在一笔待入库记录。
* “已撤销”直接对应采购申请撤销状态。
* “已退单”指关联入库单全部处于已退库或已取消状态，且至少存在一笔已退库记录；部分退单仍按原入库状态展示。
* 采购申请列表和导出必须使用同一套服务端生命周期状态判定，不能只依赖采购申请主表状态。

## 新建订单门店切换与订单可见规则（2026-08-30，按多店长规则修订）

* 新建订单门店候选由后端按当前账号已配置的经销商权限返回，配置多个经销商时返回这些经销商下全部有效门店；只有一个经销商时仅返回该经销商内部有效门店。
* 订单门店范围是销售订单专用范围，不自动扩大库存、财务、采购或其他模块的门店权限。
* 店长角色包括 `manager`、`store_manager` 和 `store_admin`；店长可以查看其已勾选授权门店的全部销售订单，不再要求匹配门店单一 `manager_staff_id`。
* 同一门店可以授权多个店长；每个店长在订单查询、详情、导出、草稿处理、订单编辑和销售订单审批上按相同规则享有权限。
* 店长的订单数据范围只能由账号已配置的有效门店决定；门店筛选参数不得扩大该范围。经销商级账号和 BOSS 继续沿用原有经销商/系统范围。
* `manager_staff_id` 可以继续作为组织资料或历史兼容字段保存，但不再作为销售订单可见和审批权限的唯一依据。

## 销售商品采购/调拨运费匹配规则（2026-08-30）

* 调拨运费归属于调拨目的门店，只能用于目的门店销售订单的商品成本；采购运费归属于采购申请收货门店，只能用于该门店销售订单的商品成本。
* 销售订单匹配运费时，SN 精确匹配优先，商品匹配作为无 SN 时的兜底；匹配必须同时满足来源门店与订单门店一致。
* 运费记录必须在订单创建时间之前已经生效；运费记录被后续编辑时，以最后更新时间作为生效时间。
* 找不到符合门店和时间条件的运费时，不自动扣减商品采购运费；毛利重算会清理订单中已失效的旧商品运费。
* 采购或调拨单的整单运费仍按商品数量分摊并保留两位小数，1.50 元、2.00 元等单位金额属于合法分摊结果。

## 商品名称自动组成规则（2026-08-29）

* 自动组成商品名称时，先使用分类第2级、第3级、第4级名称，按分类层级顺序排列。
* 第1级分类名称不加入商品名称。
* 分类名称之后，按当前有效字段配置的顺序追加字段值；字段配置来源遵循一级默认、下级继承、下级覆盖规则。
* 商品名称支持手工覆盖自动组成结果；未手工覆盖时由 ANY-ERP 后端统一生成和校验。
* Web、微信小程序、商品申请审批、批量导入和库存转商品使用同一命名顺序。

## 新建商品分类选择与字段填写规则（2026-08-31）

* Web 和微信小程序的新建商品分类使用一级、二级、三级、四级逐级筛选；可以停留在任意已选层级，也可以完全不选择分类。
* 没有合适分类时，允许手工填写商品分类、品牌、系列和型号；选择分类后自动带入的字段仍允许修改和补充。
* 选择分类后，页面只展示 ANY-ERP 根据当前分类及最近上级配置解析出的有效字段；字段继承和必填校验由服务端统一执行。
* 商品名称默认按“二级-三级-四级-其他已配置字段值”顺序使用短横线拼接，商品名称允许手工覆盖；提交时由 ANY-ERP 作为最终名称口径。
* 一级分类只用于商品分类维度，不作为默认商品名称组成部分；分类路径保存完整路径用于关联和历史追溯。

## 负毛利销售订单两级审批规则（2026-08-29）

* 归档前最终毛利为负的销售订单进入 `pending_store_approval`，第一审批节点由店长或经销商总权限审批；店长角色包括 `manager`、`store_manager`、`store_admin`，经销商总权限角色包括 `admin`、`boss`、`distributor`。
* 第一节点通过后进入 `pending_distributor_approval`，第二审批节点仍由经销商总权限审批；第一节点不再限制必须使用店长账号。
* 第二审批通过后才执行库存扣减、定金核销、资源权益处理、结算成本处理和订单归档；第一审批不执行这些归档副作用。
* 审批拒绝时订单退回“未归档”，后续可修改后重新归档并重新开始店长初审；历史 `pending_approval` 按店长初审兼容处理。
* 一个账号同时拥有店长角色和经销商总权限时，可以在第一审批通过后继续完成第二审批；经销商总权限账号也可以独立连续完成两级审批。

## 库存SN库龄与展示规则

* SN商品库龄从公司首次采购入库时间开始计算，字段为 `original_inbound_time`。
* 调拨到门店不会重置库龄；`inbound_time` 的历史数据会作为兼容回退值，新增采购入库会同时写入首次入库时间。
* SN查询优先展示在库状态；在库 SN 按入库时间从远到近排列，其他状态按入库时间从近到远排列；状态时间是最近一次状态更新时间，状态持续时间按该时间计算。
* 小程序库存查询只显示非零库存分类：销售库（可销售、占用中）、样机库、铺货仓、租赁样机、不可售。

## 商品字段继承规则（2026-08-29）

* 商品字段默认在一级商品分类配置。
* 二级、三级、四级分类未配置自己的字段时，继承所属一级分类字段。
* 下级分类保存字段后，该分类及其后代使用本级配置；字段解析按当前分类到一级分类的顺序，使用最近的有效配置。
* 商品新建、商品申请和审批可以选择任意分类层级，也可以不选分类后手工填写；字段继承不改变分类树最多四级的结构约束。
* 现有低级分类字段继续作为覆盖配置保留；删除本级配置后恢复继承上级默认字段。
* Web 和微信小程序只展示和提交 API 返回的最终有效字段，字段继承和必填校验由 ANY-ERP 后端统一处理。

## 国补 OMO 收款规则（2026-08-29）

* 新增“国补OMO（电脑）”和“国补OMO（手机平板）”收款方式；两种方式与对应国补POS品类一样拆分为“客户实收”和“政策补贴应收”。
* 电脑国补优惠封顶1500元，手机/平板国补优惠封顶500元。
* OMO立即到账的客户实收金额按 `产品原价 - min(产品原价 × 0.15, 对应封顶金额) - 优惠/手续费金额` 计算；优惠/手续费直接扣除，电脑国补优惠封顶1500元、手机/平板国补优惠封顶500元。
* OMO政策补贴应收金额按 `min(产品原价 × 0.15, 对应封顶金额)` 计算；例如手机/平板原价5999元时，政策补贴应收为500元，优惠/手续费不计入政策补贴应收。
* 历史国补POS继续沿用既有收款口径；一个订单存在多个OMO收款方式时，订单优惠/手续费按各OMO收款方式录入原价比例分摊，合计只扣除一次。
* ANY-ERP API 负责收款记录、日结、国补应收和导出口径；微信小程序只负责收款方式展示、输入和 API 适配。历史国补POS数据保持兼容。

## 返利账户手工新增与批量勾稽规则

* 结算账号管理支持手工新增和编辑“返利账户”（`SUPPLIER_REBATE`），返利账户必须绑定一个启用供应商，同一供应商只能有一个启用返利账户。
* 新增返利账户时可录入新增返利金额；金额同时形成返利账户收入流水和供应商返利池 credit 流水，并生成一笔“返利收款”待下账单及对应返利上账单来源。
* 返利账户仍属于受限额度账户，不得作为客户收款方式、普通付款账户或账户中心手工记账账户使用；返利业务核销不重复增加返利余额。
* 返利下账页面统一展示手工新增、返利收款和自动生成的返利待下账记录；返利类待下账单支持按同一供应商批量选择。
* 批量核销按选中下账单顺序分配返利上账金额，服务端在单一事务中校验供应商、状态、剩余金额和上账单可核销余额，禁止超额、跨供应商和重复核销。
* 返利账户新增金额生成的待下账单、返利上账单和账户/返利流水必须保留来源单号、金额、供应商、操作人和操作时间。

## 销售退单状态与归档规则

* 退单申请提交后，原销售订单状态为 `return_pending`，展示为“退单审批中”，不得再次归档或重复发起退单。
* 退单最终审批通过并生成销售退货入库单后，原销售订单状态改为 `return_inbound`，展示为“退货入库中”。
* 销售退货入库完成后，整单退货的原订单状态改为 `returned`（“已退单”）；部分退货的原订单恢复为“已归档”，剩余商品继续保留销售事实。
* 退单审批拒绝时，原订单恢复为“已归档”。
* 归档接口必须在服务端拦截退单审批中和退货入库中的订单，不能仅依赖小程序隐藏归档按钮。
* 所有退单入口必须填写非空退单缘由；退单缘由保存于退单申请，并在审批详情中只读展示，审批意见单独保存。

## 国补资格核销冲销规则（2026-08-30）

* 已核销的国补资格不得通过 SN 权益人工维护或批量调整直接改回可用，必须走专用核销冲销接口。
* 核销冲销仅允许财务、`admin` 或 BOSS 操作；必须填写冲销原因，并由服务端校验 SN 已回库、原销售单已完成整单退单、原 `SALE_USED` 核销流水存在且尚未冲销。
* 冲销只追加不可删除的 `USED → AVAILABLE` 权益变更记录，原销售订单、原收款、原核销流水和退单结算记录不得改写。
* 原销售权益存在待下账记录时，冲销事务中将其标记为取消并记录原因；已部分或全部下账的记录不得直接冲销资格，必须先完成对应下账冲销。
* 同一原销售核销只能成功冲销一次；冲销后的资格可被新的未归档销售订单重新使用，归档时仍按正常销售核销流程处理。

## 双经销商组织与权限规则

* 有效经销商为 `DIST001` 艾诺云（成都/R001）和 `DIST002` 艾诺志兴（重庆/R002）；区域仍表示地理位置，不替代经销商。
* 供应商主数据全局共用；门店、员工、采购申请、费用、应付款、结算单、付款批次和付款记录必须记录所属经销商。
* 中台采购、会计、出纳、BOSS等账号可配置一个或多个经销商；管理员通过用户管理逐个账号配置。店员、店长只能配置一个经销商，并且只能操作被分配的门店。
* 店员、店长可查看所有区域库存的只读信息，用于内部采购；库存写入、采购提交、财务数据和账户操作不因库存可见而放开。
* 同一采购申请的收货门店和门店分配不得跨经销商；跨经销商需求必须拆成独立采购申请。经销商之间不得直接库存调拨，需通过独立采购流程。
* 同一供应商在不同经销商下形成独立应付款；结算单不得混合不同经销商的应付款。
* 资金账户必须指定所属经销商才能用于资金付款；付款接口必须校验账户经销商与结算单经销商一致。共享/系统级账户不作为经销商应付付款账户使用。
* 历史单据无法唯一推导经销商时保留为空并由管理员核对；系统不得根据名称或区域自动猜测责任主体，除已明确的艾诺云/艾诺志兴账户名称初始化映射外，其他账户需人工确认。
* 应付结算单必须保存经销商和税务属性快照；支付管理、结算单管理和付款批次详情只读展示“艾诺云/艾诺志兴”及“含税/未税/未知”。
* 同一张应付结算单不得同时包含含税和未税应付款；同一付款申请/付款批次也不得同时包含含税和未税结算单。
* 同一付款申请/付款批次不得同时包含艾诺云和艾诺志兴的结算单；付款账户与结算单经销商仍必须一致。

---

## 采购垫付报销审批规则

* 采购垫付报销审批通过前，关联采购申请必须处于 `approved` 状态。
* 关联采购申请被拒绝或尚未通过时，报销审批人仍可以选择拒绝报销；拒绝后不生成待付款记录。

# 项目架构边界规则

## 前端与 ANY-ERP API 的职责划分

`sdxcx` 仅包含微信小程序端；网页端和后端统一位于 `D:\艾诺云\小艾虾\ANY-ERP`。所有业务数据和业务规则以 ANY-ERP API 为唯一来源。

必须遵守：

* 财务、库存、采购、销售、报表、结算、权限及金额汇总由 ANY-ERP API 执行。
* 小程序端只负责展示、交互、参数传递、状态提示和纯展示格式化；网页端相关改动在 ANY-ERP 内完成。
* 类似功能必须优先调用 ANY-ERP 已有 API，不得在前端重新计算业务结果。
* 小程序新需求缺少 API 时，先在 ANY-ERP 新增或调整 API，再由小程序接入；网页端需求直接在 ANY-ERP 内实现。
* 本项目不得新增或扩展 `backend`、`frontend` 等后端/服务端目录，不得新增本地业务数据层、服务端模型、控制器或数据库迁移。
* 不得用模拟数据、硬编码经营结果或前端临时逻辑掩盖 API 缺失；无法确认时标记【待确认】。

---

## 财务经营总览接口规则

* 财务小程序统一调用 ANY-ERP `GET /report/finance-overview`，不得同时拉取明细并在前端汇总。
* 请求必须传递 `periodType`、`date`、`regionId` 和账号允许的 `storeId`；日/月/年日期范围由 ANY-ERP 解释。
* 接口统一返回营业额、毛利额、毛利率、库存总额及分类、账户类型金额和付款状态金额。
* 付款状态固定为“全部、未生成结算单、已生成结算单、已支付”；金额判定由 ANY-ERP 根据应付款、结算明细和付款状态计算。
* 库存结构由 ANY-ERP 商品分类主数据驱动；只有在商品分类管理中开启“展示在财务页面”的分类才进入财务库存分类结果。
* 商品按最具体的已开启分类归属；若子分类已开启，商品优先归入子分类；否则沿分类树向上匹配已开启的父分类，避免同一库存重复汇总。
* 分类开关默认关闭，历史分类和新建分类均需由授权人员明确开启；小程序只展示 API 返回的分类和金额，不得按商品名称自行模糊分类。
* 已进入财务库存结果的一级分类支持继续展开二级、三级分类；子分类数量和成本金额由 ANY-ERP 递归汇总，无库存的子分类不返回，小程序不得自行拆分。
* 历史商品路径与当前分类树不一致时，ANY-ERP 可以回退到已开启的上级分类，并使用商品明确的系列、型号字段匹配现有子分类；不得使用商品名称模糊归类。
* 财务库存支持“含样机 / 不含样机”切换；含样机同时统计 `demo_qty` 和 `rental_demo_qty`，不含样机同时排除两者，非 SN 与 SN 商品必须使用同一口径。
* 商品型号与三级分类名称不完全相同时，服务端可以按分类名称对明确型号做最长前缀匹配；该匹配必须基于商品型号和分类主数据，不得按商品名称任意模糊归类。
* 选择成都或重庆时只统计对应区域账户和业务单据；公司级账户只进入“全部”汇总。

---

## 财务管理区域筛选与金额展示

* 财务管理默认展示全部区域；区域选择器必须提供“全部、成都、重庆”，并位于时间选择器之前。
* 选择成都或重庆后，营业额、毛利额、库存、账户余额和采购/报销付款统计必须使用同一 `regionId` 口径。
* 区域筛选不能扩大账号原有门店权限；服务端必须先执行账号授权门店过滤，再执行区域过滤。
* 接口异常、响应结构不兼容或数据尚未加载完成时，不得把异常状态伪装为真实金额 0，应显示加载/错误提示。
* 未绑定区域的历史账户按公司级账户保留；包含多个区域应付款的结算单不归属单一区域，只纳入全部区域汇总。

## 2026-08-17 经销商级账号可操作全部调拨门店

* 经销商级账号可以对本经销商内全部有效门店的调拨单执行确认出库、拒绝、确认入库和退回，不受用户管理中的门店分配列表限制。
* 调拨操作接口必须按调拨单实际门店校验门店存在、启用状态和经销商归属；不得因放开调拨操作而扩大普通库存、采购或其他业务权限。
* BOSS 继续拥有全局调拨权限；店长、店员等纯门店角色继续按原授权门店和本人职责限制操作。

## 2026-08-17 调拨门店候选与销售订单 SN 时点规则

* 调拨申请的调出门店、调入门店下拉展示当前经销商内全部有效门店，不受普通库存查询的已分配门店列表限制。
* 调拨相关的库存、SN 和库位读取必须通过 `scope=transfer` 进入专用权限链路；服务端仍校验门店有效、同一经销商和同一区域，不能借此扩大普通库存操作权限。
* 销售订单新建/提交阶段不要求录入 SN，不预占库存；订单归档时仍必须按商品、PN、SN 和库存规则统一校验并扣减库存。

## 2026-08-16 调拨入库单由调拨管理统一操作

* 调拨出库确认后必须继续生成 `source_type=TRANSFER` 的调拨入库单，保留入库单据和来源追溯。
* 通用/采购入库列表不得查询或展示调拨来源入库单；调拨入库单统一在调拨管理中查询。
* 通用入库执行接口不得执行调拨来源入库单；调拨入库必须通过调拨管理的调入确认流程完成。
* 采购入库、销售退库等非调拨入库继续沿用各自原有流程。

## 2026-08-15 经销商账号门店范围规则

* admin、财务、采购、商务、出纳等非门店角色账号属于经销商级账号：账号组织归属为经销商，但不再设置当前门店或默认门店。
* BOSS 继续拥有全局范围；纯门店角色为 `clerk`、`staff`、`manager`、`store_manager`、`store_admin`，继续沿用主门店/当前门店语义。
* 经销商级账号的有效门店范围唯一来源是用户管理中的有效门店分配；空分配返回空结果，不得回退到经销商全部门店、第一家门店或默认门店。
* 区域展示和区域校验只能由账号已分配门店反推；区域配置不得单独扩大门店数据范围。
* 业务新建表单必须明确选择目标门店，服务端必须校验目标门店属于账号有效门店范围；历史业务记录继续使用原 `store_id`。
* Web 和小程序均不为经销商级账号保存、恢复或自动切换当前门店；门店下拉只展示服务端返回的已分配门店。

## 2026-08-14 调拨出库 SN 自动带入调拨入库

* SN 商品调拨出库确认时，必须将实际出库的 `sn_id`、`sn_code` 保存到调拨明细，并同步带入调拨待入库明细。
* 调拨入库执行页面优先展示调拨出库已确认的 SN；已有 SN 自动带出且不可编辑，不要求入库人员重复录入。
* 调拨入库未指定目标库位时，默认使用调入门店启用的销售仓；页面仍允许改选其他有效库位。
* 调拨入库服务端必须再次校验请求 SN 与本次调拨明细的 `sn_id`、`sn_code` 一致；不一致时拒绝入库。
* 普通采购入库仍按原流程由入库人员录入 SN，销售退库继续使用原单据绑定的 SN。
* 调拨入库完成后，原 SN 才能从 `transferring` 变为调入门店的 `in_stock`，并保留调拨入库日志。

## 2026-08-09 批量出库仅调整库存

* 批量出库支持 SN 商品逐台出库和非 SN 商品按数量出库，审批通过前不得改变库存事实。
* SN 批量出库后将对应 SN 状态更新为 `out_stock`，同步扣减所属门店、仓位和库存类型的库存数量，并写入 SN 操作日志。
* 非 SN 批量出库按商品、门店、仓位和库存类型扣减数量，不允许形成负库存。
* 批量出库仅作为库存调整，不生成普通出库单、结算单、待结算记录、返利、应付或其他财务结果，也不核销资源权益。
* 批量导入必须逐行预校验；异常行不得进入申请并支持下载，校验通过的行可以生成同一张批量维护申请。
* 审批通过后的库存、SN和日志变更必须在同一数据库事务内执行，任一明细失败时整批回滚。

## 2026-08-09 单一经销商与区域归属规则

* 当前企业统一使用唯一经销商编码 `DIST001`；`DEFAULT` 仅作为历史数据编码，不再作为有效经销商归属。
* 组织运营区域固定为成都区域、重庆区域、地市区域三类，门店通过 `region_id` 归属区域。
* 用户列表展示“所属区域”，取用户已分配启用门店对应的去重区域；用户可以同时显示多个区域。
* “所属经销商”字段仍保留用于服务端组织边界和历史业务关联，不作为用户列表的主要展示字段。

## 2026-08-10 经销商级库存区域权限规则（已由 2026-08-15 规则修订）

* 经销商级账号的库存查询范围由用户管理中的有效门店分配决定；区域仅作为已分配门店的派生信息。
* 库存数据必须同时满足账号有效门店范围；账号未分配门店时返回空结果。
* 账号可以配置一个或多个区域用于展示和辅助校验，但区域配置不能替代门店分配，也不能扩大库存范围。
* 库存汇总页面增加门店字段；库存汇总导出沿用页面筛选条件，在同一工作表中按门店、商品展开，每行的库存数量对应具体门店。
* 店员和店长继续沿用现有门店权限；BOSS 继续拥有全局库存范围。
* 区域和门店范围必须由服务端校验，前端下拉和导出按钮不构成安全边界。

## 2026-08-04 数据一致性规则

账号所属经销商必须与其有效分配门店的经销商一致；账号分配多个门店时，所有门店必须属于同一经销商。门店权限发生变化时，账号所属经销商和主门店字段必须同步校验。

## 2026-08-04 权限范围最终规则（以本节为准）

* “所属经销商”用于账号组织归属、账号列表隔离和门店分配校验；不直接决定订单数据范围。
* 订单列表、订单明细、订单导出、国补照片列表和国补照片批量下载，统一按账号在“分配门店”中勾选的有效门店过滤。
* BOSS 为全局范围；其他账号没有任何已分配门店时，查询返回空结果，不得因为经销商归属而放开数据。
* 账号切换所属经销商时，原门店权限必须清空；重新分配的门店必须属于新的所属经销商。

## 经销商级订单与国补照片查询范围
* 财务、出纳、商务及其他非店员/店长的经销商级角色，可以查询所属经销商全部有效门店、全部人员创建的销售订单；BOSS 仍为全局范围。
* 订单列表、门店下拉、订单导出和国补照片批量下载均必须由服务端按 `distributor_id` 校验范围；前端传入的 `storeId` 只能缩小范围，不能扩大范围。
* 国补照片批量下载只处理当前筛选结果中用户有权限访问的订单，ZIP 按订单编号分目录；单订单下载和批量下载都必须记录业务操作日志。

## 店长销售订单导出权限
* 用户管理中显示为“店长”的 `manager` 和 `store_manager` 角色均可以导出销售订单。
* 店长订单导出范围与订单列表、订单详情保持一致，只允许导出账号已分配的有效门店；无门店授权时返回空结果。
* 店长导出继续沿用现有 58 列结构，并包含正式销售订单、定金收款订单和负向销售退单结算。
* 销售订单导出接口必须在服务端校验角色和门店范围，前端按钮显示不构成安全边界。

## 库存追溯与调拨规则

* Web 调拨管理必须查询 Web 和小程序发起的全部调拨记录；经销商级账号查询本经销商范围，BOSS 查询系统范围，店长按授权门店查询，店员只能查询本人参与的记录。
* 经销商级账号包括 admin、boss、采购、财务、商务等非店员/店长角色；店员、staff、店长、store_manager 不属于经销商级账号。
* SN 跟踪关联入库单的采购来源、供应商和价格属于受限信息，仅经销商级账号可查询，权限必须在后端接口校验。
* SN 跟踪节点关联的采购申请、销售订单、调拨单等原始单据必须沿用现有单据详情，不新建追踪专用业务表；跳转参数只用于打开只读详情。
* SN 跟踪原始单据可见范围：员工/店员仅本人发起，店长/店长账号按授权门店，经销商级账号按本经销商全部门店，BOSS 为全局；权限必须同时在追踪接口和原始单据详情接口校验。
* SN 信息和入库详情中的门店必须显示门店名称，不以门店编码作为用户可读名称。
* 入库单详情必须返回 SN 商品对应的 SN；非 SN 商品不生成虚假 SN。

## 销售退库与重新入库

* 销售退库申请必须经过店长、经销商账号两级审批；审批通过后自动生成一张 `SALES_RETURN` 待重新入库单。
* SN 商品审批通过后状态为 `return_pending`（退库待入库），在执行重新入库前不得再次销售，也不计入可销售库存。
* 重新入库必须使用原订单中的商品、PN 和 SN；系统预填原 SN，并在后端校验 SN 所属门店、商品和待入库状态。
* 执行重新入库成功后，原 SN 状态恢复为 `in_stock`，门店销售仓库存增加，写入退库和重新入库追踪日志；退库申请状态改为 `completed`。
* 销售退库待入库单以退库单号作为来源幂等键，已生成或已完成的退库单不得重复生成或重复入库。

## 销售退单结算与退款

* 销售退单完成后必须生成独立负向销售结算，负向结算关联原销售订单和退单申请；原销售订单、原用户应收、原收款方式和原收款流水不得覆盖、删除或改写。
* 退单金额按用户应收口径计算。整单退款时，用户实付与既有国补后返部分一并全额退还。
* 国补后返的 15% 是现行固定标准，不作为系统设置项，不允许门店或财务单独维护比例；计算沿用订单归档时保存的国补明细快照。
* 部分退款时，只有退回电脑、手机、平板等国补适用商品，才按退回商品占比启动国补和教育优惠分摊；退回其他商品时，国补和教育优惠分摊金额均为 0。
* 已收款订单的负向结算状态先为“待退款”；只有财务确认后才登记实际退款完成。退款方式支持原路退回，也支持选择其他收款方式，并保留退款方式、金额、确认人和确认时间。
* 已开票订单必须生成或关联红字发票处理记录后，才允许完成对应的开票冲销；不开票订单不生成红字发票。
* 国补负向结算必须分别生成“客户实收”负向明细和“政策补贴应收”负向明细；两类明细不得合并。
* 负向结算必须进入销售订单导出和经营报表。报表按原正向销售与负向退单结算的业务发生事实汇总，不通过修改原订单金额实现冲减。
* 负向结算创建、待退款、财务确认、红字发票处理和报表/导出查询必须具备幂等约束，重复执行不得重复冲减金额。


## 商品启用状态与业务选择

* 商品主数据只有同时满足 `status=1` 且 `is_deleted=0` 时，才属于业务操作可选择商品。
* 采购申请、销售、入库、出库、调拨、拆分/组装、退库、库存筛选及其他业务操作界面的商品选择，只能使用已启用商品。
* 商品搜索和列表接口应支持统一的启用商品筛选；商品管理、商品审批和启停用维护页面可以查看停用商品，不受业务选择筛选限制。
* 采购、销售和库存相关写入接口必须在服务端校验商品仍处于启用状态；前端筛选不构成业务安全边界。
* 已有历史单据的商品名称、编码和状态快照不得因商品停用被批量改写；历史单据允许查询和追溯。
* 包含已停用商品的业务草稿可以查看，但编辑、提交或继续执行时必须重新选择已启用商品。

## 商品 PN 主数据与销售归档

* `T_PRODUCT_PN` 是销售归档校验的唯一 PN 主数据来源；厂商编码就是 PN，商品 `manufacturer_code` 仅作为历史兼容镜像，历史厂商条码和库存记录不能替代 PN 主数据。
* 销售订单直接销售只允许扣减订单门店启用销售仓（`T_LOCATION.type=normal_qty`）库存；同商品同门店多个销售仓库位可以汇总扣减。
* 铺货仓、样品仓、占用库存、不可售仓和租赁样机仓不属于直接销售库存；普通 `pending_qty` 不计入销售归档可用库存。
* SN 商品归档及销售 SN 查询必须确认 SN 的 `inventory_type=normal_qty` 且所属启用库位类型为销售仓；非销售仓 SN 不允许直接销售。
* PN 比对统一按字符串处理，去除首尾空格、移除内部空格并统一小写；空值及“无、none、null、n/a、na、-、—”等占位值不作为有效 PN。
* 多个 PN 必须使用逗号、分号等明确分隔符；普通空格视为 PN 格式空格，不得把一个 PN 拆成多个编码。
* 商品创建、商品导入和商品编辑时，厂商编码必须同步维护 `T_PRODUCT_PN`，并与商品主数据变更保持在同一事务内；69码属于独立条码资料。
* 一个有效 PN 只能关联一个商品；发现 PN 已关联其他商品时必须拒绝写入，不得把既有 PN 记录直接转移到新商品。
* 销售归档必须先确认订单明细 PN 存在于有效 PN 主数据，再确认 PN 对应商品与订单明细 `product_id` 一致；PN 不存在、商品不存在或关联不一致时禁止归档。
* 库存记录存在只能证明库存事实，不能绕过 PN 主数据校验；需要 SN 时，还必须校验 SN 属于当前门店、商品、PN 且状态可用。
* 存量 PN 修复只允许依据明确的有效厂商条码自动补齐；同一 PN 被多个商品使用或只有历史删除记录时，必须进入人工复核清单，不得自动转移商品归属。
* 商品编辑页的 PN 列表必须直接来自有效的 `T_PRODUCT_PN`；PN 查询失败时必须阻止保存，不得用 `manufacturer_code` 或69码列表冒充 PN。
* 商品编辑修改 PN 时必须优先按原 `pn_id` 更新，保留 PN 主数据 ID 及 SN 关联，不得把编码修改实现为新增一条 PN 后软删除原 PN。
* `T_PRODUCT_BARCODE` 只维护69码等独立条码；历史厂商编码条码可以在商品编辑页单独删除，但删除历史条码不会删除 PN 主数据，也不会解除 SN 的 `PN_ID` 关联。
* `T_PRODUCT_PN` 的唯一性只约束有效PN（`status=1 AND is_deleted=0`）；商品删除后PN记录保留为历史，但必须同时标记为无效，不得阻塞其他商品合法使用同一PN。
* 兼容旧客户端未提交 `pns` 数组时，服务端按当前 PN 快照执行替换同步：提交中的 PN 保持有效，未提交的旧 PN 软删除，整个过程与商品编辑同一事务完成。
* 商品 PN 历史允许查询但不允许通过普通编辑直接物理删除；SN 商品最多保留一个有效 PN，发现多个有效 PN 且无法确定保留项时必须人工复核。
* 不新增 PN 关系表；继续使用 `T_PRODUCT_PN` 作为唯一 PN 主数据来源。非 SN 商品允许同一商品维护多个有效厂商编码 / PN，但每个有效 PN 仍只能归属一个商品；商品页面只保留一个 PN 维护区域。
* SN 商品继续只保留一个有效 PN；SN 的 `pn_code` 是入库时的历史快照，历史快照与当前主数据不一致时不得自动迁移商品或批量改写 SN。

## 采购结算与报销部分处理

* 公司账期采购结算支持按采购申请明细和金额选择商品，创建结算单时不展示或输入结算数量。
* 采购结算金额默认带出当前明细可结算的全部金额，允许修改为大于 0 且不超过该明细及原应付款剩余金额的部分金额。
* 采购结算明细继续保存商品和金额快照；金额结算产生的新明细数量为空，历史结算明细的数量字段继续兼容展示。
* 应付待结算清单支持按来源类型筛选，并展示筛选结果的条数和剩余待结算金额汇总；勾选记录后同步展示已选条数和金额汇总。
* 批量生成供应商结算单时，所选应付款必须属于同一供应商；勾选记录带入现有结算申请界面，继续沿用结算草稿、提交审批和审批后付款流程。
* 采购部分结算的明细可结算上限按扣除返利后的实际应付金额计算；结算明细保存商品、单价和金额快照。
* 采购结算单创建后为草稿；提交后必须进入待审批，审批通过后才进入待付款，审批拒绝退回草稿并保留审批意见。
* 应付结算单备注、提交时间、审批人、审批时间和审批意见必须随结算单保存并可在管理页面查看。
* 应付结算单按金额部分付款时，本次付款金额不得超过剩余应付金额；原始结算金额不覆盖，付款流水、累计已付金额和剩余应付金额必须持续保留。
* 个人垫付报销审批通过后，财务可以输入小于等于剩余可报销金额的本次报销金额；同一报销单允许多次生成报销结算单。
* 原采购应付款和原报销金额不得被部分结算覆盖；系统分别记录原始金额、累计已结算金额、累计已付款金额和剩余可结算金额。
* 采购部分结算后原应付款状态为“部分结算”；报销部分结算后原报销单状态为“部分报销”；剩余金额继续进入待结算清单。
* 只有累计结算金额达到原始金额且实际付款完成，采购应付款或报销单才允许进入已完成状态。
* 已生成有效结算的采购明细金额和报销金额不得再次结算；结算单作废后，仅释放尚未实际付款的分摊金额。
* 部分结算、部分报销、付款状态更新和作废释放必须在同一数据库事务中完成，并锁定原应付款、报销单和结算明细，防止超额结算。
* 报销审批仍以完整报销单为审批对象，部分报销不重复发起审批。

## 通用审批中心

规则：

* 审批实例同时记录业务类型、业务单据ID、申请人和审批主题人；审批主题人默认决定门店和组织范围。
* 审批人支持指定人员、主题人所在门店店长、主题人的直属上级以及角色加组织范围。
* 审批节点支持串行签批和或签；串行签批要求节点内审批人依次通过，或签任一审批人通过即可。
* 或签中单个审批人拒绝不会立即结束节点；全部可用审批人均拒绝后节点才拒绝。
* 申请人可以审批自己的申请；审批人解析不到有效人员时不得自动通过，申请必须阻止提交并提示配置问题。
* 审批拒绝后由申请人修改业务申请并重新提交；每次重新提交保留原审批任务和操作日志。
* 已发布流程不可直接覆盖；修改流程必须产生新版本，进行中的审批继续使用提交时的流程快照。
* 通用审批引擎只负责流程、任务和审计；库存、财务、商品等审批通过后的业务变更仍由对应业务模块在事务中执行。

## 组织审批关系

规则：

* 员工支持配置直属上级员工。
* 门店支持多个店长；“门店店长”审批人解析为主题门店内已授权且拥有 `manager`、`store_manager` 或 `store_admin` 角色的全部启用员工。
* 店长审批人必须同时满足启用状态、有效门店授权和经销商范围；普通经销商账号只能配置本经销商范围内的人员。

## Production Launch Data Cleanup Rules

Rules:
* Formal launch cleanup keeps system configuration, organization permissions, stores, users, suppliers, supplier payment accounts, product master data, and current product prices.
* Formal launch cleanup clears sales orders, inventory data, inbound documents, inventory resource rights, transfers, split/assembly documents, purchase records, daily statements, expenses, pending payables, payables, rebates, and account center balance sources.
* Settlement account definitions are kept. Account center balances must restart from zero by clearing settlement account transactions and supplier rebate records.
* If a preserved account master table later adds stored balance columns, the cleanup script must back up that table and reset those balance columns to zero.
* No cloud cleanup may run without a dry-run preview, target confirmation, backup, and explicit `CLEAR_TEST_DATA` confirmation.

---

## 文档目的

本文件用于沉淀企业经营经验。

经营规则属于企业核心资产。

所有规则应优先沉淀到本文件。

成熟后进入ERP规则中心。

禁止直接写死在代码中。

---

# 经营数据看板规则

## 结果层统计口径

规则：

* 经营数据看板只统计已归档且未删除的销售订单。
* 经营日期按销售订单 `CREATE_TIME` 归属，日期边界统一使用 `Asia/Shanghai`。
* 销售额按订单明细销售小计汇总。
* 成交单量按销售订单去重计数。
* 经营毛利使用订单归档时保存的订单经营毛利快照，不按当前商品成本或当前收款税率重算。
* 历史订单没有订单经营毛利快照时，只允许使用现有历史兼容毛利口径，并标记为旧口径。
* 毛利率等于经营毛利除以销售额；客单价等于销售额除以成交单量。
* 同比和环比由后端统一计算；比较基数为零时返回不可计算，不得显示为误导性的 `0%`。
* 自定义日期范围的环比使用紧邻当前范围之前的等长日期范围。
* 产品线按商品一级分类统计。
* 所有经营查询必须按账号授权门店过滤；无门店账号不得查询经营数据。
* 前端只负责展示后端指标结果，不得再次计算核心金额、毛利、同比或环比。

## 辅助销售业绩拆分

规则：

* 销售订单的业绩参与人包括主销售人和订单辅助销售人。
* 同一员工在同一订单参与人列表中必须去重，主销售人不得重复进入辅助销售人列表。
* 当前没有自定义比例时，订单销售额、基础毛利和已审批业绩毛利调整按参与人数平均拆分。
* 每名参与人均记录一笔参与单量；员工业绩汇总中的参与单量之和可以大于公司成交单量。
* 员工业绩明细必须保留订单号、门店、参与身份、参与人数、分摊销售额、基础毛利、调整金额和最终业绩毛利。
* 已审批毛利调整的申请编号、调整金额和原因必须在业绩明细中可查看。
* 经营毛利不因辅助销售拆分发生变化；拆分只影响员工归属和员工业绩展示。

## 重点产品

规则：

* 商品主数据可以勾选或取消“重点产品”。
* 重点产品属于经营策略标记，不改变商品售价、库存、成本、审批或销售流程。
* 重点产品看板只展示已标记商品在当前筛选范围内的真实销售额、销量和毛利。
* 未标记商品不得因为进入销售排行榜而自动成为重点产品。
* 重点产品标记变更只影响后续看板分类展示，不改写历史订单。

## 看板敏感指标权限

规则：

* BOSS、admin、财务可以查看其数据范围内的经营毛利和库存金额。
* 店长可以查看授权门店范围内的经营毛利和库存金额。
* 普通店员和采购角色可以查看授权范围内的销售额、单量、销量和排名，但不显示经营毛利、业绩毛利、成本或库存金额。
* 服务端必须执行敏感指标权限判断，禁止只在前端隐藏。

## 库存结果口径

规则：

* SN 管理商品的库存数量、成本和库龄以当前在库 SN 为准；每台 SN 必须能追溯采购价/提货价等单台成本来源。
* SN 管理商品的在线库存汇总读取与 SN 明细同步维护的 `T_INVENTORY` 物化余额；销售仓 `normal_qty` 的校验口径为有效库位中的 `in_stock` SN 数量，不在列表查询时逐商品实时数 SN。
* SN 状态变化与库存余额变化必须在同一数据库事务内完成；预占、释放、归档、调拨和退货入库等链路不得只更新其中一张表。
* `T_INVENTORY` 与 SN 明细不一致时，系统必须保留对账日志并按 SN 明细重建聚合投影；不得用两个数量取大值掩盖差异，也不得未经业务确认删除历史事实。
* 非 SN 商品的库存数量使用库存聚合记录，库存金额使用当前剩余 FIFO 成本层的加权平均成本。
* SN 库龄按入库时间计算；没有批次或入库时间的非 SN 库存不得伪造库龄。
* 当前没有库存历史快照时，库存金额同比和环比显示不可用。
* 经营预警和建议由服务端规则引擎统一生成，必须带有触发指标、原因和建议动作；规则建议只提供决策参考，不得自动修改价格、采购、库存或财务事实。
* AI 经营助手在真实模型未确认接入前使用可解释规则兜底；AI 输出不得替代库存、采购、销售和财务审批规则。

---

# 权限管理规则

## 用户多角色权限

规则：

* 一个账号可以绑定一个或多个启用角色。
* 菜单权限、按钮权限、审批资格、数据范围和门店范围均按账号全部角色取并集。
* 任一角色拥有某项权限时，账号即拥有该权限；不得只按员工表中的历史主角色判断。
* 员工角色关联表是多角色关系的正式数据来源，员工表原角色字段只用于兼容存量功能。
* 角色代码是系统内部稳定标识，由系统自动生成，业务界面只展示角色名称。

## 调拨历史查询权限

规则：

* 调拨历史查询包含全部调拨状态，不仅限于待出库、待入库或已完成记录。
* 店员只能查询本人参与的调拨记录；参与人包括申请人、调出确认人和调入确认人。
* 店长可以查询其授权门店作为调出门店或调入门店的全部调拨记录。
* 除店长、店员以外的经销商角色可以查询本经销商全部门店的调拨记录；系统 BOSS 可以查询系统内全部调拨记录。
* 调拨查询必须由服务端执行门店、经销商和参与人过滤，前端传入的门店筛选条件不得绕过数据权限。
* 历史调拨使用已保存的申请人及确认人姓名进行参与人匹配，账号姓名变更或同名账号造成的历史归属风险由后续员工主数据审计处理。

## 用户管理范围

规则：

* 用户管理只对经销商总权限账号和系统 BOSS 开放。
* 经销商总权限账号只能查看和管理本经销商及下属门店用户。
* 系统 BOSS 可以查看和管理全部经销商用户。
* 经销商不得分配 BOSS 角色，也不得修改 BOSS 账号。
* 普通账号的区域权限与门店权限同时配置；区域用于限定可选门店，门店多对多关系作为最终实际业务权限，用户列表同时展示所属区域和实际已分配门店。
* 新建账号默认不分配门店；现有账号由管理员逐个配置，不自动迁移。
* 经销商总权限账号只能分配本经销商的启用门店；不得跨经销商分配。
* 系统 BOSS 为普通账号分配门店时，可以查看并分配系统内全部启用门店，允许账号跨经销商门店工作。
* 系统 BOSS 始终拥有全部门店，不参与手工门店分配。
* 无门店账号允许登录，但不得查询或操作门店业务数据。
* 选择区域后，门店候选列表只能显示所选区域下的有效门店；选择全部有效区域时，可以选择全部有效门店。
* 服务端必须同时校验区域、门店、经销商归属；不能通过直接调用接口保存区域外或其他经销商门店。
* 网页端和小程序只能展示账号已分配门店；服务端必须对请求门店再次校验。
* 当前门店权限被取消后，客户端必须清除原门店选择并要求用户重新选择。

## 用户账号停用

规则：

* 用户账号状态分为启用和停用。
* 新建账号默认启用。
* 停用账号不得登录 ERP 或小程序。
* 停用账号在已有登录 token 未过期的情况下，也必须在下一次服务端接口鉴权时被拒绝访问。
* 停用账号不等于删除账号，历史订单、审批、库存、财务及操作记录必须保留原账号归属。
* 经销商总权限账号只能停用或启用本经销商管理范围内的普通账号。
* 经销商不得停用、启用或修改 BOSS 账号。
* 系统 BOSS 可按现有用户管理范围管理账号状态。
* 允许操作人停用自己的账号。
* 停用原因当前不强制填写，后续可扩展为操作日志或审批流程。

---

# 用户密码重置

规则：

* 用户密码以哈希形式保存，系统不得展示或反查原始明文密码。
* 用户管理范围内的 admin/boss 可将账号密码重置为该账号手机号后 6 位。
* 经销商管理员只能重置本经销商管理范围内的普通账号，不得重置 BOSS 或其他经销商账号。
* 当账号手机号为空或不足 6 位时，不允许执行密码重置。

## 用户修改密码

* 已登录用户只能修改自己的密码，服务端必须校验旧密码。
* 新密码至少 6 位，且不得与旧密码相同；校验必须在服务端执行，不能只依赖前端。
* 修改密码表单可以显示用户当前输入的内容，但系统不得读取、返回或反查已保存的原始密码。
* 密码必须以哈希形式保存，修改成功后当前登录会话保持有效。

---

# 采购规则

## 采购商品信息更正与在途采购单

* 商品名称或 PN 主数据发现录入错误时，可以先在商品主数据中按现有 PN 规则进行更正；不得因此批量改写历史采购业务快照。
* 采购单尚未入库的在途数量，允许同步修正商品名称或 PN，以保证后续收货、入库和 SN/PN 校验使用正确信息。
* 采购单已部分入库时，已入库数量对应的商品名称、PN 和原始采购事实不得修改；仅允许修正剩余未入库数量，并必须区分已入库与未入库部分。
* 采购单已全部入库、已结算或已关闭时，不得修改原始采购单中的商品名称、PN、采购价格、数量及其他业务快照；如确需说明错误，使用更正记录、调整单或备注保留审计链路。
* 在途采购信息的每次更正必须保存变更前值、变更后值、影响的未入库数量、原因、操作人和操作时间；不得静默覆盖原始变更记录。

## 采购退库与应付款冲减

* 采购退单页面统一输入本次退库数量；未入库商品取消待入库数量，已入库商品执行库存退库。
* 未入库 SN 商品不要求输入 SN；已入库 SN 商品必须选择属于当前采购入库明细、当前门店且状态为 `in_stock` 的原 SN。
* 已入库非 SN 商品按商品、门店、库位和库存类型扣减库存，不允许退库数量超过当前可退数量或形成负库存。
* 同一入库明细支持分次退库，但本次可退数量必须扣除历史已完成退库数量；已退库 SN 不得重复退库。
* 采购退库必须保留原采购单、原入库单、原应付款和付款流水，新增采购调整单、退库记录及不可变业务操作记录，不得物理删除或覆盖历史单据。
* 公司账期采购退库按扣除单位返利后的实际应付单价生成负向应付款调整。
* “财务已结算”的判定标准为：关联采购应付款只要已经生成结算单，即视为已结算；不再以结算单是否审批、是否进入待付款或是否已经实际付款作为采购退单的分界条件。
* 尚未生成结算单的采购，负向调整优先冲减原采购应付款的未结算余额；已经生成结算单的采购，负向调整形成供应商待抵扣余额，后续在同一供应商的下一次正向付款结算时抵扣。
* 负向应付款调整不得单独生成付款结算单或负数实际付款流水，必须与同供应商正向应付款合并后且净金额大于 0 才能进入付款结算流程。
* 采购退库涉及库存、SN、采购调整、返利恢复和应付款冲减时，必须在同一数据库事务中完成；任一明细校验失败时整批回滚。

## 采购付款方式与报销

规则：

* 采购申请付款方式分为公司账期和个人垫付。
* 公司账期采购审批通过后按现有供应商应付流程生成待付款清单。
* 个人垫付采购提交后自动生成采购垫付报销单。
* 个人垫付采购审批通过前，对应报销单不得生成报销结算单。
* 个人垫付采购审批通过后，领导审批报销单即可生成报销结算单。
* 个人垫付采购不得再生成供应商待付款，避免重复付款。
* 采购撤销时，关联的未完成报销单必须同步取消。

## 采购申请权限

规则：

* 一个账号只能绑定一个主岗位角色；角色决定业务能力，门店权限单独维护，不通过叠加多个角色扩大门店范围。
* 经销商级角色（采购、财务、商务、经销商总权限等）不使用 `T_STAFF.STORE_ID` 作为采购默认门店；新采购必须明确选择收货门店或填写门店分配。
* 店员/店长类门店角色可以保留主门店作为默认值，但实际可操作范围仍以独立门店权限为准。
* 所有已登录且已分配门店范围的账号均可查询其门店范围内的采购申请并提交新的采购申请。
* 采购申请审批、已通过申请撤销和供应商资料维护仍仅对采购、经销商总权限和系统 BOSS 开放。
* 供应商基础资料允许所有已登录账号查询，不要求采购角色或门店范围。

## 采购申请列表排序

* 采购入库单必须展示关联采购申请的发起人；入库操作人/审批人只作为独立审计字段，不得替代采购发起人。

规则：

* 待审批采购申请必须排在所有其他状态之前。
* 待审批申请组内按申请时间倒序排列。
* 已通过、已拒绝、已采购、已撤销以及其他非待审批状态不分组，统一按申请时间倒序排列。
* 没有独立可靠的提交时间时，使用创建时间作为排序时间。
* 分页查询必须在数据库层先完成全局排序，不得仅对当前页数据重排。

## 采购退单与未入库数量调整

规则：

* 已通过采购订单可由采购角色、经销商总权限或系统 BOSS 发起采购退单/数量调整。
* 采购退单采用独立调整单记录；原采购订单、原入库单和原始应付款不得物理删除或覆盖。
* 采购调整按“采购明细 + 待入库单明细”处理，支持增加或减少未入库数量；页面按门店和待入库单展示，避免多门店分配歧义。
* 已完成入库的数量不得通过采购退单直接减少；已入库商品必须使用库存退库流程。
* 调整后的待入库数量为 0 时，对应待入库单保留历史并标记为已取消；数量大于 0 时保留待入库状态。
* 每次采购调整必须保存原采购数量、已入库数量、调整前待入库数量、调整后待入库数量、数量变化、金额变化、原因、操作人和时间。
* 公司账期采购调整金额按采购明细实际应付单价计算；正数调整生成正数待付款记录，负数调整生成负数待付款记录，均关联采购调整单。
* 负数采购调整不得单独生成付款金额小于等于 0 的正式结算单；应与同一供应商的正数应付款一并抵扣。
* 采购明细存在返利抵扣时，调整金额按扣除单位返利后的实际应付单价计算；减少数量按比例退回返利抵扣，增加数量按比例追加返利抵扣并校验返利余额。
* 个人垫付采购不生成供应商待付款调整；采购调整金额同步调整关联采购垫付报销单，已付款报销单不得继续调整。
* 调整单、待入库数量、返利余额流水、待付款调整或报销单调整必须在同一数据库事务中完成。

---

# 商品价格规则

## 产品定价、零售价和最低售价

规则：

* 产品定价用于订单经营毛利的商品成本；如果订单明细是当前经销商配置了有效 SN 特价的 SN 商品，订单经营毛利的商品定价金额优先使用该 SN 特价。
* 零售价用于销售开单和小程序商品价格的默认带入价。
* 最低售价用于低价审批边界。
* 最低售价不得大于零售价。
* 商品价格批量导入可以更新产品定价、零售价和最低售价；空价格按 0 导入，导入后立即生效。
* 商品价格批量导入不得直接维护库存成本；库存平均成本由价格管理中的成本刷新逻辑按当前库存计算。
* 未设置零售价时，历史兼容可回退使用产品定价作为默认显示价。

## 供应商服务商与毛利上浮

规则：

* 供应商主数据维护“是否服务商”和“非服务商每件毛利上浮金额”。
* 服务商商品的订单经营毛利商品成本使用商品产品定价；特价 SN 商品优先使用有效 SN 特价。
* 非服务商商品的订单经营毛利商品成本使用采购价加每件固定上浮金额；例如采购价 4500 元、上浮 200 元时，毛利计算成本为 4700 元/件，即使产品定价为 5000 元也不使用产品定价。
* 上浮金额按销售明细数量逐件计算，不改变库存成本、采购应付、销售结算成本、厂家返利或历史财务事实。
* 未归档订单试算毛利时读取最新供应商服务商属性和上浮金额。
* 订单归档时保存供应商来源、采购价、上浮金额和毛利计算价格快照；已归档订单不得因供应商配置变化重新计算。
* SN 商品的供应商来源随入库来源保存；非 SN 商品按当前门店最近完成入库的采购供应商作为当前计算来源。

---

# 费用与报销规则

## 费用管理字段

规则：

* 费用单必须记录门店及区域、报销类型、费用发生方、金额、支付方式、是否有发票、费用发生日期和申请人。
* 报销类型由系统设置维护，费用单只引用启用的报销类型。
* 支付方式分为财务对公和私人垫付。
* 财务对公费用提交后生成应付管理中的待付款清单。
* 财务对公费用从应付清单生成费用结算单，提交后进入付款管理统一付款。
* 私人垫付费用提交后进入报销审批。
* 私人垫付费用审批通过后生成报销结算单，报销结算单提交后进入付款管理统一付款。
* 新建费用单保存后生成 `draft` 草稿记录并进入费用列表；草稿不生成应付记录、不进入审批或付款流程。
* 费用单草稿支持创建人、店长、管理员或 BOSS 编辑、重复保存、提交和删除；删除使用 `is_deleted=1` 软删除。
* 草稿提交时，对公费用进入待付款并生成应付记录；私人垫付费用进入报销审批；提交失败时草稿仍保留。
* 已提交、审批中、已付款或已结算费用单不得按草稿编辑或删除，草稿编辑不得生成新的费用单号。
* 申请人可以审批自己的报销单，但仍必须满足报销审批角色和数据范围要求。
* 付款完成后，关联费用单状态更新为已付款。

---

# 库存规则

## 库位与库存类型

规则：
* 库位管理采用全局标准仓位，不维护柜台、展台、售后区等门店物理位置。
* 标准仓位包括：销售仓、样品仓、铺货仓、不可售仓、占用仓、租赁样机仓。
* 租赁样机仓用于存放从厂商租入、用于门店出样的样机，不代表系统新增租赁业务；该仓位固定不可售，仅用于库存查询和库存分类展示。
* 库位类型即仓位编码，库位名称即业务显示名称；系统设置中不再重复维护“库位类型”和“库位名称”两套业务含义。
* 标准仓位按门店配置有效范围；经销商 admin 只能配置本经销商启用门店，BOSS 可以配置全部启用门店。
* 新建门店时默认生成六类标准仓位；后续可以在系统设置中按门店停用或重新启用。
* 仓位停用不等于删除；停用后不得用于新业务选择，但历史库存、SN、入库、出库、调拨和批量维护记录继续保留原仓位引用。
* 如果某门店仓位仍存在数量库存或在库 SN，禁止停用该门店仓位，必须先处理库存后再停用。
* Excel 批量维护可填写库位ID或库位名称；标准仓位名称按销售仓、样品仓、铺货仓、不可售仓、占用仓、租赁样机仓填写。
* `normal_qty` 显示为销售仓，`demo_qty` 显示为样品仓，`display_qty` 显示为铺货仓，`unsellable_qty` 显示为不可售仓，`pending_qty` 显示为占用仓，`rental_demo_qty` 显示为租赁样机仓。
* 库存汇总必须按标准仓位展示库存结果，并与系统设置中的仓位管理保持一致。

## 库存批量维护

规则：

* 库存批量维护必须通过 Excel 导入生成批量维护申请单，审批通过前不得直接修改库存、SN、资源权益或成本相关事实。
* 门店权限范围内发起的所有批量库存操作均必须由经销商总权限账号审批。
* 审批人只允许通过或拒绝，不得在审批环节修改申请内容；拒绝后由申请人修改后重新提交。
* 申请人可以审批自己的批量维护申请，但仍必须满足经销商总权限和数据范围要求。
* Excel 导入按行预校验；校验正确的行可以生成批量维护申请，失败行必须返回异常原因并支持下载后修正重导。
* 批量维护导入页应区分 SN 商品导入和非 SN 商品导入；批次级统一选择操作类型、标准仓位和资源权益，Excel 只保留行级必要字段。
* 预校验范围至少包括账号门店权限、经销商范围、商品是否存在、商品是否启用、商品是否 SN 管理、SN 当前状态、库位有效性、数量有效性、资源权益必填项和重复行。
* 审批通过后按申请明细逐条执行；每条明细的库存变更、SN 清单变更、资源权益变更、执行结果和操作记录必须在同一数据库事务中完成。
* 正确明细可以继续执行；单条明细失败时不得修改该明细的库存、SN或资源权益，并必须记录行号、执行状态、执行时间和失败原因。
* 批量申请状态区分执行中、已执行、部分执行和执行失败；部分明细成功且部分失败时，申请状态为“部分执行”，详情必须展示成功和失败明细。
* 执行中的申请恢复或重复调度时，已成功和已失败明细不得重复执行，只处理待执行明细；同一库存行执行时必须加锁。
* 批量维护必须保存导入文件、批次号、申请人、审批人、审批时间、执行时间、调整前值、调整后值、失败原因和备注。
* 批量维护记录不得物理删除；发生错误时应通过作废、拒绝或后续冲销流程处理。

## SN商品批量维护

规则：

* SN 商品批量维护以 SN 清单为准，每一行对应一台 SN。
* SN 商品不得通过数量字段批量增加、减少或覆盖库存。
* SN 商品批量入库时数量固定为 1，导入提货价/采购价必须落到 SN 单台成本字段，后续可按 SN 查询原始成本。
* SN 入库时必须填写或选择资源权益，资源权益必须落到 SN 维度。
* SN 批量维护应校验 SN、PN、商品、门店、库位、入库状态和资源权益规则的一致性。
* 历史状态为 `out_stock` 或 `sold` 的 SN 重新入库时，必须复用原 SN 记录并恢复为 `in_stock`；原销售、出库和追踪记录继续保留，不得新增重复 SN。
* 当前为 `in_stock`、`transferring`、`reserved`、`occupied` 或其他非可复用状态的 SN，不得重复入库；销售退库仍使用原销售退库流程。
* SN 批量维护审批通过后，应逐 SN 生成可追溯的变更记录。

## 非SN商品批量维护

规则：

* 非 SN 商品批量维护以门店、商品、库位和数量为核心，每一行对应一组库存数量调整。
* 非 SN 商品数量变更必须形成库存调整记录，不得直接覆盖库存余额。
* 非 SN 商品批量入库时，本次导入数量和提货价/采购价形成库存成本层；商品库存平均成本按当前剩余 FIFO 成本层加权计算。
* 非 SN 商品入库同样涉及资源权益时，资源权益按本次入库批次管理。
* 非 SN 商品批次权益应记录商品、门店、库位、批次数量、权益类型、权益规则和来源批量维护单。
* 非 SN 商品出库触发资源权益时，应按出库数量从对应批次权益中扣减或核销，保持数量与权益可追溯。
* 非 SN 商品库存调整不得导致可用库存数量异常为负，除非后续明确建立负库存业务规则。

## 批量出库资源权益

规则：

* 批量出库时，可对同一批货品统一勾选是否按规则产生资源权益结果。
* 同一批出库只能按一个标准统一执行，不得同一批内逐行选择不同权益触发标准。
* 勾选按规则产生资源权益后，系统应按当前有效规则统一生成权益核销、返利预估、下账、PO奖励或其他配置化权益结果。
* 未勾选按规则产生资源权益时，出库只处理库存事实，不得自动生成权益结果。
* 出库权益结果不得由人工在 Excel 中直接填写最终金额；金额应由系统按规则计算，人工只负责选择是否触发和提供必要来源信息。

## SN修改申请与审批

* 后台库存查询的“查看序列号”页面不得直接修改 `T_PRODUCT_SN`；SN变更必须先创建SN修改申请。
* 一张SN修改申请可以包含多条SN明细，每条明细保存原SN、新SN、PN、商品、门店和申请时快照。
* 仅允许修改当前状态为 `in_stock` 且属于申请人可访问门店的SN；已售、退库、调拨中及其他非在库状态不得修改。
* SN修改申请原因必填；审批拒绝时必须填写审批意见，申请人可以重新提交。
* SN修改申请由 `admin` 或 `boss` 审批，申请人可以审批自己的申请。
* 审批通过前不得改变正式SN；审批通过时必须在同一数据库事务内重新校验SN状态、原SN和同一PN下的新SN唯一性，然后更新SN并写入 `T_SN_LOG`。
* SN修改记录必须保存原SN、新SN、申请人、审批生效人、申请原因和操作时间；不得物理删除历史记录。
* SN跟踪按原SN或新SN查询时，都必须能看到该次SN修改记录。

## SN库存清单与经销商SN特价

规则：

* SN库存清单默认只展示当前在库且未删除的 SN；已售、退库及其他历史状态通过 SN 追踪查询。
* 清单必须按账号授权门店过滤，并在数据库层完成筛选、排序和分页。
* 清单展示 SN、PN、商品名称、门店、库位、资源权益当前状态、商品统一售价、SN特价、当前适用售价、库龄和备注。
* 当前在库 SN 可以通过库存清单直接调整库位，但目标库位必须属于 SN 当前所在门店且处于启用状态。
* 同门店库位调整只修改 SN 的库位，不改变门店、库存数量或 SN 状态；调整必须写入 SN 变更日志并记录原库位和目标库位。
* 已售、调拨中、退库中或其他非在库状态的 SN 不得使用同门店库位调整；跨门店移动必须使用调拨流程。
* 本规则暂不覆盖非 SN 商品按数量在不同库位之间的调整。
* SN 库龄按当前时间与入库时间的完整天数计算；缺少有效入库时间时显示未知，不得伪造库龄。
* 资源情况必须读取 SN 当前资源权益记录，不得使用货源性质或历史货型替代。
* SN 特价是销售售价，不改变库存成本或销售结算成本；订单经营毛利计算时，特价 SN 商品的商品定价金额优先使用该 SN 特价。
* SN 特价按 `SN + 经销商` 唯一保存；当前适用售价优先使用 SN 当前门店所属经销商的有效特价，否则使用商品最新统一售价。
* 商品统一调价不得新增、修改、取消或覆盖任何 SN 特价。
* 取消 SN 特价后，当前适用售价立即回退为商品最新统一售价。
* SN 跨经销商门店调拨后，只允许使用目标门店所属经销商配置的 SN 特价；其他经销商配置不得跨组织生效。
* 经销商 admin 只能维护本经销商当前在库 SN 的特价；BOSS 可以按 SN 当前门店所属经销商维护。
* 普通账号只能查看其授权门店范围内的 SN 库存，不得通过传入门店、经销商或 SN 参数越权维护价格。
* 销售选择 SN 后自动带入当前适用售价，允许继续修改实际成交价。
* 最低售价仅作为价格资料展示和经营参考，不再触发销售订单审批；实际成交价可以低于最低售价。
* SN 特价设置、修改和取消必须保存操作人、操作时间、调整前价格、调整后价格及备注，不允许物理删除变更记录。
* 已归档销售订单保存实际成交价格，不随统一售价或 SN 特价的后续变化自动修改。

## 资源类别配置

规则：

* 货型是采购资源子内容的组合模板，不替代 SN 维度的独立资源权益状态。
* 货型及其包含的资源子内容均由系统设置维护；采购选择货型时自动带出模板中的采购可选资源，并允许按商品调整。
* 货型删除采用停用方式，历史采购记录中的货型名称继续保留。
* 资源类别必须通过系统设置维护，不得继续写死在销售或库存代码中。
* 初始类别包括国补资格、教育补贴资格、销量报号资格；厂商返利作为独立下账类别管理。
* 类别应支持名称、简称、销售使用、公司套回、默认到账账户、排序、启停和备注配置。
* 类别应支持权益类型配置，包括销售使用、内部标记、PO奖励、Care可用金、返利和其他类型。
* 类别应支持是否采购可选、是否销售触发、是否生成下账、是否生成销售个人 Care 可用金、是否计入销售者业绩毛利等配置。
* 停用类别不再用于新业务，历史权益、变更和下账记录必须保留。
* 删除资源类别等同于停用并从货型组合中移除，不得物理删除历史权益、变更、成本和下账数据。
* 新增启用类别后，应自动出现在 SN 权益维护和适用的销售使用场景。

## 库存资源权益

规则：

* SN库存本体、税务属性和资源权益必须分离保存。
* 商品资源权益必须最终落到 SN 维度；判断一台机器有什么权益时，以该 SN 当前权益记录为准。
* 首期资源类型为国补资格、教育补贴资格和销量报号资格。
* 权益状态为可用、已锁定、已核销、已套回、不适用和异常。
* 已核销表示客户销售过程中使用；已套回表示公司提前收回，两者不得混用。
* 采购申请明细可以勾选多种采购可选权益；采购审批通过并入库后，入库 SN 继承采购明细勾选的权益。
* 采购后允许对未销售在库 SN 批量调整权益，调整必须生成权益变更记录。
* 权益规则变更后，可以按最新规则批量刷新未销售在库 SN 的权益金额和规则引用。
* 批量刷新不得影响已归档销售单、已销售 SN、已触发权益、已下账记录或已确认业绩毛利。
* 销售订单创建时不锁定资源权益。
* 销售订单归档时，校验并核销订单明细明确选择且当前可用的权益。
* 兼容历史已锁定权益的未归档订单；归档时继续核销，取消或审批拒绝时恢复可用。
* 权益锁定、核销、释放、套回及人工维护必须生成不可删除的变更记录。
* 销售标签由税务属性和权益状态自动生成，销售人员不得修改。
* 未税库存需要开票时必须提示操作人确认，但当前不强制拦截。

## 商品资源权益规则

规则：

* 商品资源权益规则按商品、供应商和权益类型配置。
* 未指定供应商的规则作为通用规则；存在指定供应商规则时，优先使用指定供应商规则。
* 规则算法至少支持固定金额、按库存成本比例、按销售金额比例。
* PO奖励来源可由商品和供应商共同决定，并可配置生效时间、失效时间和触发条件。
* PO奖励可选择是否计入销售者业绩毛利；计入时按配置比例生成业绩毛利调整，影响员工提成口径。
* PO奖励计入销售者业绩毛利时，不得改写订单原始毛利、库存成本、销售结算成本或财务利润。
* 套装资格只作为内部标记，不参与销售校验，不阻止或强制销售。
* 套装资格可在商品销售完成后用于提醒销售人员继续做组合销售。
* Care卡可用金属于销售个人可用金，不属于客户账户。
* 销售具备 Care 可用金属性的权益后，可以为销售人员生成个人 Care 可用金流水。
* 销售个人 Care 可用金未来只能用于配置允许的商品或服务核销，例如延保；具体核销范围必须由规则配置决定。

## 资源套回审批与产品成本

规则：

* 资源套回由运营或授权人员提交，财务单级审批。
* 申请人可以审批自己的套回申请，但仍必须满足财务角色和权益锁定校验。
* 套回申请提交后，对应权益进入锁定状态，审批期间不得用于销售。
* 财务审批通过后权益转为已套回；审批拒绝后恢复可用。
* 商品可以按资源类型分别定义资源成本金额，套回申请金额未填写时可引用该定义。
* 财务确认套回后生成产品资源成本调整流水。
* 产品资源成本调整后不得小于零。
* 产品资源成本流水不得自动改变销售结算成本。
* 资源套回与厂家政策分别记账，不互相自动计算。
* 成本流水必须使用来源类型和来源单号保证幂等，禁止重复入账。

## 资源权益下账

规则：

* 财务管理在“国补应收单”后设置独立“返利下账”页面；“资源权益核销与成本调整”不再包含资源下账子页。
* 销售使用权益和公司套回审批通过均生成待下账记录。
* 厂家返利预估和手工返利均进入同一待下账清单。
* 非供应商返利类资源的权益业务完成不代表资金到账；财务确认下账后，金额才进入资源类别配置的默认到账账户。
* 供应商返利采用“返利上账形成可用额度、返利下账关联核销”的并行流程，下账核销不得重复增加余额。
* 同一来源、同一资源类别只能生成一笔有效待下账记录，禁止重复入账。
* 下账目标可以是资金账户、供应商返利账户或 Care 可用金账户。
* 手工返利属于独立返利类型，不要求关联 SN 或商品，必须绑定供应商、填写金额和备注。
* 手工返利下账单新增时只记录厂商实际返利事实，不生成账户流水或增加供应商返利余额。
* 返利下账单必须关联同一供应商的返利上账单核销，不允许无上账单直接下账。
* 下账单支持部分核销，并展示原金额、已核销金额和剩余待核销金额。
* finance、admin 和 BOSS 均可查询、新增、核销、取消及撤销核销。
* 手工待核销记录允许填写原因后取消；部分或全部核销记录只能通过撤销核销释放关联金额。
* 下账及纠错必须记录来源、SN（如有）、商品（如有）、资源类别、供应商、金额、备注、操作人和操作时间。
* 下账数据预留批次号，用于未来批量上传和批量下账。
* 返利下账清单支持按创建时间、SN、备注、资源类型、来源类型、状态和供应商组合查询。
* 所有组合筛选、待处理优先排序和分页必须在数据库层完成。

## 统一账户中心

规则：

* 通用账户中心展示资金账户、政策补贴应收账户、Care 可用金账户和厂商返利账户；厂商返利账户的业务余额仍可在返利管理和采购页面查看。
* 资金账户包括银行卡、微信、支付宝等真实可用资金。
* 供应商返利账户属于系统内部受限额度账户，必须绑定供应商；返利池只能通过返利账户新增金额、返利上账单、采购抵扣、采购退单或上账单冲销流程变动。
* 每个供应商只允许一个启用的供应商返利账户；返利上账时后台仍可自动创建或恢复，也允许在结算账号管理中手工新增和编辑。
* 账户中心允许修改账户类型，支持资金账户、政策补贴应收、Care 可用金和厂商返利四种类型；已有流水、业务绑定和历史关联不阻止类型变更，历史流水保留不改写。
* 账户新增或修改为厂商返利时必须绑定启用厂商；修改为其他类型时清空厂商关联。账户中心不提供厂商返利普通手工记账入口，新增返利金额必须通过返利账户创建流程形成可追溯的返利业务记录。
* Care 可用金属于受限额度，不得作为普通现金理解；可在采购延保等指定用途时手工选择使用。
* 收款方式的默认结算账户和门店结算账户只能选择启用的资金账户，不得选择供应商返利账户、Care 可用金或政策补贴应收账户。
* 政策补贴应收必须使用独立字段绑定启用的政策补贴应收账户。
* Care 可用金继续在账户中心展示，但不得作为客户收款账户。
* 不同账户类型分别记账；返利预估和返利下账金额不得直接计入账户余额，只有已生效返利上账单形成可用额度。

## 返利上账、下账核销与冲销

规则：

* 财务通过“返利上账”创建正式上账单；上账单必须填写供应商、上账日期、大于零的金额和备注。
* 返利上账单生效后立即增加对应供应商返利余额并生成账户流水，采购可以立即使用。
* 采购订单继续手工填写返利抵扣金额；返利抵扣比例当前不由系统控制。
* 厂家返利预估或手工返利形成返利下账单后，只与已经生效的返利上账单核销，不再生成返利余额流水。
* 返利必须先上账才能使用或核销，不允许返利下账单直接增加供应商返利余额。
* 一张上账单支持分多次核销，一张下账单支持关联多张上账单；系统按每笔分配金额保存多对多核销明细。
* 上账单和下账单均必须展示已核销金额与剩余待核销金额，状态包括待核销、部分核销和已核销。
* 每次核销金额必须大于零，不得超过上账单或下账单剩余金额，且双方供应商必须一致。
* 活动级返利可以不关联 SN，但上账单和下账单都必须绑定供应商并填写备注。
* 撤销下账核销时保留原核销记录并标记已撤销，释放上账单和下账单的核销金额，不改变供应商返利可用余额。
* 返利余额流水不允许直接冲销；返利上账单已有核销时必须先撤销下账核销。
* 供应商存在尚未退回的采购返利占用时，不允许冲销任何返利上账单；必须先完成采购退单并恢复返利余额。
* 上账单冲销后生成反向供应商返利流水和账户流水，不允许物理删除或改写原记录。

## 缺货预警

条件：

库存 < 最近7天销量

建议：

补货

优先级：

高

---

## 高库龄预警

条件：

库存库龄 > 45天

建议：

促销

优先级：

高

---

## 库存积压预警

条件：

库存数量 > 0

且连续14天无销售

建议：

清库存

优先级：

高

---

# 销售规则

## 销售订单创建与归档

规则：

* 销售订单查询列表仅按订单创建时间倒序排列，最新创建的订单优先；不再按订单状态或订单 ID 追加排序规则。
* Web 端销售订单列表允许对正式销售订单的“未归档”状态发起归档；草稿、待审批、定金收款单和已归档订单不显示该操作。
* 正式销售订单只有“未归档”状态允许作废；已归档订单不得作废，必须通过销售退单申请进入退单审批和退货入库流程。前端入口隐藏不能替代服务端状态校验。
* Web 端归档必须复用现有销售归档接口和完整校验，不得通过直接修改状态绕过库存、PN、SN、毛利、返利、结算和操作日志流程。
* 销售订单备注在列表和订单详情中展示；备注允许独立修改，不得因此改写订单金额、库存、收款、返利或归档事实。
* 订单号属于展示字段，过长时允许在销售订单列表中最多分两行显示；订单列表不展示审批人列，但订单详情和审计记录继续保留审批人、审批时间及审批意见。
* 新建未归档订单时，PN 和 SN 均为可补录信息；系统只保存订单明细文本，不校验商品、PN、SN 或库存是否存在。
* 商品尚未建档、PN/SN 暂不存在或 PN/SN 为空时，仍允许提交未归档订单。
* 新建订单不得绑定 SN 库存记录，不得占用或扣减库存，不得锁定商品资源权益。
* 归档时必须校验 PN 对应商品存在。
* 商品需要 SN 管理时，归档前必须填写 SN，并校验 SN 属于对应 PN、当前门店且处于可售状态。
* 非 SN 管理商品填写了 SN 时，归档时也必须校验该 SN。
* 只有归档校验全部通过后，才能绑定 SN；正毛利订单随后扣减库存、核销资源权益并生成归档结算数据，负毛利订单在审批前预占库存并锁定资源权益，审批通过后再完成最终扣减和核销。
* `T_INVENTORY` 按商品、门店和库位保存多条库存行；销售归档必须汇总订单所属门店的全部库存行，不得使用不带库位条件的单条记录作为库存总量。
* 非 SN 商品归档可售数量统计销售仓、铺货仓和已有占用库存；样品仓、不可售仓和租赁样机仓不计入可售数量。
* 归档校验和实际扣减必须在同一事务内使用锁定的库存行，并按占用库存、销售仓、铺货仓顺序分配，禁止因库位行读取顺序导致库存误报或扣减错误。

## 销售订单草稿

* 新建销售订单点击保存后必须生成一条 `draft` 草稿记录，并在销售订单列表展示。
* 销售订单草稿支持由创建人、已授权门店店长、管理员或 BOSS 编辑、重复保存、提交和删除；删除采用 `is_deleted=1` 软删除。
* 销售订单只有从未提交过的草稿可以删除；已提交后不得删除。
* 草稿编辑必须更新原订单及其明细、收款信息，不得重新生成订单号或重复生成业务单据。
* 销售订单草稿不扣减库存、不绑定 SN 库存、不锁定或核销资源权益、不占用定金。
* 草稿提交时不触发低价审批；服务端计算当前试算毛利，若为负毛利只向提交人提示“归档时将进入审批”，不阻断提交。
* 点击归档时必须先完成 PN、SN、库存、定金和资源权益校验；校验失败不得进入审批。负毛利订单校验通过后，在进入审批的同一事务内预占库存、锁定资源权益，但不得最终扣减库存、核销权益或生成归档结算数据。
* 归档前最终毛利包含支付手续费、增值税、补录净额及符合条件的外调费；最终毛利小于零时订单进入 `pending_approval`，等待店长或经销商总账号审批。
* 负毛利订单审批通过后，在同一事务内完成预占库存/SN状态变更、定金核销、资源权益最终核销、结算数据和最终毛利快照，并自动转为“已归档”；审批通过前保留一次带行锁的二次校验。
* 负毛利订单审批拒绝后释放预占库存和锁定的资源权益，退回“未归档”，保留拒绝原因和操作日志，允许修改后重新归档；审批期间不得重复归档。
* 已提交、审批中、已归档或已取消的销售订单不得按草稿编辑或删除。

## 国补照片查询与维护

* 销售管理“国补照片”页面查询订单创建时间、补贴人姓名、补贴人手机号和开票信息中的云闪付订单号。
* 页面展示销售订单 `SUBSIDY_PHOTOS` 中已保存的全部照片；没有国补照片的订单不进入结果列表。
* BOSS、admin、财务和店长可以访问国补照片功能；店长仅能访问其授权门店范围内订单，多名店长按同一范围规则处理。
* 国补照片的查询、重新上传和下载必须由服务端再次执行账号及门店范围校验，不能只依赖前端菜单或按钮隐藏。
* 重新上传采用整组替换，至少上传一张 JPG、PNG 或 WEBP 图片，单张不超过 10MB，最多 20 张。
* 重新上传不改变订单、收款、库存、审批或归档状态；仅更新 `SUBSIDY_PHOTOS`，并追加不可变销售订单操作日志。
* 服务端生成的照片文件存放在受保护目录中，下载必须通过鉴权接口；下载行为需要记录操作人、订单和照片标识。

## 国补 POS 收款记录

规则：

* 国补 POS 参与补贴金额必须拆分为“客户实收”和“政策补贴应收”两条订单收款记录。
* 客户实收金额等于参与补贴金额扣除国补优惠后的金额。
* 政策补贴应收金额等于国补优惠金额，表示后续待政策结算的应收款。
* 收款记录名称保留原支付渠道和品类，例如“国补POS（电脑）-客户实收”和“国补POS（电脑）-政策补贴应收”。
* 当前客户实收校验不得包含政策补贴应收，避免把后到账款误判为已收客户款。
* 两条国补收款都必须持久化到订单收款表，订单归档时都必须进入日结订单明细。
* 客户实收日结明细下账到国补 POS 配置的客户实收资金账户。
* 政策补贴应收日结明细下账到单独配置的政策补贴应收账户；未单独配置时使用系统默认“政策补贴应收”账户。
* 日结明细没有下账账户时不得标记为已下账，防止只改状态但未生成账户流水。
* 订单详情必须同时展示客户实收、政策补贴应收和定金抵扣。
* 定金抵扣属于订单资金来源，不属于本次新增现金收入；详情展示不得造成账户流水或营业收入重复入账。

## 国补应收到账与核销

规则：

* 政策补贴应收账户表示尚未回收的应收资产，不得直接作为实际银行到账资金账户。
* 实际到账账户按区域配置并允许切换；成都区域默认四川银行（艾诺云），重庆区域默认兴业银行（艾诺志兴），地市区域暂不配置。
* 区域账户配置变更只影响后续到账登记；到账单必须保存实际到账账户快照。
* 未配置区域到账账户时不得自动跨区域兜底，必须先配置再登记到账。
* 一笔银行到账可以分配到多张国补应收单，一张国补应收单可以被多次到账核销。
* 本次分配金额不得超过到账单未分配金额，也不得超过对应应收单剩余应收金额。
* 银行实际到账金额进入对应资金账户；只有分配到应收单的金额才减少该应收单剩余应收。
* 国补应收状态分为待回款、部分到账、已到账和差额结清。
* 到账金额超过已分配金额时，剩余部分作为未分配到账款保留，可后续继续分配或退款。
* 手续费和无法继续收回的差额必须单独记录类型、金额、原因、申请人和审批人。
* 任何差额核销均须由财务提交、admin 或 BOSS 审批，申请人可以审批自己的申请。
* 差额审批通过后才减少剩余应收；拒绝时不得改变应收余额。
* 到账、核销、差额和退款记录不得物理删除；错误记录必须通过冲销生成反向记录。
* 历史已下账记录保持原样，不自动迁移为新的到账单或重新计算账户余额。

---

## 爆款增长预警

条件：

最近7天销量

> 最近30天平均销量 × 1.5

建议：

增加备货

优先级：

中

---

## 销售下滑预警

条件：

最近7天销量

< 最近30天平均销量 × 0.5

建议：

分析原因

优先级：

中

---

# 毛利规则

## 订单经营毛利

公式：

订单经营毛利 = 用户应收 - 产品定价 - 应收税率费用 - 增值税 + 补录净额

增值税 = max(开票金额 - 产品定价, 0) × 13%

规则：

* 用户应收等于订单商品总额扣除普通折扣后的金额，不因使用定金等付款来源而减少。
* 国补和教育补贴分别属于国家及厂商承担的订单收入，测算毛利时必须计入用户应收口径，不得作为优惠从毛利收入中扣除。
* 定金抵扣及其他收款记录只用于确定用户应收在各收款方式中的分配比例，不作为毛利收入项。
* 应收税率费用按用户应收在各收款方式中的分配额乘对应默认税率后汇总，不按实际到账金额计算。
* 收款方式默认税率使用百分数维护，例如 `0.6` 表示 `0.6%`。
* 产品定价按订单明细计算时保存的单件产品定价乘数量后汇总。
* 尚未设置产品定价时，商品首次采购入库以采购价作为默认产品定价。
* 产品定价可在“商品管理－价格管理”中人工调整；非零产品定价不得被后续采购入库或成本刷新自动覆盖。
* 开票金额减产品定价为负数时，增值税计税差额必须核为零。
* 补录项目必须配置增加或减少毛利方向；订单补录时保存项目、金额和方向快照。
* 订单归档时必须生成最终经营毛利快照，并保存用户应收、公式版本和全部组成明细。
* 未归档订单可以生成试算快照；订单归档后转为最终快照。
* 已归档快照使用计算时保存的收款税率，不随收款方式后续修改自动变化。
* 毛利公式版本升级后，系统启动时必须重算旧版本快照，使经营看板和员工业绩同步使用新口径。
* 产品定价授权重算、开票金额修改或订单补录修改后，必须同步重算订单毛利；重算仍沿用已封存的收款税率。
* 经营看板、销售报表和员工业绩基础毛利统一使用订单经营毛利快照。
* 订单提交时只提示负毛利，不阻断提交；点击归档后以包含手续费、增值税、补录净额和外调费的最终毛利判断是否进入审批。
* 最终毛利小于零的订单必须在归档前进入销售审批；审批通过后自动归档，审批拒绝退回未归档并允许重新归档。
* 商品和产品线毛利按商品销售小计占订单销售总额的比例分摊。
* 没有新毛利快照的历史订单可以暂按原归档毛利兼容展示，但必须标记为旧口径。
* 毛利查看权限继续只开放给 BOSS、admin、财务及授权门店店长。

## 员工业绩毛利调整

口径：

业绩毛利 = 订单经营毛利快照 + 已审批增加金额 - 已审批减少金额

规则：

* 只有已归档销售订单可以提交毛利调整申请。
* 所有启用账号都可以提交申请。
* 申请必须明确增加或减少、调整金额和调整原因，可上传证明附件。
* 同一订单可以多次提交调整申请。
* 每笔申请先由财务账号初审，再由 admin 账号复审。
* 申请人可以审核自己的申请，但仍必须按财务初审、admin 复审的节点和角色要求执行。
* 仅 admin 复审通过后，调整金额才计入员工业绩。
* 财务或 admin 拒绝申请时必须填写原因。
* 审批后的调整归属原订单员工和原订单日期。
* 毛利调整不修改订单原始毛利、销售结算成本、返利预估或厂家政策记录。
* 员工业绩基础毛利使用订单归档时保存的订单经营毛利快照，不使用当前商品成本或当前收款税率重算。
* 未生成订单经营毛利快照的历史订单，报表临时按原归档毛利兼容展示，并明确标记为旧口径。
* 历史订单由授权角色通过现有重算入口单笔处理，系统不自动批量改写；完成重算后自动转为归档快照口径。
* 申请、附件、财务初审、admin 复审和拒绝原因必须可追溯。

---

## 毛利异常预警

条件：

毛利率低于预设阈值

建议：

检查售价

检查采购价

优先级：

高

---

# 商品审批规则

## 新建商品审批

规则：

* 手工新建商品必须先提交商品申请，不得直接进入正式商品库。
* 商品申请必须保存商品名称组合、分类、动态属性、厂商商品名称、厂商编码、单位、SN/IMEI要求和备注快照。
* 经销商级 finance、purchaser、admin 账号及系统 boss 可以审批商品申请。
* 申请人可以审批自己的申请，但仍必须满足商品申请审批角色和经销商范围要求。
* 拒绝申请时必须填写原因。
* 只有审批通过后才能创建正式商品并生成商品编码。
* 待审批和已拒绝的商品不得用于采购申请、库存转换或其他正式业务单据。
* 审批申请和正式商品必须保留关联关系，便于审计追溯。

---

# 商品价格规则

## 价格管理导入口径

规则：

* 商品价格管理中的“产品定价”是订单经营毛利的商品成本口径。
* 产品定价首次默认采用采购价，允许在价格管理中单独调整；人工调整后不得被采购价或库存加权成本自动覆盖。
* 商品价格管理中的批量导入定价只更新定价、零售价和最低售价；空价格按 0 处理并立即生效。
* 商品库存平均成本不通过价格导入模板维护，应由价格管理中的成本刷新按 SN 个别成本或非 SN FIFO 剩余成本层计算。
* 批量导入模板不使用商品名称作为匹配字段。
* 商品编码和厂商编码至少填写一个。
* 只填写商品编码时，按商品编码匹配商品。
* 只填写厂商编码时，按厂商编码匹配所有关联商品，并全部更新。
* 商品编码和厂商编码都填写时，商品编码优先，同时校验厂商编码必须属于该商品。
* 定价和最低售价至少填写一个。
* 最低售价必须小于或等于定价，否则对应行标记为异常。
* 上传价格文件后只选择文件，不立即触发校验。
* 用户点击立即导入时，系统执行校验并导入。
* 导入时只处理校验通过的记录，异常记录跳过。
* 异常记录不入库、不生成价格变更记录，并应支持下载 Excel 后修改重提。
* 同一商品在同一批导入中被多行命中时，相关行标记为异常。
* 调价原因非必填。
* 暂不支持按门店设置不同商品价格。

## 商品与定价批量上传处理

规则：

* 商品和定价 Excel 上传后，必须先校验文件格式、工作表、表头和数据行格式，校验完成前不得开始正式导入。
* 导入按行处理；校验正确的行继续导入，校验失败的行不得入库，必须进入失败清单并记录 Excel 行号、原始数据和失败原因。
* 失败清单必须支持下载；用户修正后可以重新上传，原失败任务记录继续保留。
* 商品批量上传保持现有口径：厂商编码命中已有商品时更新，不存在时新增。
* 商品和定价批量上传采用后台异步任务，前端必须展示排队中、处理中、已完成、部分失败或失败状态；请求超时不得直接显示为导入失败。
* 异步任务必须保存来源文件、任务号、提交人、处理时间、处理结果和失败行快照，支持服务重启后恢复未完成任务。
* 定价批量上传必须在校验通过后批量加载商品、厂商编码和现有价格，并按批次生成价格变更记录；不得因为文件行数增加而退回逐行同步导入口径。

## 价格生效与追溯

规则：

* 生效时间为空时，价格立即生效。
* 生效时间晚于当前时间时，价格作为待生效记录保存，到达生效时间后由系统任务自动生效。
* 商品当前价格用于销售开单和当前价格展示。
* 每次价格调整必须生成商品级价格变更记录。
* 价格变更记录应包含商品、价格字段、调整前价格、调整后价格、生效时间、来源、批次、操作人、操作时间、调价原因和备注。
* 厂商编码对应多个商品时，应为每个被影响商品分别生成价格变更记录。
* 已发生的历史销售订单不因商品调价自动重算。
* 如需修正历史订单金额或结算口径，应后续由授权角色通过独立重算流程处理。
* 商品统一售价是未配置经销商 SN 特价时的销售回退值；统一调价不得覆盖独立的经销商 SN 特价。
* SN 特价影响销售默认带入价格；订单经营毛利计算时，特价 SN 商品使用 SN 特价替代商品统一定价，但不修改商品定价主数据。

---

# 门店运营规则

## 门店低活跃预警

条件：

当日无开单

或

开单量低于预设阈值

建议：

检查销售动作

优先级：

中

---

# 规则管理原则

所有规则必须：

* 可配置
* 可启停
* 可追溯
* 可扩展

规则不得写死在代码中。

规则应存储于数据库。

由规则引擎统一执行。

---

# AI扩展原则

AI不得直接替代规则。

正确顺序：

规则
→ 分析
→ AI增强

而不是：

AI
→ 猜测经营逻辑

AI负责：

* 总结
* 分析
* 建议生成

经营规则来源于企业实践。

---

# 主数据展示规则

## 人工排序

规则：

* 商品分类、结算账户、供应商等基础主数据应支持人工维护展示顺序。
* 商品分类按同级分类内部排序，不跨父级分类混排。
* 其他业务页面引用商品分类、结算账户和供应商时，应优先按维护顺序展示。
* 新增主数据默认排在当前列表或当前同级分类最后。
* 停用或删除的主数据不参与正常业务引用展示，但历史记录仍应保留原有名称和关联信息。

## 业务清单排序

规则：

* 销售、采购、审批、库存单据、财务处理和资源下账等业务清单，默认将仍需人工处理的记录排在已完成记录之前。
* 同属待处理组的记录按业务日期或提交时间倒序排列，最新记录在最前。
* 非待处理记录不再按完成结果细分排序，统一按业务日期或提交时间倒序排列。
* 同一时间的记录按业务单据唯一 ID 倒序排列，保证分页顺序稳定。
* 分页清单必须先在数据库层完成全局排序，再执行分页，不得仅重排当前页。
* 商品分类、结算账户、供应商等基础主数据不使用待处理优先规则，继续按人工维护顺序展示。

---

# 应付结算规则

## 应付结算单基础状态

结算单状态：

* 草稿
* 待审批
* 待付款
* 已作废

规则：

* 待付款记录生成结算单后，结算单默认进入草稿状态。
* 结算单生成后，对应应付款不再显示在待付款清单中。
* 草稿结算单提交后进入待审批状态；审批通过后进入待付款状态；审批拒绝后退回草稿并保留审批意见。
* 结算单仅允许删除从未提交过的 `draft` 草稿；已提交后即使退回草稿，也不得删除。
* 结算单草稿删除使用 `is_deleted=1` 软删除，保留结算单、明细和业务操作日志；正常结算列表不展示已删除记录。
* 删除结算单草稿后，必须在同一事务内重新计算关联应付款及费用状态，释放本次草稿占用的结算金额。
* 待付款结算单可以进入付款管理。
* 待付款结算单允许作废。
* 已作废结算单不退回待付款清单。
* 应付结算单管理作为财务模块独立页面，负责结算单详情、审批、作废和部分付款。

## 应付实际付款

付款状态：

* 未付款
* 部分付款
* 已付款

规则：

* 只有待付款且未付清的结算单可以进入付款管理清单。
* 剩余应付金额必须大于0；金额为0的结算单不进入待付款清单，也不允许执行立即付款。
* 付款管理是财务管理页面内的顶部标签页，与日结单、费用管理、应付管理、返利管理同级，不进入左侧菜单树。
* 当前应付结算审批纳入现有财务角色权限，制单人不得审批自己的结算单；实际付款仍由付款管理处理。
* 付款清单支持导出。
* 付款结果通过导入方式回写系统。
* 导入付款结果前，必须在页面统一选择一个付款账户。
* 导入确认前，必须提示当前操作账户、付款笔数和付款总额。
* 导入时先整批预校验。
* 任一行校验失败，则整批拒绝，不扣款、不生成付款记录、不生成账户流水。
* 本次付款金额必须大于0，且不得超过结算单剩余未付款金额。
* 支持部分付款。
* 待付款清单支持单笔立即付款登记，单笔付款默认带出全部剩余应付金额，并允许改为部分付款金额。
* 单笔立即付款必须选择一个启用的结算账户。
* 单笔立即付款只登记付款事实，不调用银行或第三方支付接口。
* 结算账户余额不足时允许继续付款并形成负数余额，但必须在提交前提示本次付款后的预计余额。
* 单笔立即付款应生成只有一条付款记录的付款批次，并沿用付款批次撤销规则。
* 导入成功后，应生成付款批次、付款记录和结算账户流水。
* 导入成功后，应从所选付款账户扣减对应金额。
* 同一导出行不得重复导入扣款。
* 付款完成后，应可在结算单详情中查看付款记录。
* 付款批次支持整批撤销。
* 撤销付款批次时，不删除原记录，应生成反向账户流水，并回退结算单付款状态。

## 应付付款权限

规则：

* 当前阶段应付结算提交和实际付款操作纳入现有财务角色权限控制。
* 暂不新增独立权限管理页面。
* 后续需要支持独立权限管理页面和按钮级权限。
* 后续付款审批人、审批条件和审批流转由独立审批流程管理统一配置。

---

# 厂家返利与销售结算成本规则

## 核心口径

规则：

* 厂家返利不直接等同于销售奖励或销售提成。
* 厂家返利预估用于财务、运营和供应商对账。
* 销售结算成本用于厂家政策成本调整和历史销售毛利口径；订单经营毛利及员工业绩基础毛利使用产品定价快照。
* 销售端不展示厂家返利、P差、活动补贴、激励分配等复杂明细。
* 销售端只展示当前销售所需的结算成本口径。

## 销售结算成本

公式：

销售结算成本 = 原始库存成本 - 成本调整金额

厂家政策销售毛利 = 销售金额 - 销售结算成本

规则：

* 成本调整金额来自厂家政策自动计算。
* 一台机器可以同时命中多个厂家政策。
* 后台返利预估总额和销售结算成本调整金额可以不同。
* 原始成本是否展示给销售，由现有角色权限控制。
* 自定义成本调整规则当前仅预留，不执行自定义计算。

## P差计算

公式：

P差金额 = 库存原始提货价 - 销售时厂家当前提货价

规则：

* 只有库存原始提货价大于销售时厂家当前提货价时，才生成P差返利预估。
* P差不按销售价格计算。
* P差不按当前售价和进货价差额计算。
* 库存入库时应记录原始提货价。
* 销售完成时按销售日期、PN和供应商匹配厂家价格历史中的有效提货价。

## 厂家价格历史

规则：

* 厂家主体沿用现有供应商资料。
* 厂家价格按供应商、PN、型号、生效日期、失效日期和提货价形成历史记录。
* 同一PN允许存在多条不同日期的价格记录。
* 销售计算时匹配销售当日最近一条有效价格。
* 价格导入必须整批校验，校验失败时整批不入库。

## 返利预估与成本调整

规则：

* 销售归档时自动生成返利预估记录。
* 销售归档时自动生成销售结算成本调整明细。
* 返利预估记录表示供应商预计返多少钱。
* 销售结算成本调整记录表示公司允许下浮多少销售成本。
* 两类记录可以关联，但不得混为一张账。
* 已生成销售结算成本调整的订单不自动重复计算。
* 如需修正历史订单，由授权角色手工触发重算。

---

# 定金管理规则

## 定金单状态

定金单状态：

* 可使用
* 已占用
* 已核销
* 已退款
* 已作废

规则：

* 客户或店员可以提交定金单。
* 定金单提交后立即进入收款员工名下的客户定金库，无需二次归档。
* 只有存在可用余额、未退款、未作废的定金单可以用于正式订单抵扣。
* 定金单不计算销售毛利，不生成销售结算成本调整。
* 定金单不参与库存扣减。
* 正式订单提交并选择定金抵扣后，定金状态变为已占用，不在此时增加已核销金额。
* 正式订单归档后，预占记录转为核销记录，定金状态变为已核销。
* 正式订单作废时，无论作废前是否归档，都撤销该订单对应的预占或核销金额，定金状态恢复为可使用。
* 定金预占、核销和释放必须与订单状态变化使用同一个数据库事务。

## 定金核销

规则：

* 正式订单收款方式选择“定金”时，必须绑定具体定金单。
* 定金抵扣金额默认按定金可用余额与订单应付金额中的较小值带出，允许手工修改。
* 定金支持部分抵扣，剩余余额继续保留在原定金单中。
* 本次抵扣金额必须大于零，不得超过定金可用余额或订单扣除其他优惠后的应付金额。
* 一张正式订单最多绑定一张定金单。
* 店员只能核销自己收取的定金单。
* 不支持定金转让。
* 定金单客户电话必须与正式订单客户电话一致。
* 订单未归档期间，被该订单占用的定金不得再次用于其他订单。
* 订单归档后定金状态变为已核销。
* 正式订单取消、作废或审批拒绝时，只释放该订单实际占用或核销的金额，原记录标记作废。

## 定金退款

规则：

* 当前阶段定金退款只记录业务事实。
* 定金退款不处理收付款账户。
* 定金退款不生成账户流水。
* 定金退款不做审批。
* 只有未核销的定金单可以退款。
* 当前阶段只支持全额退款记录，不支持部分退款。

---

# 商品拆分与组装规则

## 核心口径

规则：

* 商品拆分与组装属于库存转换业务，不属于采购业务重写。
* 原采购单、原入库单必须保留真实采购商品，不因后续拆分或组装被修改。
* 拆分与组装必须保持商品、PN、SN、门店、库位、单台成本和来源单据可追溯。
* 库存成本以单台成本作为销售毛利和经营核算主口径。
* 财务报表中的移动加权成本仅作为统计分析口径，不替代单台成本追溯。
* 所有商品默认允许拆分和组装，当前不按商品类别限制。

## 商品新建规则

规则：

* 拆分后或组装后的结果需要长期销售、独立库存、独立定价、独立售后或独立毛利核算时，应新建商品。
* 仅用于一次性客户定制、临时组合销售或销售话术组合时，不应强制新建商品。
* 销售套餐如果不真实入库，应作为销售组合处理，不应混同为库存商品。
* 组装成品如果提前入库并作为独立库存销售，应新建商品并生成对应库存和 SN。
* 拆分单和组装单目标商品区域可以按商品管理的新建商品流程创建商品。
* 拆装单内新建商品成功后，应自动选中新建商品作为目标商品。

## 拆分规则

规则：

* 拆分单应记录被拆商品、被拆 PN、被拆 SN、被拆门店、被拆库位和被拆商品单台成本。
* 拆分执行后，被拆商品可以继续销售，不强制减少被拆商品库存。
* 被拆商品如有 SN，原 SN 保留原有 SN 和在库状态，不需要标记为已拆分。
* 拆分执行后，原商品成本 = 拆分前原商品成本 - 拆出商品成本合计。
* 拆出商品成本合计不得超过被拆商品当前成本。
* 拆出商品如需要 SN，应录入或生成新的 SN。
* 拆出商品 SN 应能追溯到原商品 SN、原商品 PN、原采购入库记录和拆分单。
* 拆出商品成本默认带出目标商品成本价，允许用户手工修改。
* 拆分单中被拆商品成本可以自动带出当前商品成本，拆出商品价格输入提示为“请输入商品价格”。
* 拆分单成本守恒口径为：拆出商品成本合计 + 拆分后原商品成本 = 拆分前原商品成本。
* 拆分成本新规则只影响新拆分单，不处理历史拆分单。
* 拆分单允许作废或反向冲销。
* 作废或反向冲销不得删除原记录，应生成反向库存、成本和 SN 追溯记录。

## 组装规则

规则：

* 组装单应记录组件商品、组件 PN、组件 SN、组件门店、组件库位和组件单台成本。
* 组装执行后，组件商品库存减少，组装成品库存增加。
* 组件商品如有 SN，原 SN 不得删除，应变更为已组装或已转换状态。
* 组装成品如需要 SN，应录入或生成新的 SN。
* 组装成品 SN 应能追溯到所有组件 SN、组件 PN、组件采购入库记录和组装单。
* 组装成品成本默认带出目标商品成本价，允许用户手工修改。
* 组装服务费计入组装成品库存成本。
* 组装成品成本原则上应等于组件单台成本合计加组装服务成本。
* 组装单允许作废或反向冲销。
* 作废或反向冲销不得删除原记录，应生成反向库存和 SN 状态变更记录。

## 销售与毛利规则

规则：

* 拆分商品销售时，按拆分后商品的单台成本计算销售毛利。
* 组装成品销售时，按组装成品的单台成本计算销售毛利。
* 销售端不需要展示完整拆分/组装成本链路。
* 财务、采购、库房和授权角色应能查看拆分/组装来源追溯。
* 已销售的拆分商品或组装成品，不允许直接作废原拆分单或组装单，应通过授权冲销或调整流程处理。

## 采购申请规则

* 新建商品申请（包括二手商品）填写 PN 后，必须先精确校验有效 PN 主数据；PN 已存在时不得重复发起新建商品申请。

* 采购申请商品明细必须填写商品名称、采购单价和数量。
* 采购申请列表、详情和导出中的申请金额 = 当前采购金额 - 当前抵扣金额；采购退单或数量调整后，按当前有效金额重新计算，原始采购金额和抵扣金额保留用于追溯。
* 采购申请审批通过后，展示状态为“待支付”；关联供应商应付款或采购垫付报销已全部支付时，展示状态为“已支付”。数据库内部采购审批状态仍保留 `approved`，支付状态由关联应付款/报销记录决定。
* 二手商品勾选“审批完成及入库”时，必须同时填写有效 PN、唯一 SN，且数量固定为 1；PN 必须保存到采购明细，并用于审批创建商品和直接入库校验。
* 保存草稿生成采购申请记录，但草稿不进入审批流程、不生成采购入库单或其他后续业务单据。
* 草稿允许编辑和重复保存。
* 采购申请草稿必须在采购申请列表展示；创建人、店长、管理员或 BOSS 可以编辑、重复保存、提交和删除草稿。
* 删除采购申请草稿使用 `status=deleted` 标记并保留原记录，不物理删除申请及其明细；正常列表不展示已删除草稿。
* 采购申请只有从未提交过的草稿可以删除；已提交后不得删除。
* 草稿创建人、店长、管理员或 BOSS 可以提交草稿。
* 草稿提交后进入现有采购申请待审批状态，后续审批逻辑保持不变。
* 采购申请列表支持按提交人、商品名称/PN/商品编码、供应商和状态查询。
* 新采购申请必须完成门店和库位两级分配；库位只允许选择对应门店的启用库位。
* 每个门店的库位分配数量必须等于该门店分配数量，全部门店分配数量必须等于采购数量。
* 新增门店分配时默认使用该门店的销售仓。
* 采购审批通过生成入库单时，必须将采购申请中的门店和库位分配带入入库明细。
* 历史没有库位分配的采购申请继续兼容原有入库流程，不强制迁移历史数据。

## 采购退库与负向应付款规则

* 采购退库继续采用“审批通过”和“执行退库”两步流程；审批通过只改变审批状态，不提前扣库存或生成财务事实。
* 退库执行成功后，库存扣减、原入库明细状态、采购调整单和负向应付款必须在同一事务内完成；任一步失败则整体回滚。
* 退库执行必须生成采购调整单，调整单号沿用退库单号，关联原采购申请、原采购明细和原入库明细；原采购申请保留原始数量/金额，同时返回当前有效数量、原价、抵扣金额和实际金额。
* 退库产生的应付款使用 `source_type=purchase_return`，金额为负数，关联原采购申请和供应商；它代表供应商应退/待抵扣金额，不代表实际支付一笔负数付款。
* 原采购应付款尚未结算且尚未付款时，退库负向应付款优先直接冲减原采购应付款；已经生成结算事实或已经付款的，原付款事实保持不变，负向应付款转为供应商信用余额。
* 供应商信用余额只能在同一供应商、同一经销商范围内抵扣后续正向供应商应付款；不得把供应商退库信用直接冲减个人垫付报销或无关费用。
* 个人垫付采购已付款后仍不允许通过采购退库改写已付款报销事实；如需处理，必须走单独的财务退款/追偿流程【待确认】。
* 退库记账必须幂等：重复执行或历史修复不得重复生成采购调整和负向应付款，也不得改写已有结算事实。
* 已执行但缺少采购调整或负向应付款的历史退库单，允许按退库单号执行事务性修复；无法唯一匹配原采购明细的记录进入人工核对，不自动猜测。

## 销售、采购、调拨流程审计规则

* 销售订单列表和详情必须保留并展示提交人、提交时间、审批人、审批时间及审批意见。
* 采购申请列表和详情必须保留并展示申请人/提交人、提交时间、审批人、审批时间及审批意见。
* 调拨必须展示申请人、调出确认人、调入确认人及各自操作时间；调拨出入库确认不等同于审批。
* 销售、采购、调拨的创建、草稿保存、提交、审批、拒绝、撤销、删除和调拨出入库确认必须追加不可变业务操作日志。
* 操作日志必须保存业务类型、业务单据ID、业务单号、动作、前后状态、操作人、操作时间、意见和必要的操作明细。
* 审批拒绝后重新提交不得覆盖原日志；历史数据缺失新增字段时，页面显示为空，不得阻断历史单据查询。
## 国补 POS 客户实收到账金额

* 国补 POS 的首部分使用“国补POS（电脑）-客户实收”记录。
* 国补金额取国补 POS 两部分金额合计；补贴金额取对应国补应收单的应收金额；国补补贴金额取订单 `national_subsidy`。
* 首部分到账金额公式为：`国补金额 - 补贴金额 - (国补金额 - 国补补贴金额) × 0.006`。
* 该公式仅调整首部分日结到账金额，订单原始收款记录和国补应收记录保留原始金额。
## PN与SN关联规则

* `T_PRODUCT_PN` 是商品 PN 主数据表，新增或恢复 PN 必须通过统一 PN 服务处理。
* 不新增 PN 关系表；非 SN 商品允许一个商品编码对应多个有效 PN，SN 商品继续只允许一个有效 PN。
* 一个有效 PN 只能绑定一个商品编码；发现同一 PN 在历史 SN 中出现在多个商品下时，保留历史记录并进入人工复核，不自动合并商品。
* SN 商品只能绑定一个启用 PN；不同 PN 的入库或库存转换必须被拒绝。
* SN 新建必须同时保存 `pn_id` 与 `pn_code`；`pn_code` 用于历史快照和人工识别，`pn_id` 用于主数据关联。
* 历史单据中的 PN 文本属于业务快照，不因主数据清理而批量改写。
* 历史 SN 只有在同商品下能唯一匹配启用 PN 时才允许自动补齐 `pn_id`；无法唯一匹配必须进入异常清单。
* 商品列表、搜索和库存入口优先读取 `T_PRODUCT_PN`，旧商品字段仅作为历史兼容兜底。

## 销售订单、定金订单与导出分摊

* 销售订单列表和销售订单导出同时包含正式销售订单与定金收款订单，并通过业务类型区分。
* 定金收款订单的实际收款方式和金额进入对应收款方式列；正式订单的定金抵扣只进入“定金抵扣”列，不重复计入现金收入。
* 财务日结明细中的定金收款使用 `deposit_receipt` 业务类型，定金抵扣不新增日结现金明细。
* 导出保持现有 58 列结构；国补金额只填写电脑、手机、平板等主商品行，配件行的国补、国补状态、国补人和国补人 ID 字段为空。
* 一单包含多个主商品时，国补按主商品小计占比进行金额分摊，分摊后的金额合计必须等于订单国补金额，尾差归入最后一个主商品行。
* 订单包含多个商品行时，各实际收款方式按商品小计占比进行分摊，分摊后的各收款方式合计必须等于订单收款记录金额。
* 商品主次识别沿用商品分类、配件类别及商品名称的现有分类口径；无法识别且订单只有一条商品明细时，该行作为主商品兼容处理。

## 销售退货单与导出负向明细

* 原销售订单和原始收款流水保持不变；销售退货审批完成后形成关联的独立销售退货单。
* 销售退货单在销售订单导出和经营报表中以负向明细体现：退货数量为负数，用户应收、客户实收、政策补贴应收、教育补贴及商品金额按会计方向保存为负数。
* 退货单号作为负向明细的订单编号，原销售订单号保留在备注或关联字段中，确保退货单与原销售订单可追溯。
* 经营看板、销售报表和毛利统计的正向订单只统计 `已归档`、`completed`、`archived`；整单退货的原订单状态 `returned` 仅用于追溯，不作为正向销售额、成交单量或正向毛利重复统计。
* 部分退货仍保留原订单正向事实，并通过关联退单负向结算及负向毛利台账冲减；整单退货原订单与对应退单负向事实的净额为零。

## 销售辅助人员金额分配导出

* “辅助销售人比例分配”展示销售人、辅助销售人1、辅助销售人2的参与顺序。
* “辅助销售人金额分配”只读取订单中保存的实际金额分配明细。
* 没有金额分配明细时显示 0；不得按订单总额平均推算金额。

## 采购申请查询与返利抵扣草稿

* 采购申请列表和采购申请导出支持按申请单号模糊查询。
* 编辑采购申请草稿时重新加载供应商当前返利余额，但不得覆盖草稿中已保存的返利抵扣金额。
* 草稿提交时按当前供应商返利余额校验抵扣金额，余额不足不得提交。

## 入库人员记录

* 入库执行人员必须写入每一条入库商品明细，允许同一入库单不同商品由不同人员执行。
* 入库单表头继续保留单据级入库人员，作为汇总和历史数据兼容字段；详情页同时展示单据级和商品级人员。

## 2026-08-19 库存查询与店长经营报表范围

* 所有已登录账号可以只读查询本经销商下全部有效门店的商品库存，包括库存汇总、SN库存清单、SN查询和库存导出。
* BOSS 的库存只读范围继续为系统全部有效门店。
* 库存查询范围的扩大不改变入库、出库、调拨、库存调整、资源权益维护、SN库位调整等写入权限；写入接口继续按账号原有门店授权校验。
* `manager` 和 `store_manager` 店长角色可以查询本经销商下全部有效门店的经营报表，包括经营数据看板、销售报表、库存报表和员工业绩统计。
* BOSS 的经营报表范围继续为系统全部有效门店；其他非店长角色继续沿用原有经营报表门店范围。
* 店长跨门店查看经营结果只扩大门店数据范围，不新增财务账户、应付、付款等财务数据权限。
* 门店筛选项和请求参数不构成安全边界；库存和经营报表接口必须在服务端按上述规则校验。

## 2026-08-21 经营报表店长范围回退与小程序财务账号范围

* `manager`、`store_manager`、`store_admin` 和店员角色的经营报表只能查看账号已配置的授权门店；店长不再自动获得经销商全部门店的经营结果。
* BOSS 的经营报表继续查看系统全部有效门店；所有人员查询本经销商全部有效门店库存的只读规则保持不变。
* 小程序财务管理仅对经销商级账号开放，允许 `distributor`、`admin`、`finance`、`purchaser`、`business`、`cashier`、`boss` 等非门店角色访问。
* 店长和店员角色（`clerk`、`staff`、`manager`、`store_manager`、`store_admin`）不得看到小程序财务管理入口，不得通过直接打开页面进入财务管理。
* `/report/finance-overview` 必须在服务端校验经销商级账号；小程序入口隐藏和页面拦截仅用于交互控制，不能替代接口权限。

## 月度任务与业务达成规则

### 任务设置

规则：

* 月度任务按自然月设置，月份格式为 `YYYY-MM`。
* 任务对象包括门店和员工；门店目标与员工目标独立配置，门店目标不要求先存在员工目标。
* 首期指标包括销售额、毛利和指定商品销量批次。
* 指定商品销量支持多个商品组合为一个批次，并支持同一月份配置多个批次；批次内每个商品保存明确目标数量。
* 同一月份、同一对象、同一指标只能存在一条有效任务；当月允许修改，历史月份只读。
* 任务配置、修改、停用和毛利分摊必须保留操作人、操作时间及变更明细。

### 毛利分摊

规则：

* 门店毛利目标可以在后台手工分摊到员工。
* 员工业务达成中的毛利目标读取门店毛利分摊记录；门店与员工销售额、指定商品销量目标仍分别读取各自任务配置。
* 门店毛利分摊金额不得为负数，分摊总额不得超过门店毛利目标；未分摊金额必须在后台和报表中可见。
* 毛利分摊只影响任务达成责任归属，不修改订单经营毛利、财务利润或员工业绩原始事实。

### 达成计算

规则：

* 任务达成只统计已归档且未删除的销售订单。
* 已完成销售退货按负向业务事实冲减门店销售额、门店毛利和指定商品销量。
* 门店销售额和毛利按门店汇总；员工销售额和毛利沿用主销售、辅助销售平均分摊规则。
* 指定商品销量按订单商品数量汇总；同一批次内每个商品分别与其目标数量比较，批次完成率取批次内商品完成率的最低值，避免单个商品超额掩盖其他商品未完成。
* 完成率由 ANY-ERP 后端统一计算，前端只展示结果；目标为零时完成率返回空值，不显示误导性百分比。
* 任务查询按账号授权门店范围过滤，门店和员工筛选参数不构成安全边界。
* 销售归档、退货完成或毛利调整后，下一次任务报表查询应实时反映最新数据。

### 展示

规则：

* 网页端销售管理提供月度任务配置入口，报表管理纳入门店及员工任务达成查询。
* 小程序不新增独立任务页面，在员工销售内容后增加“业务达成”区域。
* 业务达成同时展示门店和员工的销售额、毛利、指定商品批次完成情况及总体完成率。
* 门店和员工达成列表按总体完成率从高到低排序；完成率相同时按销售额从高到低排序。
* 总体完成率为已配置的销售额、毛利和商品批次完成率的算术平均；未配置的指标不进入平均值。

## 商品分类与商品主数据维度

* 商品分类树最多四级；新建商品可选择任意一级、二级、三级或四级分类，也可以不选择分类并手工填写商品维度。
* 分类层级映射为：一级商品分类、二级品牌、三级系列、四级型号。
* 商品主数据和新建商品申请分别保存商品分类、品牌、系列、型号；完整分类路径只作为历史兼容字段，不用于正常展示和报表分组。
* 选择分类后，系统自动带入已存在的层级字段，用户可以修改已带入内容并补充剩余字段。
* 商品名称默认按品牌、系列、型号和分类字段管理中配置的补充字段组合；用户填写商品名称时可作为名称兜底。
* 报表、商品导出、库存统计和销售统计按商品分类、品牌、系列、型号独立维度处理；历史路径数据迁移期间保留兼容查询。
* 商品字段管理仍以一级分类作为默认配置，下级分类仅在需要差异时保存覆盖配置。
## 2026-08-28 商品分类与新建商品规则

* 商品分类树最多四级；同一父级下启用分类名称不得重复。
* 新建商品、商品申请审批通过和批量导入新商品必须选择第四级且没有子分类的最后一级分类。
* 商品字段在一级分类配置默认值；二级、三级、四级分类未配置自己的字段时继承所属一级分类，需要差异时可保存本级覆盖配置。
* 商品分类在商品和商品申请数据中保存为完整路径字符串，格式为 `一级/二级/三级/四级`；历史三级商品数据保留原值并可继续查询。
* Web 和微信小程序均通过 ANY-ERP 分类接口读取分类树和末级字段配置，前端限制不能替代服务端校验。
