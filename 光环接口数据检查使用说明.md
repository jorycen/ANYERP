# 光环接口数据检查使用说明

## 📋 概述

本文档说明如何检查上传到光环接口的数据内容。

---

## 🛠 方法一：使用调试云函数（推荐）

### 1. 部署云函数

在微信开发者工具中：
1. 右键点击 `cloudfunctions/testGuanghuanApi` 文件夹
2. 选择「上传并部署：云端安装依赖」
3. 等待部署完成

### 2. 调用云函数

#### 方式 A：预览上传数据（不上传）
用于查看订单会生成什么样的上传内容，但不会实际上传到光环接口。

```javascript
wx.cloud.callFunction({
  name: 'testGuanghuanApi',
  data: {
    action: 'preview',
    orderNo: '你的订单号'
  }
}).then(res => {
  console.log('预览结果:', res.result);
  // res.result.guanghuanRequestData - 业务数据
  // res.result.fullRequestBody - 完整请求体
}).catch(err => {
  console.error('调用失败:', err);
});
```

#### 方式 B：实际上传测试
用于测试完整的上传流程，会实际上传到光环接口。

```javascript
wx.cloud.callFunction({
  name: 'testGuanghuanApi',
  data: {
    action: 'upload',
    orderNo: '你的订单号'
  }
}).then(res => {
  console.log('上传结果:', res.result);
}).catch(err => {
  console.error('上传失败:', err);
});
```

### 3. 在微信开发者工具中测试

1. 打开云开发控制台
2. 选择「云函数」→「testGuanghuanApi」
3. 点击「云端测试」
4. 输入测试参数：

**预览测试：**
```json
{
  "action": "preview",
  "orderNo": "你的订单号"
}
```

**上传测试：**
```json
{
  "action": "upload",
  "orderNo": "你的订单号"
}
```

---

## 📊 上传数据结构说明

上传到光环接口的数据包含以下部分：

### 1. 公共参数 (HRT_ATTRS)
```javascript
{
  "App_Sub_ID": "100001KKM",
  "App_Token": "c7dnmd0sk3d579c30f94",
  "Api_ID": "kukumao.orderCollect",
  "Api_Version": "1.0.0",
  "Time_Stamp": "2024-01-01 12:00:00:000",
  "Sign_Method": "md5",
  "Sign": "签名值",
  "Format": "json",
  "Partner_ID": "100001",
  "Sys_ID": "69b911cfb1816aaa6c8d8ab4",
  "App_Pub_ID": "10001",
  "REQUEST_DATA": "业务数据JSON字符串"
}
```

### 2. 业务数据 (REQUEST_DATA)
```javascript
{
  "orderId": "订单号",
  "mall": "GHL419",
  "store": "GHL419",
  "tillId": "01",
  "cashierId": "创建者",
  "checkCode": "P88888888",
  "type": "SALE",
  "source": "01",
  "time": "20240101120000",
  "totalAmt": 1000,
  "mobile": "联系电话",
  "comments": "订单号",
  "itemList": [
    {
      "itemCode": "商品PN码",
      "price": 1000,
      "quantity": 1
    }
  ],
  "payList": [
    {
      "paymentMethod": "CH",
      "payAmt": 1000,
      "value": 1000,
      "discountAmt": 0,
      "time": "20240101120000",
      "cardBank": "",
      "cardNumber": ""
    }
  ],
  "refOrderId": ""
}
```

---

## 🔍 方法二：查看云函数日志

在 `cloudfunctions/queryOrders/index.js` 中，上报订单时会输出日志：

```javascript
console.log('光环API请求地址:', GUANGHUAN_CONFIG.Base_URL);
console.log('光环API请求数据:', JSON.stringify(requestBody, null, 2));
console.log('光环API响应:', JSON.stringify(response.data, null, 2));
```

查看日志步骤：
1. 打开微信开发者工具
2. 进入「云开发」控制台
3. 选择「云函数」→「queryOrders」
4. 点击「日志」标签
5. 查看相关的日志记录

---

## 📝 支付方式映射

| 本地支付方式 | 光环接口代码 |
|-------------|------------|
| 现金 | CH |
| 支付宝 | AP |
| 微信 | WP |
| 银行卡 | CI |
| 其他 | OT |

---

## ⚠️ 注意事项

1. **仅重庆光环购物公园店的订单**会上传到光环接口
2. 订单必须是「已归档」状态才能上报
3. 只有店长权限可以执行上报操作
4. 已上报过的订单不能重复上报

---

## 📞 相关文件

- 接口文档：`API/guanghuan/接口文档.txt`
- 配置参数：`API/guanghuan/LENOVO参数和接口地址.txt`
- 云函数代码：`cloudfunctions/queryOrders/index.js`
- 调试云函数：`cloudfunctions/testGuanghuanApi/index.js`
