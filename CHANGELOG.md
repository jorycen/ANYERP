# CHANGELOG

## 2026-08-31 - 修复商品管理分类树运行异常

* 修正四级分类模板的作用域嵌套，避免在三级分类循环外访问 `level3.children` 导致页面报 `Cannot read properties of undefined (reading 'children')`。

## 2026-08-31 - 修复采购人员筛选与前端缓存

* 提交人筛选同时匹配提交人、申请人和制单人字段，兼容按员工姓名或员工ID关联的历史数据。
* 经手人筛选同时匹配经手人ID和姓名快照，并兼容旧采购单的申请人/制单人ID回退。
* 新建采购申请和草稿提交补齐经手人及制单人快照；静态首页禁止缓存，避免发布后继续加载旧筛选页面。

## 2026-08-31 - 采购管理增加生命周期状态筛选

* 采购申请筛选新增“待入库”“已撤销”“已退单”。
* 待入库按关联入库单存在待入库记录判断；已退单按关联入库单全部关闭且至少有一笔退库判断。
* 采购申请列表和导出复用同一套服务端生命周期状态口径。

## 2026-08-31 - 统一 Web 与小程序新建商品

* Web 和微信小程序新建商品改为一级至四级逐级筛选，允许选择任意层级或不选分类后手工填写。
* 选择分类后按 ANY-ERP 字段管理返回的继承字段填写补充信息，商品名称默认按“二级-三级-四级-其他字段”用短横线组合且可手工修改。
* 后端统一保存完整分类路径并在提交时兜底生成默认名称，避免两端规则不一致。

## 2026-08-31 - 统一收敛审批列表数据权限

* 审批中心的销售、采购、销售退单、退库、报销、商品、资源套回和批量库存审批列表统一按当前账号的审批角色及门店/经销商范围查询，非审批账号不再看到待审批记录。
* 通用审批的 admin、财务及其他非 BOSS 账号改为沿用授权门店范围，BOSS 保持全局范围，避免列表可见但详情或操作时报无权。
* 退库审批、报销审批、商品审批和资源套回审批补充服务端的角色、门店或经销商二次校验。
* 销售订单跨门店切换只对经销商级账号放行到销售控制器校验，店长/店员仍由通用门店中间件拦截未授权主门店。
* 增加跨模块审批列表的越权不可见回归测试。

## 2026-08-31 - 修复店长订单列表越权展示

* 销售订单列表和导出按纯门店角色已配置的管理门店过滤，非管理门店订单不再出现在列表中。
* 修正订单列表与详情权限范围不一致导致的“列表可见、打开后无权查看”问题；经销商级账号和 BOSS 范围保持不变。
* 增加店长订单列表门店范围及未授权门店筛选的回归测试。

## 2026-08-31 - 修复销售订单导出门店范围变量回归

* 修复销售订单导出仍引用已废弃的 `accessibleStoreIds` 导致点击导出直接返回 HTTP 500 的问题。
* 订单、定金和退单导出统一复用订单专用门店范围 `orderStoreIds`，保持多门店授权过滤一致。
* 增加销售订单导出控制器回归测试，覆盖订单、定金和退单三类数据的门店范围。

## 2026-08-31 - 分离店长审批权限与库存只读范围

* 修正回归：店长库存汇总、SN库存清单、SN查询和库存导出按所属经销商展开全部有效门店，不再被当前主门店或审批授权门店截断。
* 店长报表和通用审批继续只允许查看、处理账号已配置的管理门店；库存写入、入库、出库、调拨和库位调整权限不放开。
* 新增 `/store/inventory-readable` 作为库存门店下拉接口，`/store/readable` 保持报表门店范围，避免报表下拉显示越权门店。

## 2026-08-30 - 同步 Web 生产静态资源

* 将最新 `frontend/web` 构建结果同步到后端实际提供静态页面的 `backend/public` 和发布副本 `frontend/dist-web`，避免部署后继续加载旧版前端包。

## 2026-08-30 - 修复重点商品勾选后的页面异常

* 修复商品管理提交函数引用未声明变量 `isEditing` 导致的运行时错误。

## 2026-08-30 - 收敛店长库存与审批权限

* 店长类账号的库存、SN 查询和审批记录仅显示已配置管理门店。
* 审批详情、提交、处理和重新提交同步校验门店权限，避免通过接口访问其他门店记录。
* BOSS 和经销商级账号维持原有门店范围。

## 2026-08-30 - 修复已绑定 SN 的调拨出库误报

* 调拨申请创建时已绑定 SN 的明细，只要数量仍大于 0，出库确认继续视为待出库商品。
* 出库确认自动沿用原 SN，并重新校验其仍属于调出门店且状态为 `in_stock`，避免因明细已绑定 SN 被误报为“无待出库商品”。

## 2026-08-30 - 销售订单支持多店长同权

* 店长按 `manager`、`store_manager`、`store_admin` 角色和已勾选门店权限查看全部授权门店订单，不再依赖单一门店正式店长字段。
* 订单详情、导出、草稿处理、订单维护、销售订单审批和销售退单审批统一使用店长门店范围。
* 通用审批中心的“门店店长”改为解析主题门店内全部已授权店长，支持多名店长共同审批。

## 2026-08-30 - 修正国补 OMO 手机平板到账计算

* 手机/平板 OMO 原价5999元时，国补优惠按500元封顶，优惠/手续费187元直接从首次到账扣除，首次到账为 `5999 - 500 - 187 = 5312` 元，后续政策补贴应收为500元。
* OMO电脑沿用同一口径，政策补贴按15%计算并按1500元封顶；小程序金额汇总与收款拆分同步修正。

## 2026-08-30 - 新建订单支持按经销商权限切换门店

* 新增订单门店选项接口，按账号配置的一个或多个经销商返回全部有效门店。
* 销售订单创建、草稿和编辑支持订单专用经销商门店范围。
* 门店角色的订单列表/详情按创建人本人和门店资料正式店长控制可见范围。

## 2026-08-30 - 修复销售商品运费跨门店匹配

* 调拨运费仅按调拨目的门店匹配销售订单，采购运费仅按采购归属门店匹配。
* 商品运费匹配排除订单创建时间之后的运费记录，避免未来运费倒灌到历史订单。
* 重新计算毛利时清理订单中已失效的旧商品运费，避免错误运费残留。
* 增加历史订单运费差异只读预览脚本，先输出原值、新规则值和差额，不直接回写数据。

## 2026-08-30 - 新增国补资格核销冲销流程

* Web 库存资源权益增加国补资格“冲销核销”入口，支持恢复已退单且已回库 SN 的国补资格。
* 后端新增专用冲销接口，校验原销售核销流水、退单状态、SN库位状态、重复冲销和下账状态。
* 冲销保留原核销记录并追加 `USED → AVAILABLE` 审计流水；待下账记录随事务取消，已结算记录要求先冲销下账。

## 2026-08-30 - 月度任务达成接口接入与部署兼容

* 月度任务达成接口补充门店/员工汇总结果和计算来源说明，前端无需复制目标汇总规则。
* 月度任务达成接口按经营报表权限返回毛利字段，避免无毛利权限账号读取目标和实际毛利。
* 报表达成、月度任务查询及月度任务选项加入只读门店访问白名单，兼容无直接门店列表的经销商级账号。
* Web 端 404 提示补充接口路径和 `backend` 部署目录信息，便于定位线上后端版本未更新问题。

## 2026-08-30 - 统一小程序订单备注与金额补录链路

* 小程序订单备注、金额补录和提货运费统一通过 ANY-ERP 订单 API 保存，避免继续写入已停用的云数据库订单路径。
* 小程序订单详情和列表统一展示 API 返回的备注、补录明细、补录合计及运费金额；删除补录同步使用服务端接口。
* 金额补录变更写入业务审计日志，Web 导出继续输出教育优惠、提货运费、追加商品、补录明细和备注字段。
* Web 订单 Excel 新增“商品采购运费”列，按商品明细导出 `T_ORDER_ITEM.freight_cost`，与订单级手工提货运费分开统计。
* 订单 Excel 补齐门店、客户来源、收款明细、商品及辅助销售人等字段，兼容下划线、驼峰和关联对象数据。

## 2026-08-30 - 审批查看补全发起信息

* 审批中心“查看”补充展示发起人、门店、提交时间、申请字段、业务明细及完整发起数据。
* 采购、报销、商品申请、销售退单和毛利调整审批查看时加载对应业务详情，便于审批人核对原始申请内容。

## 2026-08-30 - 修正已退单原订单经营统计口径

* 经营看板、销售报表和员工业绩正向订单统计排除 `returned`，避免整单退货原订单继续贡献正向销售额、成交单量和毛利。
* 部分退货保留已归档原订单，并由退单负向毛利台账冲减员工业绩销售额和毛利。
* 经营看板毛利快照公式统一使用当前 `ORDER_GP_V8_20260810_FREIGHT` 版本。

## 2026-08-29 - 销售订单补录信息展示与导出

* Web 销售订单详情展示补录项目、金额方向、内容、券码、补录人、时间和凭证。
* 重新构建并同步 Web 生产静态资源，销售订单导出继续输出“补录信息”字段及补录内容。

## 2026-08-29 - 销售订单查询按时间排序

* 订单查询列表改为仅按订单创建时间倒序排列，取消待处理状态优先和订单 ID 兜底排序。

## 2026-08-29 - 修复采购SN入库完成判断

* 采购入库完成校验同时统计历史已入库数量/SN与本次请求数量/SN，避免一次性录入全部 SN 时被误报为数量不足。
* 增加历史分批入库与本次补齐 SN 的回归测试。

## 2026-08-29 - 商品名称默认组成优化

* 商品名称默认先拼接第2、3、4级分类，再按有效字段配置顺序追加字段值。
* Web、微信小程序、商品申请、批量导入和库存转商品统一使用新的命名顺序。

## 2026-08-29 - 放开经销商总权限负毛利第一节点审批

* `admin`、`boss`、`distributor` 账号可处理负毛利订单第一节点，不再提示必须使用店长账号。
* 审批仍按第一节点、第二节点串行推进，第二次通过后才归档。

## 2026-08-29 - 新建商品申请增加PN重复校验

* 新增精确 PN 可用性查询接口 `/product/pn-availability`。
* 新建商品申请提交接口统一校验有效 PN，已存在的 PN 不允许重复发起新建申请。

## 2026-08-29 - 销售退单缘由纳入审批

* 小程序所有销售退单入口要求填写退单缘由，取消默认退单文案。
* ANY-ERP 服务端拒绝空退单缘由，并将有效缘由保存到退单申请。
* Web 与微信小程序审批详情展示退单缘由，审批意见继续独立保存。

## 2026-08-29 - 负毛利销售订单改为两级审批

* 负毛利订单归档前先进入店长初审，店长通过后再进入经销商总权限复审。
* 后端按当前审批节点限制角色，复审通过后才执行库存、定金、资源权益、结算和归档事务。
* Web 与微信小程序审批中心展示“待店长审批”和“待经销商总权限审批”，并兼容历史 `pending_approval` 记录。

## 2026-08-29 - 库存查询分门店销售库存与SN库龄排序

* 库存查询商品结果增加分门店可销售数量展示，数据仅取销售库可销售库存。
* SN查询结果按库龄倒序排列，库龄最长的SN优先展示。

## 2026-08-29 - SN库存库龄与小程序库存分类展示

* 库存中心SN库龄改为按公司首次采购入库时间计算，调拨到店不再重置库龄，并按库龄最长优先展示。
* SN查询补充状态时间、状态持续天数及采购入库时间字段。
* 微信小程序库存查询改为仅展示非零的销售库、样机库、铺货仓、租赁样机和不可售数量。

## 2026-08-29 - 商品字段一级默认与下级覆盖

* 商品字段管理支持在一级分类配置默认字段，二至四级分类自动继承。
* 下级分类支持保存本级字段覆盖，并支持恢复上级默认字段。
* 商品新建、采购申请、库存转商品和商品导入统一使用 API 解析后的有效字段；Web 与微信小程序保持一致。

## 2026-08-29 - 新增国补 OMO 收款方式

* 新增“国补OMO（电脑）”和“国补OMO（手机平板）”，分别适用电脑1500元、手机/平板500元国补封顶规则。
* OMO收款拆分为客户实收和政策补贴应收：立即到账为 `min(产品原价 × 0.85, 封顶金额) - 优惠/手续费`，政策补贴应收为 `min(产品原价 × 0.15, 封顶金额 × 0.15)`。
* ANY-ERP 日结、国补应收、财务账户和销售导出支持 OMO；微信小程序同步支持展示、录入、拆分和历史订单查看。

## 2026-08-29 - 清理路径型一级商品分类

* 将历史上误保存为一级名称的 `XX/XX` 分类恢复为真实分类树，避免路径型分类显示为一级。
* `电子产品/笔记本` 下的鼠标垫归入“选件”，二手七彩虹 P15 归入“二手优品”。
* Legion 27Q-10 显示器归入“选件”，并停用无商品使用的异常路径型一级节点。

## 2026-08-29 - 联想商品分类树迁移

* 笔记本、平板、手机下统一增加或复用“联想”二级分类。
* 原启用二级分类调整为联想下的三级分类，原启用三级分类调整为四级分类。
* 合并平板下已有的联想拯救者重复节点，保留既有四级分类字段配置；商品路径仅执行可由旧完整路径确定的前缀迁移。

## 2026-08-28 - 系统设置商品字段管理支持分类维护

* “商品字段管理”左侧分类树增加新增一级、添加子级、编辑和删除分类入口。
* 分类维护完成后可直接选择第四级分类，在右侧配置对应的商品字段。

## 2026-08-28 - 应付结算增加经销商筛选与展示

* 应付管理增加经销商筛选条件和经销商列，支持按经销商区分同一供应商的应付款。
* 结算选择弹窗同步展示经销商，并在前端提前提示不能混合多个经销商生成结算单。
* 应付列表接口和导出支持经销商筛选，并返回经销商名称。

## 2026-08-28 - 新建商品启用四级末级分类和字段配置

* 商品分类管理扩展为最多四级，商品字段管理只允许配置第四级最后一级分类。
* 新建商品、审批通过和新建商品导入统一校验第四级末级分类；商品分类继续以 `一级/二级/三级/四级` 路径字符串保存，不改数据库结构。
* Web 和微信小程序同步展示四级分类及末级字段配置，审批编辑入口同步限制分类选择。

## 2026-08-28 - 新建商品遇到已存在 PN 时提示直接入库

* 新建商品或采购申请中新建二手商品提交时，若 PN 已绑定有效商品，统一提示“PN 已存在，无需新建商品，请直接入库”。
* 仅增加提交前校验和提示，不自动复用商品、不自动执行入库，保留 PN 唯一绑定规则。

## 2026-08-27 - 入库单和退库申请支持按单号查找

* 入库单管理增加入库单号输入和“按单号查找”按钮，支持回车查询。
* 退库申请增加退库单号输入和“按单号查找”按钮，支持回车查询。
* 后端接口按单号模糊匹配并保留原有状态、门店和分页筛选。

## 2026-08-27 - 完善电脑端销售订单作废、退单与备注

* 未归档销售订单增加“作废”入口，已归档销售订单增加“退单”入口；后端同步限制仅未归档订单可以作废。
* 销售订单列表增加备注列，订单详情支持修改并保存备注；作废原因会追加记录到订单备注。

## 2026-08-27 - 支持手工返利账户与批量勾稽核销

* 结算账号新增/编辑支持“返利账户”，必须绑定供应商；新增时可录入返利金额，金额同步进入返利池并生成返利收款待下账单及对应返利上账单来源。
* 返利下账页面统一展示返利收款、手工新增和自动生成记录，支持选择同一供应商的多笔待下账单并批量勾稽返利上账金额。
* 后端新增批量返利核销事务接口，校验供应商、状态和可核销余额，防止超额或跨供应商核销。

## 2026-08-27 - 修复采购退库未回写采购和应付款

* 退库执行后在同一事务内生成负向采购调整单，关联原采购申请、采购明细和入库明细，采购申请详情同步展示当前数量和金额。
* 退库执行后生成 `purchase_return` 负向应付款；未结算原采购应付款直接冲减，已付款采购保留原付款事实并形成供应商后续应付款信用余额。
* 新增按退库单号幂等修复历史记账的脚本，并修复 `RTN20260826043915938`。

## 2026-08-27 - 修复批量 SN 出库库存投影误报

* 批量 SN 入库和出库执行前后按 SN 实际状态、门店和库位重建库存投影，避免 `T_INVENTORY` 聚合数量过期时误报库存不足。
* 非 SN 商品继续使用原有库存扣减和禁止负库存校验。

## 2026-08-27 - 销售订单列表直接返回毛利

* 销售订单列表统一关联订单毛利快照，并返回订单级毛利金额、快照状态和计算时间。
* 小程序订单列表可直接展示已有毛利，不再为每条销售/定金记录逐一请求毛利接口。

## 2026-08-27 - 分离定金管理与销售订单查询

* 销售订单查询不再混入定金收款记录，定金继续通过定金管理单独查询。
* 定金管理调整为当天优先、未归档优先、时间倒序；销售订单导出和财务日结导出继续保留定金收款金额，日结 Excel 明确标记“定金收款”。

## 2026-08-27 - 修复销售订单导出补录金额为0

* 销售订单导出兼容补录表的下划线字段、接口常用驼峰字段及订单毛利快照中的补录明细，教育优惠和商品提货运费不再因字段来源不同而显示为 0。
* 保持现有 58 列导出结构和补录金额增减方向不变。

## 2026-08-26 - 修复月度任务接口404导致页面运行异常

* 月度任务页面捕获首次加载选项接口的 404，改为显示明确的后端部署提示，不再触发全局“页面运行异常”。
* 月度任务接口仍由 `/sales/monthly-tasks/options` 提供；若继续返回 404，需使用包含月度任务路由的后端镜像重新部署。

## 2026-08-26 - 修复小程序补录数据未同步 Web 与导出

* 小程序金额补录改为调用统一补录接口，写入 `T_ORDER_SUPPLEMENT`，补录金额、方向、内容和凭证可被 Web、小程序及 Excel 导出读取。
* 补录删除及兼容调用同步走统一补录接口；订单备注继续通过订单 API 更新。
* 历史云数据库中的嵌套 `orders.supplements` 尚未自动迁移，需单独执行数据迁移【待确认】。

## 2026-08-26 - 统一修复小程序弹窗列表滚动

* 修复订单详情退单商品选择、部分退单弹窗滚动手势被外层拦截，改为明确高度的 `scroll-view`，避免页面背景跟随滚动。
* 同步修复订单创建商品/门店/预览、采购供应商与商品选择、调拨商品、库存订单与 SN、审批详情、门店选择、失败详情、图片链接、订单毛利与导出任务等同类弹窗。
* 将订单列表和商城销售查询的资源/金额补录弹窗内容改为标准纵向滚动容器，统一弹窗滚动交互。

## 2026-08-26 - 修复小程序销售审批中心待审批订单同步

* 小程序销售审批加载兼容分页接口返回结构，避免接口包装变化时静默丢失销售审批单。
* 将销售待审批首批加载数量与 Web 审批中心统一为最多 100 条，保持后端审批权限口径不变。

## 2026-08-25 - 修复业务商品搜索展示停用商品

* 销售新建/编辑订单、采购申请、库存查询、调拨及订单归档相关商品搜索统一传递启用筛选。
* 商品列表和 PN 列表接口在启用筛选下同时校验商品 `status=1`、`is_deleted=0`；前端合并搜索结果增加状态兜底过滤。
* 商品管理页面继续允许查看和维护停用商品。

## 2026-08-25 - 增加经营规则预警与 AI 助手兜底协议

* 经营看板增加销售下降、毛利率偏低、门店下降和高库龄库存等服务端规则预警，并返回触发指标、原因和建议动作。
* 增加 `aiAdvisor` 统一输出结构，当前明确标记为 `rule_engine_fallback`，不伪造真实模型调用。
* 真实 AI 模型供应商、费用、数据范围及自动执行权限继续标记为【待确认】。

## 2026-08-25 - PN有效唯一约束与ZAJA商品PN归集

* `T_PRODUCT_PN` 改为仅对有效PN（`STATUS=1 AND IS_DELETED=0`）执行唯一约束，历史软删除PN继续保留但不再阻塞新商品使用。
* 商品删除时同步将关联PN标记为无效；PN编辑冲突检查不再把历史删除PN误判为其他有效商品。
* 出库、调拨和入库的SN/PN比对统一忽略大小写及空格差异。
* 将 `SP04956` 的有效PN及12条在库SN统一归集为 `ZAJA0002CN`，保留原PN_ID、SN、库存和历史商品记录。

## 2026-08-25 - 统一 SN 库存余额投影并清理汇总差异

* SN 商品库存汇总改为读取与 SN 明细同步维护的 `T_INVENTORY` 物化余额，避免列表查询逐商品实时统计 SN。
* 补齐销售预占、取消释放、销售归档后的 SN 库存同步，并移除调拨库存使用 `MAX(聚合库存, SN 数量)` 掩盖差异的逻辑。
* SN 详情查询改为商品、门店、库位批量加载，增加库存对账和订单/调拨 SN 查询索引。
* 新增 `T_INVENTORY_RECONCILIATION_LOG` 对账日志；本次修复 318 条聚合记录，未删除 SN、订单、入库或调拨事实数据，复核剩余差异为 0。

## 2026-08-25 - 销售订单详情展示补录明细并完善导出

* Web 端销售订单“查看”详情增加金额补录明细，展示项目、金额、增减方向、补录内容、券码、补录人、时间和凭证入口。
* 小程序订单详情补充补录增减方向、券码及正确的补录时间展示，补录合计改为接口返回的补录净额。
* 销售订单详情接口返回补录条数和补录净额；导出继续沿用 58 列结构并汇总全部补录文本。

## 2026-08-25 - 区分退单审批与退货入库状态

* 退单申请提交后显示“退单审批中”，最终审批通过后改为“退货入库中”。
* 退货入库完成后，整单退货显示“已退单”，部分退货恢复“已归档”。
* 后端禁止退单审批中或退货入库中的订单再次归档；小程序同步隐藏归档并展示当前退单阶段。

## 2026-08-25 - 优化调拨出库多数量商品的 SN 选择

* 调拨出库确认弹窗按实际调拨数量逐台生成 SN 选择行，数量为多个时可以选择多个不同 SN。
* 提交确认时同步传递所选 SN、PN 和商品明细信息，沿用后端逐台出库及重复 SN 校验。

## 2026-08-25 - 应付支付管理增加经销商与税务属性校验

* 结算单管理和付款管理增加经销商、税务属性只读展示及筛选项。
* 结算单保存经销商和税务属性快照，历史单据按关联应付款回退展示，无法判断时显示“未知”。
* 新增含税与未税不得在同一结算单或同一付款申请/付款批次中混合发起的后端校验，保留艾诺云与艾诺志兴不得混合付款的校验。

## 2026-08-25 - 修正采购申请金额与支付状态展示

* 采购申请金额改为当前采购金额扣除当前抵扣金额后的实际金额，并同步修正详情和导出。
* 审批通过的采购申请显示为“待支付”，关联应付款或采购垫付报销全部支付后显示为“已支付”。
* 采购申请状态筛选同步支持“待支付”和“已支付”。

## 2026-08-25 - 修复日结单批量下账超 3 条失败

* 修复日结单批量下账超过 3 条明细时因账户流水关联字段超长导致事务失败的问题。
* 批量下账确认提示新增已选明细总金额。

## 2026-08-25 - 修复多经销商账号门店选择范围

* `/store/all` 改为按账号已授权的多个经销商返回门店，不再错误使用员工主表中的单一经销商字段过滤。
* 修复采购申请收货门店分配无法选择重庆门店的问题，并同步修复销售门店选择、定金门店选择和国补照片门店筛选。
* 保留采购申请提交时的单据单经销商校验，不允许同一采购申请跨经销商分配。

## 2026-08-24 - 补充应付结算与付款管理的对方收款信息

* 应付结算单列表、详情及付款确认窗口展示对方收款单位、开户行、收款账号、税号和备注。
* 付款管理的待付款清单、付款导入预览、付款批次详情及付款登记窗口展示对方收款信息，并与我方付款账户明确区分。
* 付款批次接口关联结算单历史收款账户快照，导出文件同步包含对方收款信息；没有账户资料的历史单据显示“未配置”，不补造数据。

## 2026-08-24 - 修复小程序订单详情商品修改保存

* 订单详情保存商品时，以当前输入框中的商品名称、单价和数量为准，并同步重新计算商品小计。
* 修改已有商品不再被历史商品字段覆盖，商品明细无需删除后重新新增即可保存。

## 2026-08-24 - 修复用户角色切换时的经销商数量校验

* 店员/店长账号继续限制只能归属一个经销商。
* 编辑用户切换为采购、会计、出纳、BOSS等中台角色后，允许按配置选择多个经销商。

## 2026-08-24 - 新增双经销商权限与采购财务隔离

* 启用艾诺云（DIST001）和艾诺志兴（DIST002）经销商主数据，区域继续作为地理维度。
* 用户管理支持中台账号配置多个经销商；店员/店长限制为单个经销商，库存查询允许跨区域只读。
* 采购申请、费用、应付、结算、付款批次、付款记录和结算账户增加经销商归属及历史数据回填。
* 禁止跨经销商采购申请分配、直接调拨和应付合并结算；付款账户与结算单经销商不一致时由后端拒绝。

## 2026-08-24 - 业务操作界面仅允许选择已启用商品

* 商品业务选择接口新增启用商品筛选，采购申请、Web 库存筛选和拆装目标选择不再展示停用商品。
* 采购、销售、入库、调拨、拆分/组装和退库等后端写入链路增加商品状态校验，停用商品不能通过旧客户端或直接接口继续提交。
* 商品管理、商品审批和启停用维护仍保留停用商品的查看与维护能力；历史单据快照不被改写。

## 2026-08-24 - Web 销售订单列表增加归档操作

* 销售订单列表对正式未归档订单增加“归档”按钮，并复用现有归档校验和审计流程。
* 长订单号最多分两行显示，减少订单号对表格宽度的影响。
* 销售订单列表移除审批人列，审批信息仍保留在订单详情和审计记录中。

## 2026-08-24 - 调整采购申请列表金额展示

* 采购申请列表移除审批人列。
* 新增采购原价、抵扣金额两列，导出同步增加对应字段。

## 2026-08-24 - 统一账号单一主岗位角色与门店权限

* 用户管理改为每个账号只能选择一个岗位角色，服务端同步拒绝多角色提交。
* 角色编码统一按小写处理，采购、财务、商务等经销商级角色不再受历史主门店字段影响。
* 调拨、入库和采购小程序统一使用账号有效角色及独立门店权限；经销商级采购新增时必须明确收货门店。
* 清理历史多角色账号并保留既有门店授权，工号 18200194756 保留采购角色和原有 14 个门店权限。

## 2026-08-24 - 补充应付管理采购信息展示

* 应付管理待付款清单新增采购原价、返利抵扣和采购发起人列，采购金额继续由原有应付接口口径提供。
* 应付管理导出同步包含上述采购信息。

## 2026-08-24 - 明确采购商品信息更正范围

* 商品名称或 PN 更正只作用于采购单未入库的在途数量。
* 已部分入库的采购单保留已入库部分原始商品信息，只处理剩余未入库部分。
* 已全部入库、已结算或已关闭的采购单不再修改原始商品名称、PN 和采购事实；在途更正保留变更前后值及审计信息。

## 2026-08-24 - 修复角色菜单权限保存后回显

* 统一角色菜单权限接口响应格式，切换角色时清空旧树状态，并按服务端返回结果重新加载权限。

## 2026-08-24 - 收紧库存简表导出范围

* 库存简表只导出销售仓库存大于 0 的商品。
* 只保留笔记本、平板、手机、台机、配件五类，并按该顺序排列；其他分类不进入简表。

## 2026-08-23 - 收紧小程序库存查询数量口径

* 小程序库存查询的当前门店、其他门店、全部门店及门店明细数量只统计销售仓库存。
* SN 商品数量同时要求 SN 状态为“在库”，非销售仓或非在库 SN 不再计入门店数量。
* 保留 Web 库存页对铺货仓、样品仓等非销售仓分类字段的展示能力。

## 2026-08-23 - 统一商品厂商编码 / PN维护入口

* 商品新建和编辑统一在“厂商编码 / PN”区域维护 PN，移除独立 PN 管理弹窗和 PN 条码输入。
* 69码继续独立维护，历史厂商编码仅保留删除入口，不再新增厂商编码条码。
* PN修改按原 `pn_id` 更新，避免编码修改时新增PN、破坏SN和库存关联。
* 库存入库、销售和拆分/组装页面只能选择已有PN，不再允许业务单据临时创建PN。

## 2026-08-23 - 新增库存简表导出

* Web 库存汇总新增独立“导出库存简表”功能，保留原库存导出接口和字段不变。
* 新导出仅包含产品名称、主 PN、定价和销售仓总库存，并按商品类别顺序排列。

## 2026-08-23 - 小程序审批中心同步统一审批任务

* 小程序审批中心新增统一审批任务查询，与 Web 端同步展示 SN 修改等通用审批单。
* 通用审批详情通过审批实例接口补充申请说明、审批节点和业务明细。
* 通用审批通过/拒绝调用统一审批动作接口，保持与 Web 端处理结果一致。

## 2026-08-23 - 二手商品PN默认前缀

* 新建二手商品时，PN码输入框默认填入 `ANY-ESYP-`，用户可继续输入商品PN，也可手工删除前缀。

## 2026-08-23 - 优化财务页面金额与库存分类布局

* 财务管理页面金额统一显示为两位小数并使用千分位分隔，兼容手机端小程序运行环境。
* 商品分类卡片调整为一行两列显示。

## 2026-08-22 - 修复小程序二手采购PN未提交

## 2026-08-23 - 修复采购拒绝后关联报销无法拒绝

* 采购申请被拒绝后，关联的采购垫付报销允许执行拒绝审批，避免报销单长期停留在待审批状态。
* 关联采购未通过时仍禁止报销审批通过，避免生成无效待付款记录。

## 2026-08-23 - 兼容历史采购审批状态

* Web 与小程序审批中心同时查询采购申请的 `pending` 和历史 `pending_approval` 待审状态。
* 合并结果按采购申请 ID/单号去重，避免历史状态兼容导致重复显示。

## 2026-08-23 - 修复审批中心类型同步与采购待审状态

* 修正采购申请待审批状态使用 `pending`，避免 Web 审批中心漏掉采购审批。
* 统一 Web 与小程序的经销商、系统管理员、门店管理员角色映射，确保同一账号加载相同审批类型。
* 统一兼容通用审批、销售审批及各业务列表接口的返回结构，避免接口有数据但前端按错误层级读成空列表。

## 2026-08-23 - Web 审批中心同步全部业务审批

* 审批中心统一汇总通用审批、销售负毛利、采购、报销、商品、退库、销售退单、资源套回和毛利调整审批。
* 各业务类型继续调用原有查询和审批接口，保持与小程序审批中心一致，不复制后端业务规则。

## 2026-08-23 - 修复采购SN部分入库状态和草稿行数问题

* 修复入库单 `IN202608220543437552`：保留原有7个SN和7台库存，恢复为待入库，剩余1台由原单补录。
* 网页端恢复入库草稿时按当前剩余数量校正SN输入行，缺少的行自动补齐，不再直接覆盖当前入库数据。
* 网页端增加SN重复提交校验。
* 后端增加采购SN入库完成条件校验，数量或SN数量不足时保持待入库并拒绝标记完成。
* 增加采购SN入库完成条件单元测试。

## 2026-08-23 - 兼容 CloudBase GitHub 根目录构建

* 新增仓库根目录 `Dockerfile`，兼容 CloudBase GitHub 部署将仓库根目录作为构建目录的场景；后端仍推荐使用 `backend` 作为目标目录。

## 2026-08-23 - 修复月度任务页面空白

* 生产静态资源同步到最新 `frontend/web` 构建结果，补齐月度任务路由和页面代码。
* 同步更新 `backend/public` 与 `frontend/dist-web`，避免后端服务继续引用旧版前端包。

## 2026-08-23 - 修复生产环境月度任务接口404

* 生产网页 API 地址改为直连现有 CloudRun `anyerp-api` 服务，避免静态托管对 `/api/v1` 请求返回 404。
* 月度任务门店、员工和商品选项继续由 ANY-ERP 后端统一查询。

## 2026-08-22 - 合并审批中心销售负毛利审批

* Web 审批中心将通用审批与销售负毛利审批统一展示在同一张待审批列表中。
* 销售负毛利审批保留原有查看、通过、拒绝接口和自动归档行为，不新增审批业务逻辑。
* 小程序继续复用现有销售审批任务链路，保持与 Web 使用同一审批数据口径。

## 2026-08-22 - 修复二手采购审批误报PN缺失

* 采购审批重新组装二手商品明细时补齐 `pn_code` 到 PN 校验对象。
* 已填写 PN 和 SN 的“审批完成及入库”二手采购申请不再因审批阶段字段丢失而误报 PN 缺失。

## 2026-08-22 - 修复特价SN商品毛利计算口径

* 订单毛利计算按订单门店所属经销商读取有效 SN 特价。
* 特价 SN 商品使用 SN 特价替代商品统一定价计算毛利，并将单价来源和特价标记保存到毛利明细快照。
* 普通商品、非特价 SN 商品及非服务商采购价毛利规则保持不变；库存成本和销售结算成本不受影响。

## 2026-08-22 - 财务库存增加样机口径切换并修复YOGA三级明细

* 财务库存增加“含样机 / 不含样机”切换，含样机同时包含普通样品仓和租赁样机仓。
* 切换由 ANY-ERP 统一处理，影响库存总额及各级分类数量、成本金额。
* YOGA 等系列按商品型号与后台三级分类名称进行最长前缀匹配，支持展示 Air、Pro 等三级明细。

## 2026-08-22 - 修复订单备注保存失败

* 订单列表提交备注改为调用 ANY-ERP 销售订单更新 API。
* 移除对历史 `updateOrderRemark` 云函数和云数据库字段的依赖，保持备注与订单详情数据一致。

## 2026-08-22 - 修复采购申请详情编码缺失

* 采购申请接口在读取历史明细时，按商品主数据补齐缺失的商品编码和厂商编码。
* 新建采购申请和保存草稿时保存商品编码、厂商编码快照，确保“查看采购单”可以展示。
## 2026-08-22 - 采购单详情展示商品编码和厂商编码

* 采购申请详情的商品明细新增“商品编码”和“厂商编码”列，直接展示采购明细保存的编码快照。
* 采购管理和财务管理中的采购申请详情保持相同展示口径。

## 2026-08-22 - 修复二手采购明细丢失导致无法生成入库单

* 二手采购申请明细允许 `PRODUCT_ID` 为空，兼容尚未建立商品主数据的二手商品。
* 采购申请表头、明细、返利、运费和审计记录改为同一事务提交，明细保存失败时不再留下可审批的空采购单。
* 审批采购申请时增加空明细拦截，缺少商品明细的采购申请不得批准。
* 二手商品勾选“审批完成及入库”时，前端和后端均强制填写有效 PN，避免审批阶段因 SN 商品缺少 PN 失败。

## 2026-08-21 - 修复销售退单导出单价方向

* 销售订单导出中的退单明细保持单价为正数、数量为负数。
* 退单小计、商品应收金额和订单金额继续按负数导出。

## 2026-08-21 - 菜单管理支持拖拽调整顺序和层级

* 系统设置菜单管理支持拖拽调整菜单顺序，并允许跨父级、跨层级移动。
* 增加保存排序和放弃未保存调整能力，排序保存后对所有用户全局生效。
* 左侧导航改为递归渲染，支持调整后的多级菜单结构。
## 2026-08-21 - 优化订单详情备注与补录展示位置

* 将订单备注从详情顶部移动到主体内容最下方。
* 备注与金额补录记录相邻展示，补录内容、金额、补录人和凭证继续沿用后台 API 返回数据。

## 2026-08-21 - 商品分类管理控制财务库存展示

* 商品分类管理新增“展示在财务页面”开关，历史分类和新建分类默认关闭。
* 财务经营总览改为按最具体的已开启商品分类汇总库存数量和成本金额，移除商品名称模糊分类口径。
* 网页端分类管理和小程序财务页面继续通过统一 ANY-ERP API 使用同一分类结果。

## 2026-08-21 - 采购结算创建改为直接按金额结算

* 创建采购结算单移除剩余数量和结算数量展示及输入，改为直接编辑本次结算金额。
* 金额默认带出当前明细可结算的全部金额，支持修改为部分结算金额。
* 重新构建并同步 `frontend/dist-web`、`backend/public` 和 `frontend/web/dist.zip`，确保本次上传使用新页面。

## 2026-08-21 - 国补照片批量压缩包按查询日期命名

* 批量下载压缩包名称改为使用所选开始日期和结束日期，不再使用下载当天日期。
* 单日、日期范围和未选择日期时分别使用对应的日期范围或“不限时间”标识。

## 2026-08-21 - 国补照片批量下载跳过异常文件

* 批量生成压缩包时，单张照片读取失败或内容为空会记录日志并跳过，不再中断整个压缩包。
## 2026-08-21 - 财务管理库存结构增加商品大类和品牌/系列明细

* ANY-ERP 财务经营总览接口将库存分类聚合为电脑、手机、平板、配件四个大类，并返回各大类的品牌/系列子分类数量和成本金额。
* 小程序财务管理页面新增库存大类点击切换，默认展示电脑并支持查看拯救者、小新、Yoga 等子分类。
* 商品分类仍由 ANY-ERP 统一识别和汇总，小程序不新增库存业务计算逻辑。
* 修复一级分类接口未返回二级明细的问题；二级明细现在按有效商品分类树及商品系列字段由后台归集后返回。

## 2026-08-22 - 财务库存分类支持三级下钻

* 财务库存接口递归返回二级、三级分类的数量和成本金额。
* 兼容历史商品分类路径，使用后台商品的系列和型号字段匹配现有分类树。
* 小程序支持点击二级分类展开三级分类，并隐藏无库存子分类。

## 2026-08-19 - 排除小程序项目中的后台源码

* 在 `project.config.json` 中同时忽略 `frontend` 和 `backend` 目录，避免网页端及后台源码进入微信小程序上传包。
* 保留 `cloudfunctions` 云函数目录，不改变云函数上传和运行配置。
## 2026-08-21 - 批量库存维护支持部分执行和失败明细追踪

* 批量维护审批后的正式执行改为按明细独立事务处理，正确明细不再因其他明细失败而回滚。
* 新增明细执行状态、执行时间和失败原因，详情可以定位具体Excel行和库存不足等执行异常。
* 批次状态新增“部分执行”；服务重启或重复调度时跳过已处理明细，避免重复扣减库存或重复变更SN。
* 库存执行查询增加行锁，保持并发批量维护下的库存安全。

## 2026-08-21 - PN主数据采用最小优化方案

* 不新增 PN 关系表，继续使用 `T_PRODUCT_PN` 作为唯一 PN 主数据来源。
* 非 SN 商品允许一个商品维护多个有效 PN；SN 商品继续只保留一个有效 PN。
* 补强统一 PN 写入入口，防止单条库存维护绕过 SN 商品唯一 PN 校验。
* 扩充 PN/SN 一致性审计，区分有效 PN 多商品、SN 商品多 PN、在库 SN 商品冲突和未注册 PN。

## 2026-08-21 - 接入销售负毛利审批中心

* 后端待审批销售列表附带已保存的毛利快照，统一展示最终毛利和审批要求。
* Web 审批中心新增销售负毛利审批列表，支持查看、通过和拒绝，并复用原有销售审批事务。
* 保留小程序现有销售审批链路，不上传小程序项目改动。

## 2026-08-21 - 修复销售订单退单导出编码和补录字段

* 退单负向明细的“商品编码”改为读取商品主数据编码，不再把内部 `product_id` 导出为商品编码。
* 历史商品关联缺失时优先使用商品编码/PN快照，无法确认时留空，避免导出不可识别的内部ID。
* 补录教育项目兼容“教育”“教补”名称，运费项目兼容“运费”“提货费用”名称。
* 保持原销售订单、退单负向结算和作废订单的既有金额方向及 58 列结构不变。

## 2026-08-21 - 收紧店长经营报表和小程序财务管理权限

* 店长经营报表恢复为账号已配置的授权门店范围，不再默认查看经销商其他门店经营情况；BOSS 全局范围保持不变。
* 小程序财务管理仅向经销商级账号开放，店长和店员隐藏入口并拦截直接访问。
* ANY-ERP `/report/finance-overview` 增加服务端经销商级账号校验，防止绕过小程序页面限制。

## 2026-08-21 - 修复SN特价未自动带入销售价

- 新单或切换 SN 时使用接口返回的当前适用售价；编辑已有订单时保留已保存的实际成交价。
- 明确 SN 特价只影响销售售价，不改变毛利成本口径。

## 2026-08-21 - 修复库存汇总历史PN查错商品

- 库存汇总按历史在库 SN 的 PN 快照补充真实商品匹配，避免 PN 主数据已绑定其他商品时打开错误商品的序列号列表。

## 2026-08-20 - 修复SN库位调整未同步销售库存

* SN从样品仓等库位调整到销售仓时，同步更新 `T_PRODUCT_SN.INVENTORY_TYPE`。
* 同步迁移商品库存聚合记录的库位数量，避免主页仍显示样品仓库存并被销售归档误判为不在销售仓。
* 增加 SN 库位调整库存类型回归测试。

## 2026-08-20 - 国补照片上传时保存标准文件名

* 重新上传国补照片时，直接将照片名称保存为“订单号_姓名_原文件名”标准格式。
* 批量下载直接使用已保存的标准名称，避免下载阶段重复拼接订单号和姓名。

## 2026-08-19 - 修复历史PN查询不到在库SN

* PN主表无记录但在库SN保留PN快照时，商品PN查询为空会继续回退到商品搜索接口。
* 兼容 `83F60002CD` 查询 `PF5ZENLL`、`83NN006LCD` 查询 `PF65R86N` 等历史PN数据，不改变SN库存和销售校验规则。

## 2026-08-19 - 修复商品审批 PN 提示乱码

* 修复商品审批创建/编辑商品时，PN 重复绑定错误提示使用乱码字符串的问题。
* 统一提示为“PN码 [PN] 已关联其他商品，不能重复绑定”，避免审批页面显示乱码。

## 2026-08-19 - 优化国补照片下载文件命名

* 国补照片单张下载、单订单 ZIP 和查询结果批量 ZIP 中的图片文件名统一增加订单号和补贴人姓名。
* 在保留原照片名及其类型（如 SN）的基础上，统一使用“订单号_姓名_原文件名”命名，并清理文件名中的非法字符。

## 2026-08-19 - 销售归档仅允许扣减销售仓库存

* 销售归档按库位类型过滤库存，只统计启用销售仓（`normal_qty`）的库存。
* 铺货仓、样品仓、占用库存、不可售仓和租赁样机仓不再作为直接销售库存；普通 `pending_qty` 不计入可售数量。
* SN 商品归档和销售 SN 查询同步要求 SN 位于销售仓，增加非销售仓库存回归测试。

## 2026-08-18 - 修复小程序误加载网页端 ESM 脚本

* 在 `project.config.json` 中忽略网页端 `frontend` 目录，避免 `import.meta.env` 被微信小程序运行时按普通脚本执行。
* 保持 `sdxcx` 与网页端/后端的项目边界，未修改小程序业务代码。

## 2026-08-19 - 采购单增加去入库入口

* 采购单列表为已通过且存在待入库单的采购单增加“去入库”按钮。
* 一个采购单对应多张待入库单时，先选择具体收货门店和入库单。
* 跳转到入库管理后直接打开对应入库单的执行入库窗口，继续沿用现有库存、SN、PN、库位和权限校验。

## 2026-08-18 - 修复销售归档多库位库存误报不足

* 修复销售归档按商品和门店读取单条库存记录，导致同门店其他库位有库存时仍返回可用数量 0 的问题。
* 归档校验和扣减统一汇总同商品、同门店的库存行，并在事务中按占用库存、销售仓、铺货仓顺序处理。
* 增加同门店多库位库存归档回归测试。

## 2026-08-18 - 修复批量入库选择租赁样机仓提示仓位无效

* 批量维护后端校验补齐 `rental_demo_qty`，批量入库可以选择租赁样机仓。
* 新建门店库存记录时同步初始化租赁样机仓库存字段。

## 2026-08-18 - 负毛利订单改为归档前审批

* 提交订单时仅提示负毛利，不再阻断提交。
* 取消低于最低售价触发审批的旧规则。
* 归档时先完成 PN、SN、库存、定金和资源权益校验，再以最终毛利判断是否进入审批。
* 审批通过后自动完成归档；审批拒绝退回未归档，支持修改后重新归档。
* 毛利判断统一由 ANY-ERP API 执行，小程序和网页端只负责展示提示及状态。

## 2026-08-18 - 明确 sdxcx 与 ANY-ERP 的项目边界

* 明确 `sdxcx` 仅维护微信小程序端；网页端和后端统一位于 `D:\艾诺云\小艾虾\ANY-ERP`。
* 明确业务数据、业务逻辑、统计汇总、权限和财务口径统一由 `D:\艾诺云\小艾虾\ANY-ERP` API 提供。
* 小程序新增需求先补充 ANY-ERP API，再由小程序接入；本项目不新增或扩展网页端、`backend`、`frontend` 等目录。

## 2026-08-18 - 财务经营总览改为 ANY-ERP 聚合接口

* ANY-ERP 新增 `/report/finance-overview`，统一返回营业额、毛利、库存分类、账户余额和采购/报销付款状态。
* 小程序移除明细分页、库存名称模糊分类、账户余额汇总和付款状态推断，改为调用单一聚合接口。
* 区域账户、采购/报销区域快照、库存分类和付款状态判断统一由 ANY-ERP 执行。

## 2026-08-18 - 修复小程序配置文件解析错误

* 修复 `privacy.json` 描述字段未正确闭合导致的 JSON 解析错误。
* 新增 `sitemap.json`，补齐 `app.json` 中声明的站点地图配置。

## 2026-08-18 - 修复财务管理区域筛选和金额显示

* 修复小程序财务管理页面区域选择器未显示的问题，默认展示“全部”，支持成都、重庆切换。
* 修复财务报表响应嵌套字段未读取导致金额显示为 0 的问题，并将区域参数贯穿报表、库存、账户、应付和结算查询。
* 结算账户、应付和结算单增加区域快照及历史回填，账户管理增加所属区域设置。

## 2026-08-18 - 修复后端静态资源中间件语法错误

* 将 `backend/src/index.js` 中与 JavaScript 保留字冲突的 `static` 变量改为 `serveStatic`，修复后端启动解析失败的问题。

## 2026-08-18 - 移除无用数据库连接测试脚本

* 删除未被项目引用且存在语法错误的 `backend/test-connection.js`，避免编译时触发无关报错。

## 2026-08-18 - 统一经营报表金额显示格式

* Web 端和小程序经营报表的金额统一使用千分位分隔并固定显示两位小数，例如 `111,111,111.11`。
* 订单数、库存数量和百分比继续保持原有显示规则。

## 2026-08-18 - 修复单个入库检查脚本语法错误

* 将包含 `await` 的入库明细遍历改为可顺序等待的 `for...of`，修复脚本启动时报 `Unexpected reserved word 'await'` 的问题。

## 2026-08-18 - 修复小程序弹窗触摸穿透

* 所有自定义弹窗的遮罩层和前景内容层拦截 `touchmove`，手指在弹窗内滑动时不再带动背景页面上下滚动。
* 弹窗内部已有的 `scroll-view` 继续保留自身滚动能力。

## 2026-08-17 - 修复财务管理真实数据汇总

* 修复小程序财务管理数据全部显示为 0 的问题：账户金额改为读取账户中心余额，采购/报销付款改为读取应付清单与结算单，并修正库存分页汇总。
* 财务管理保留后端经营报表作为营业额、毛利额和库存总额的数据源，前端不再用订单商品成本自行计算毛利。

## 2026-08-17 - 修复销售退单审批通过失败

* 修复销售退单审批恢复国补资源权益时调用未定义 `selectedResources` 导致审批事务回滚的问题。
* 退单审批现在直接从订单明细读取已选择资源类型及兼容标记字段，继续按原规则恢复国补权益和政策补贴退款金额。
* 增加销售退单资源读取回归测试。

## 2026-08-17 - 经销商账号开放全部调拨门店操作

* 经销商级账号可以对本经销商全部有效门店的调拨单确认出库、拒绝、确认入库和退回。
* 调拨操作通过专用控制器校验门店有效性和经销商归属，不扩大普通库存、采购或其他业务权限。
* 店长、店员和调拨申请撤销的原有门店/申请人限制保持不变。

## 2026-08-17 - 修复调拨门店范围和销售订单 SN 时点

* 调拨门店下拉恢复展示当前经销商内全部有效门店，调拨库存、SN 和入库库位读取同步使用专用调拨范围。
* 调拨提交仍校验同一经销商、同一区域和门店有效性，普通库存权限不被扩大。
* 新建/提交销售订单取消 SN 强制校验和库存预占，SN 校验与库存扣减保留到订单归档阶段。

## 2026-08-17 - 统一发布并补齐筛选列表导出

* 采购申请、日结单、国补应收单、费用管理、应付管理和应付结算单支持按当前筛选条件导出全部结果。
* 销售订单、商品成本、定金收款和退单负向结算等既有导出能力继续保留并纳入同一发布版本。
* 生产发布前统一构建 `backend/public`，避免前端静态资源与后端接口版本不一致。
* 补齐采购、销售、费用和结算单的经手人/制单人模型字段、商品编码快照字段及幂等数据库迁移，历史单据自动回填兼容快照。

## 2026-08-17 - 全局允许审批人审批自己提交的单据

* 通用审批中心及商品、报销、应付结算、库存、资源套回、国补差额和业绩毛利调整等专用审批流程取消申请人自审限制。
* 保留角色、组织、门店范围、审批节点、审批意见和业务事务校验。
* 审批页面同步允许审批人操作自己提交的待审批单据。

## 2026-08-17 - 店长账号支持销售订单导出

* 用户管理中的 `manager` 和 `store_manager` 角色新增销售订单导出入口。
* 店长导出严格按账号已分配门店过滤，继续使用现有 58 列结构及订单分摊口径。
* 保持正式订单、定金收款订单和负向销售退单结算的现有导出范围不变。

## 2026-08-17 - 修复销售订单导出归档状态缺失

* 销售订单导出的“归档状态”列补充展示已作废、已取消、退库处理中和已退单状态，不再只显示已归档。
* 修复线上OMO、龙湖POS、智店通、二手回收抵扣等实际收款方式与导出固定列名不一致导致金额落空的问题。
* 修复开票状态为“不开票”时导出仍带出开票金额的问题，现统一导出为 0。
* 修复辅助销售人金额分配缺少金额时只显示姓名的问题，按主销售人与辅助销售人平均分配订单金额。

## 2026-08-16 - 调拨入库单仅在调拨管理中操作

* 调拨确认后仍生成调拨入库单，保持调拨入库单据和追溯事实不变。
* 通用/采购入库列表排除调拨来源入库单，避免在采购入库页面重复查询。
* 通用入库执行接口拒绝调拨来源入库单，调拨入库继续由调拨管理确认完成。

## 2026-08-16 - 分离商品 PN 主数据与厂商/69码并增加一致性审计

* 商品编辑页独立展示有效 PN，明确区分 `T_PRODUCT_PN` 与厂商编码/69码；条码删除不再被误认为 PN 删除。
* PN 主数据读取失败时禁止使用旧商品字段或条码列表兜底保存，避免“界面已删除、主表仍残留”。
* 商品 PN 管理支持查看软删除历史；兼容旧客户端按 PN 快照替换同步，不再追加制造重复 PN。
* 新增 `npm run pn:audit` 一致性审计，检查 SN 商品多 PN、缺 PN、失效 PN 引用和 SN/PN 快照不一致；不删除核心表或不确定归属数据。

## 2026-08-16 - 修复商品删除 PN 后再次编辑失败

* 兼容未提交 PN 列表的旧客户端，编辑商品时将当前 PN 作为快照同步，不再把新 PN 追加到已删除 PN 后触发 SN 商品多 PN 校验。
* 被删除的旧 PN 继续保留为软删除历史，商品当前只保留提交的有效 PN。

## 2026-08-16 - 修复历史 SN 入库唯一约束错误

* 普通入库和批量入库按 `PN + SN` 唯一身份定位历史记录，不再因商品关联或 PN 格式差异漏查后重复创建 SN。
* `sold` 和 `out_stock` 状态继续复用原 SN；当前在库或其他不可复用状态继续拦截。
* 当历史 SN 已关联其他商品时，返回明确业务提示，不再暴露数据库唯一约束错误。

## 2026-08-16 - 账户中心支持修改账户类型

* 财务账户中心编辑账户时支持资金账户、政策补贴应收、Care 可用金和厂商返利四种类型。
* 账户类型变更保留已有账户流水和业务关联；修改为厂商返利时必须关联启用厂商，并校验同一厂商只能有一个启用返利账户。
* 账户中心展示厂商返利账户并支持查询流水，但仍禁止通过账户中心手工记账，返利余额继续由返利业务流程维护。

## 2026-08-16 - 优化库存汇总查询与毛利指标

* 库存汇总进入页面后不再自动加载全量库存，改为输入商品信息或选择机型类型后查询。
* 库存汇总毛利展示新增最高毛利指标，后端按订单维度汇总商品所在订单的销售额和毛利，避免跨商品明细重复累计。

## 2026-08-15 - 商品编辑支持完整 PN 管理

* 商品编辑页改为读取商品全部有效 PN，支持新增、修改、删除和设置主 PN。
* PN 保存由后端统一事务处理；删除采用软删除，保留历史 PN 记录，SN 商品继续限制只能绑定一个 PN。
* 修复编辑商品只读取第一个 PN、保存时旧厂商编码覆盖完整 PN 列表的问题。

## 2026-08-15 - 经销商账号取消当前门店并统一按用户管理门店范围

* 后端登录态和权限中，非门店角色账号不再生成或依赖当前/默认门店；门店范围统一取用户管理中的有效门店分配，区域仅从已分配门店派生。
* 库存、调拨、销售、采购、定金、费用、报表和门店列表接口补齐显式门店参数校验，未授权门店不能通过请求参数访问。
* 经销商级新建采购、销售、定金和费用业务必须明确选择目标门店；历史单据继续保留实际 `store_id`。
* Web 和小程序移除经销商账号的当前门店恢复、自动首店和切换语义；小程序显示所属经销商及已分配门店数量，纯门店账号保持当前门店体验。
* 新增经销商账号门店范围和调拨可见性回归测试；Web 生产构建通过。

## 2026-08-15 - 价格管理增加成本导出

* 价格管理新增“成本导出”按钮，按当前商品搜索条件导出全部匹配商品，不受分页限制。
* 导出包含商品编码、厂商编码、商品名称、分类、单位、库存成本和产品定价，不改变现有成本计算与价格维护规则。
* 成本导出复用商品管理批量导出接口，通过导出类型区分价格导出，避免依赖独立价格导出路由。

## 2026-08-15 - 修复库存、门店选择和新建商品提交校验

* 修复库存入库及采购申请打开时库位列表仍未加载导致下拉为空的问题；提交入库前明确校验每条明细必须选择启用库位。
* 修复新建销售订单在门店异步加载完成前打开导致无法选择门店的问题，并对无门店权限账号给出明确提示。
* Web 新建商品与商品申请接口统一必填校验：商品分类、分类配置的必填字段、商品名称和 PN 码；PN 码同时写入厂商编码/PN 主数据入口，服务端审批前再次校验，避免 69 码冒充 PN。

## 2026-08-14 - 允许历史 SN 重新入库

* `out_stock` 和 `sold` 状态的历史 SN 重新入库时复用原 SN 记录，恢复为 `in_stock` 并更新当前门店、库位和入库成本。
* 普通入库和批量入库不再显示“SN历史记录已存在”错误；当前在库、调拨中等不可复用状态仍会拦截重复入库。
* SN 历史销售、出库和追踪日志继续保留，资源权益初始化保持幂等，避免重复 SN 或重复权益记录。

## 2026-08-14 - 调拨入库自动带出出库 SN

* 调拨入库执行窗口自动展示调拨出库已确认的 SN，已有 SN 不再要求入库人员重复录入。
* 调拨入库提交时携带 SN 主键和 SN 编码；后端校验请求值不得替换本次调拨明细的 SN。
* 调拨入库未指定库位时自动使用调入门店销售仓，避免历史待入库单缺少目标库位而无法确认。
* 普通采购入库、销售退库和历史调拨兼容逻辑保持不变。

## 2026-08-14 - 修复生成采购结算单时报应付抵扣字段不存在

* 移除 `Settlement` 模型误挂的应付抵扣字段，避免生成结算单时向未包含该字段的 `T_SETTLEMENT` 写入。
* 补齐 `Payable` 模型的 `offset_amount` 和 `offset_payable_id` 映射，保持采购退库抵扣与应付款数据库结构一致。

## 2026-08-14 - 修复采购退单金额未同步到订单与结算明细

* 采购订单列表和详情新增当前有效金额口径，采购退单后显示扣减后的申请金额、返利抵扣和实际应付，同时保留原始金额用于追溯。
* 应付结算明细按采购调整后的有效数量和剩余金额生成，采购退单后不会继续按原始采购数量结算。
* 生成过结算单的采购退单不再冲减原结算应付款，负向应付款保留为供应商待抵扣余额。
* 结算数量提交时由服务端再次校验采购调整后的有效数量，防止通过接口绕过退单限制。

## 2026-08-14 - 应付待结算清单增加状态与税务属性展示

* 应付待结算清单新增状态筛选，支持按待付款、部分结算、结算中、供应商待抵扣和已抵扣查看。
* 应付列表新增“税务属性”列，采购及采购调整读取采购申请发票类型，费用/报销读取费用单发票类型，历史缺失数据显示“未知”。
* 应付待结算清单新增单号模糊筛选和查询按钮，支持按来源单号或关联申请单号查询。
* 本次仅增加查询和展示，不改变应付金额、结算、付款或税务计算口径。

# 2026-08-16 - 订单导出补充完整金额补录信息

* 保持订单导出原有 58 列结构，复用原“预留字段2”位置并改为“补录信息”。
* 导出金额补录项目名称、金额、增减方向、补录说明和优惠券码，避免通用补录信息在 Excel 中缺失。

## 2026-08-14 - 销售订单与导出纳入定金订单并修正商品行金额

* 销售订单列表和导出合并展示正式销售订单与定金收款订单，定金收款订单标记为“定金收款”。
* 定金收款继续以 `deposit_receipt` 进入财务日结，定金抵扣不重复生成现金收入。
* 导出保持 58 列结构；国补按电脑、手机、平板等主商品行分摊，配件行的国补相关字段留空。
* 导出收款金额按实际收款方式归入对应列，多商品订单按商品小计比例分摊，避免逐行重复统计。
* 导出“辅助销售人比例分配”改为“销售人/辅助销售人1/辅助销售人2”格式，金额分配字段保留金额明细。

## 2026-08-14 - SN库存清单增加状态信息

* SN库存清单查询增加当前SN状态和状态变更时间字段，现有查询权限与数据范围保持不变。
* SN库存清单导出同步增加状态和状态变更时间列，沿用现有导出权限与筛选条件。
* 状态变更时间读取现有 `T_PRODUCT_SN.UPDATE_TIME`，不新增状态流转规则或数据库字段。

## 2026-08-14 - 统一PN主数据与SN关联

* 新增共享 PN 主数据服务，统一商品维护、普通入库、批量入库和库存转换的 PN 创建与冲突校验。
* 新建 SN 自动保存 `pn_id`，避免只保存 PN 文本造成主数据关联缺失。
* 安全补齐 1588 条可唯一匹配的历史 SN PN 关联；保留 9 条缺少 PN 主数据的异常记录待确认。
* 商品列表的 PN 搜索和展示改为优先读取启用的 `T_PRODUCT_PN`。
* 新增 `backend/scripts/migrateProductSnPnId.js`，默认 dry-run，使用 `--apply` 才执行补齐。

## 2026-08-13 - 采购退库支持已入库商品及SN并冲减应付款

* 修复采购退单提交时 `unit_price is not defined` 报错。
* 采购退单改为输入本次退库数量；未入库商品取消待入库，已入库非 SN 商品同步扣减库存。
* 已入库 SN 商品增加原采购入库 SN 选择，校验门店、商品、入库明细和在库状态。
* 采购退库新增退库记录、库存/SN日志及负向应付款调整，保留原采购、入库和付款历史。
* 未付款采购负向调整优先冲减原应付款，已付款或超出部分形成供应商待抵扣余额；负向调整不能单独付款。

## 2026-08-13 - 销售订单查询条件优化

* 销售订单查询增加提交人、客户姓名、订单号、商品名称（模糊查找）和商品编码筛选。
* 优化销售订单状态筛选，覆盖草稿、待审批、未归档、已归档、已取消、退库处理中、已退单及历史已完成状态。
* 缩短销售订单查询开始时间和结束时间控件的显示宽度。

## 2026-08-12 - 新增租赁样机仓库存分类

* 新增“租赁样机仓”标准库位和 `rental_demo_qty` 独立库存字段，固定为不可售。
* 库存汇总、库存导出、库存明细及入库库存类型选择支持租赁样机仓。
* 明确本次不开发租赁业务流程。

## 2026-08-12 - 修复日结单国补 POS 筛选无结果

* 日结单按“国补POS（电脑/手机平板）”查询时，兼容匹配实际保存的“客户实收”明细及历史未拆分明细。
* 普通收款方式和已带“客户实收/政策补贴应收”后缀的筛选仍保持精确匹配。

## 2026-08-12 - 修复采购申请接口失败提示与生产 API 回源诊断

* 采购申请列表和提交失败时透传服务端真实错误，区分接口不可连接与业务校验失败。
* 采购申请加载失败会清空旧列表和分页总数，避免“无数据”掩盖接口异常。
* 生产构建脚本在未设置 `VITE_API_BASE_URL` 时给出明确警告，提醒确认 `/api/*` 已回源到 ERP 后端。

## 2026-08-12 - 用户权限区域与门店联动配置

* 用户管理的分配范围改为同时配置区域和门店，区域选择后门店列表只展示对应区域门店。
* 保存和回显同时处理区域权限与精确门店权限；用户列表展示实际已分配门店。
* 服务端增加区域、门店和经销商三重归属校验，防止接口越权保存区域外门店。
* 跨区域调拨继续禁止，并提示改走出库及采购流程。

## 2026-08-11 - 调整库存汇总门店信息展示

* 库存汇总查询列表隐藏首列“门店”，减少页面信息占用。
* 库存汇总导出继续保留“门店”列，并按门店展开导出库存明细。

## 2026-08-11 - 修复新建商品分类下拉框为空

* 商品管理页首次加载时同步加载商品分类树，修复新建商品中的商品分类下拉框显示 `No data` 的问题。

## 2026-08-11 - 优化国补照片批量下载

* ZIP 内照片缺少扩展名时，按原文件、存储文件名或图片 MIME 类型自动补齐 JPG、PNG、WEBP 扩展名。
* 批量下载目录改为 `YYYYDDMM姓名手机号码`，不再使用订单号命名。
* 批量下载改用一次性短时下载凭证接入浏览器原生下载，点击后可在浏览器下载栏查看下载进度。
* 兼容 CloudBase 测试域名拦截：改为页面内请求并显示下载进度，避免跳转到测试域名提示页。

## 2026-08-11 - 库位按门店配置与停用库存校验

* 系统设置库位管理支持为销售仓、样品仓、铺货仓、不可售仓和占用仓选择适用门店，不再查询时自动补回所有门店。
* 库位编辑支持调整适用门店；停用库位前检查数量库存和在库 SN，存在库存时拒绝停用并提示门店及库存信息。
* 保留历史库存、SN和单据对原库位的引用；不提供批量导入或批量操作。

## 2026-08-10 - 修复销售订单和国补照片日期筛选

* 销售订单查询增加开始日期和结束日期，订单列表与导出使用同一组日期条件。
* 国补照片查询和查询结果批量下载统一传递开始日期、结束日期，并在查询时重置到第一页。
* 服务端按中国时区将开始日期转换为当日 00:00:00、结束日期转换为当日 23:59:59.999，支持只填写开始或结束日期。

## 2026-08-10 - 库存汇总增加门店维度和经销商区域权限

* 系统设置中，经销商级账号支持直接配置一个或多个管理区域；区域权限不再从门店权限反推。
* 经销商级账号查询库存汇总、门店下拉和库存汇总导出时，服务端按所属经销商和账号区域过滤。
* 库存汇总页面增加“门店”列；导出增加“门店”列，并在同一工作表内按门店、商品排序展示门店库存。
* 历史经销商级账号通过自动迁移从原门店范围补齐初始区域权限。

## 2026-08-10 - 修复 Web 调拨发起门店选择

* Web 调拨新增弹窗改用与小程序一致的调拨门店选项接口。
* 调出门店默认带入当前门店但允许重新选择；添加商品后锁定调出门店，避免商品来源与调拨门店不一致。

## 2026-08-10 - 优化国补照片查询筛选与批量下载提示

* 修复国补照片查询日期范围未稳定传递的问题，查询和批量下载统一使用标准日期参数。
* 增加按门店筛选，并由服务端继续校验账号可访问的门店范围。
* 批量下载减少无关订单关联数据加载；下载超时或文件过大时给出明确提示，并建议将查询范围控制在一周以内。

## 2026-08-10 - 修复SN批量导入机器号表头识别

* SN商品批量入库和批量出库兼容 `SN`、`机器号`、`序列号`三种Excel表头。
* 修复使用业务常用“机器号”表头时所有正确数据均被误报“SN商品必须填写SN”的问题。
* 批量入库预校验覆盖所有历史SN状态，避免已出库、已销售或退库待入库SN在执行阶段触发数据库唯一约束。
* 批量维护详情改为独立查询明细并排除原始Excel行快照，保证大批量申请可以稳定展示明细。
* 执行失败信息增加具体行号、SN和历史状态提示，不再只显示 `Validation error`。

## 2026-08-09 - 批量出库限定为库存调整

* 批量管理支持 SN 商品逐台出库和非 SN 商品按数量批量出库。
* 移除批量出库的资源权益触发入口，服务端强制禁止生成结算单和待结算记录。
* SN 批量出库新增操作日志，记录批量维护申请单号和执行人。
* 修复大批量出库文件存在异常行时无法继续的问题：异常行排除并支持下载，正确行生成申请；审批执行失败时继续整批事务回滚。
* 导入部分成功后增加“完成并查看申请”入口，避免文件清空后按钮不可继续操作。

## 2026-08-09 - 统一经销商归属并按区域展示用户

* 将历史 `DEFAULT` 经销商归属迁移为唯一有效编码 `DIST001`，覆盖门店、员工和经销商关联业务数据。
* 用户管理列表将“所属经销商”改为“所属区域”，按已分配启用门店汇总去重区域，支持多区域显示。
* 用户列表区域筛选改为按已分配门店的区域筛选。

## 2026-08-09 - 修复门店 ID 尾随空格导致库存批量导入失败

* 统一修复门店 `D0215327` 的尾随空格及员工、门店权限关联数据。
* 保留规范仓位，停用同门店下重复的带空格仓位记录，避免历史数据丢失。
* 门店新建时自动清理首尾空格；库存批量导入兼容历史门店 ID 尾随空格。

## 2026-08-08 - 优化 Web 登录失败提示

* Web 登录失败时优先展示后端返回的业务消息，避免将 `Request failed with status code 401` 作为密码错误提示。
* 登录请求收到 401 时不再触发会话失效跳转，保留登录页以展示“账号或密码错误”等接口消息。

## 2026-08-04 - 全量修正账号归属与门店权限数据

* 完成全量核查并修正 41 个有效非 BOSS 账号的账号经销商与门店权限不一致问题。
* 清理 11 条跨经销商历史门店权限，保留账号实际所属经销商下的有效门店。
* SN 追踪权限同步改为按已分配门店控制，避免仅凭经销商字段扩大可见范围。

## 2026-08-04 - 所属经销商与分配门店权限范围修正

* 系统设置增加所属经销商展示和编辑，BOSS 可维护归属，普通管理员只能维护本经销商账号。
* 订单列表、订单明细、订单导出、国补照片和门店下拉改为按已分配门店过滤，不再把所属经销商直接当作订单查询范围。
* 切换账号所属经销商时自动清空原门店权限，重新分配时只允许选择新经销商下的有效门店。
* 已将邓红梅（14780834570）调整为 `DIST001`，保留 11 家 `DIST001` 门店权限，移除 3 家 `DEFAULT` 门店权限。

## 2026-08-04 - 订单明细导出与经销商范围、国补照片批量下载优化
* 修复经销商级账号门店下拉仍受逐门店权限限制的问题：财务、出纳、商务及其他经销商级角色可按本经销商查询全部有效门店和所有人员订单。
* 订单导出改为附件同结构的 58 列明细，按商品展开，补充产品、定价、补贴、收款方式、补录、退货和订单状态信息。
* 国补照片查询页在“重置”后新增“批量下载查询结果”，服务端按当前筛选结果生成按订单分目录的 ZIP，并保留下载日志。

## 2026-08-04 - 经销商级订单查询与导出

* 经销商级角色（包括财务、出纳、商务及其他非店员/店长角色）可查询本经销商全部门店、全部人员的销售订单；BOSS 保持全局范围。
* 销售订单列表增加 Excel 导出，导出遵循当前筛选条件，不导出分页限制，并包含订单号、创建时间、门店、创建人、客户、金额和状态等列表信息。
* 服务端重新校验经销商范围，店员和店长继续沿用原门店/人员数据权限。

## 2026-08-04 - 优化国补照片加载与批量下载

* 修复国补照片页面未解析历史 `cloud://` 文件标识、导致照片全部加载失败的问题；云存储照片改为由服务端生成临时访问地址，页面增加失败重试提示。
* 兼容历史照片对象中的 `displayUrl` 公共地址；云存储凭证暂不可用时优先使用已保存的公共图片地址。
* 国补照片新增按订单整组下载 ZIP 功能，继续沿用账号、门店范围校验和下载审计；支持本地受保护照片及历史云存储照片。
* 批量下载改为按当前筛选条件下载全部匹配订单照片；未选择任何筛选条件时禁止全量下载并提示用户先选择条件。

## 2026-08-04 - 已实现功能迁移到左侧二级菜单

* 修复左侧二级菜单未及时显示的问题：布局加载时自动从后端刷新当前用户菜单权限并更新本地缓存。
* 销售、库存、采购、审批、财务、商品、报表和系统设置的已实现功能统一增加左侧二级菜单和独立路由入口。
* 原页面顶部页签改为由路由驱动，隐藏顶部页签栏；定金管理继续保留在销售订单页面内。
* 更新菜单权限种子和迁移逻辑，停用没有对应页面的旧菜单项，并保留旧模块入口重定向兼容。
* 未新增退货管理、采购单、采购入库、库存预警等尚未实现功能。

## 2026-08-01 - 修正采购入库单采购发起人

* 入库列表和详情按采购申请 ID/采购单号关联 `T_PURCHASE_REQUEST.APPLY_USER`，返回真实采购发起人。
* 保留入库单 `CREATE_USER` 作为入库操作/制单审计字段，不再将审批人误显示为采购发起人。

## 2026-08-01 - 修复库存导出失败

* 补齐前端库存汇总和 SN 库存清单共用的 Excel 下载方法，恢复导出请求、文件名解析和浏览器下载。

## 2026-08-01 - SN追踪修复

* 修复 SN 追踪仅按明细 SN 文本精确匹配导致采购入库记录缺失的问题：改为以库存 SN 主记录为锚点，同时按 `SN_ID`、当前/历史 SN_CODE 关联采购申请、入库、销售、退库、调拨和 SN 日志。
* 修复采购入库生成 SN 后未回写入库明细 `SN_ID` 的问题，并在 SN 追踪中补充 PN 展示与库存 SN 记录兜底信息。

## 2026-08-01

* 采购申请补充供应商群截图、申请人和撤销记录字段；申请人及授权角色可撤销符合状态条件的采购申请，并同步处理入库、应付款、费用、结算冲销和返利抵扣。
* 新增报销申请撤销接口及未付款结算单冲销机制，保留原结算单并生成负向冲销明细。
* 商品申请列表支持“我的申请”范围，申请人可撤销待审批的新建商品申请。
* 库存销售仓明细按全资源货、仅国补和无法国补拆分展示，并兼容历史未拆分库存及 SN 资源权益数据。

## 2026-07-31

* SN修改改为申请审批流程：库存查询的序列号查看页面不再直接写入SN，支持一张申请单提交多条在库SN明细。
* SN修改申请由admin或boss审批；审批通过后在同一事务内更新SN并写入不可变的SN修改追踪记录。
* 申请原因必填，审批拒绝保留意见；SN跟踪支持通过原SN或新SN查询修改记录。

## 2026-07-30

* 修复销售订单中需 SN 管理商品的录入与提交：前端支持直接输入 SN 码，服务端在提交正式订单时强制校验 SN。
* 销售订单详情补充商品 `need_sn` 字段，兼容草稿回显和历史数据中的布尔/数字类型值。

## 2026-07-28

* 完善登录后修改密码功能：用户可从顶部账号菜单打开修改密码弹窗，填写旧密码、新密码和确认密码。
* 修改密码表单按明文显示当前输入内容，增加必填、两次输入一致、至少 6 位及新旧密码不能相同校验；密码服务端仍只保存 BCrypt 哈希。
* 修改密码成功后保留当前登录会话；后端补充密码类型和长度校验，避免仅依赖前端校验。

## 2026-07-25

* 新增独立 Windows 图形心跳客户端：双击后自动启动，默认每 5 分钟对 API 健康接口执行一次只读 GET，并显示每次执行结果和历史记录。
* 心跳客户端支持停止运行、保存 API 地址和超时配置；不需要登录凭证，不连接数据库，不修改业务数据。
* 销售管理新增“国补照片”二级页面，支持按订单时间、补贴人姓名、手机号和云闪付订单号查询订单照片。
* 国补照片支持缩略图预览、下载和按订单整体重新上传；重新上传会替换原照片并记录销售订单操作日志。
* 国补照片查询、修改和下载新增服务端角色及门店范围校验，BOSS、admin、财务和店长可使用，店长仅限授权门店。

## 2026-07-21

* 应付待结算清单新增来源类型筛选，并按采购、采购调整、费用和报销使用不同颜色标签展示。
* 应付待结算清单新增筛选结果及已勾选记录的条数、剩余待结算金额汇总；每条记录支持多选。
* 批量生成结算单前校验所选记录属于同一供应商，并将勾选记录带入原有结算申请明细，继续沿用结算审批和付款流程。

## 2026-07-20

* 修复库存汇总按仓位类型统计的问题：绑定不可售仓但历史数量写入 `normal_qty` 的记录会正确展示到不可售库存，并从销售仓库存中剔除。
* 库存汇总明细按门店和仓位分别展示；入库时以标准仓位类型作为库存字段归属，避免后续库存数量写错字段。
* 修复 SN 跟踪关联单据按钮丢失：现支持采购申请、销售订单、调拨单和经销商可见的入库单跳转，沿用现有单据详情页面，不新增数据表。
* SN 跟踪关联单据增加服务端权限校验：员工仅可查看本人发起的原始单据，店长按授权门店，经销商级账号按本经销商，BOSS 查看全局。

## 2026-07-19

* 调拨管理兼容 Web 和小程序发起的调拨记录，并按经销商字段补齐历史调拨查询范围。
* SN 跟踪关联入库单修复错误跳转，增加经销商级账号权限校验，并展示采购来源、供应商、采购价格和门店名称。
* SN 信息门店改为显示门店名称；入库单详情增加商品 SN 展示。

## 2026-07-19

* 统一采购申请、销售订单和结算单的草稿删除规则：仅从未提交过的草稿可删除，已提交单据不可删除。
* 结算单新增软删除接口和前端操作按钮；删除时同步恢复关联应付款/费用状态，并保留结算明细及审计日志。

## 2026-07-18

* 应付结算单新增备注和审批流程：提交后进入待审批，支持审批通过、退回草稿及审批记录留痕。
* 新增独立应付结算单管理页面，支持按金额部分付款，保留原结算金额、付款流水和剩余应付金额。
* 结算创建页支持按采购数量拆分，字段改为“结算数量”，数量展示去除无意义尾数。
* 国补 POS 首部分“客户实收”日结到账金额新增 0.6% 税费扣除，按国补金额、国补应收金额和订单国补补贴金额统一计算；原始订单收款和应收记录保持不变。

* 采购申请和销售订单新增服务端持久化草稿：保存后进入列表，支持原单编辑、重复保存、提交和草稿删除。
* 销售订单草稿复用原订单号提交，草稿阶段不扣库存、不占用定金、不处理 SN 和资源权益；提交后沿用现有正式订单流程。
* 采购申请草稿删除改为 `status=deleted` 标记，销售订单草稿删除使用 `is_deleted=1` 软删除，正常列表隐藏已删除记录。
* 费用单新增服务端持久化草稿：保存后进入费用列表，支持编辑、提交和删除；提交时才生成对公应付或进入私人垫付审批。
* 库存管理页面新增库存汇总和 SN 库存清单导出按钮，导出沿用当前筛选条件并返回全部匹配记录，不受页面分页限制。
* 新增库存导出接口，继续执行后端门店权限过滤，导出内容包含页面库存数量、价格、库龄和资源状态等字段。
* 采购申请查询改为按经手人筛选，不在列表中单独展示经手人列；导出继续沿用经手人筛选条件。
* 按新建采购申请页面稿优化采购申请弹窗：商品明细改为表格展示商品编码、厂商编码、单价、数量和总价，增加商品合计、门店分配和资源权益区域。
* 修复采购申请列表提交人为空的问题，兼容读取历史单据的提交人字段，并保证新建申请保存有效提交人信息。

## 2026-07-17

* 修复销售退库只有申请/审批、没有生成待重新入库单的问题：审批通过自动生成 `SALES_RETURN` 入库单，执行入库后恢复原 SN、门店库存和销售订单状态。
* 销售退库 SN 增加“退库待入库”中间状态，重新入库页预填原 SN 并校验不可替换 SN；SN 追踪增加退库和重新入库日志。

* 小程序经营报表金额统一按实际金额展示，取消“万”等缩写，保留千分位和两位小数。

* 优化库存汇总展示：现有库存、铺货仓、样品仓、不可售仓和占用仓数量均支持鼠标悬停查看各门店对应库存明细。
* 现有库存明细改为仅展示各门店销售仓数量，并增加全资源货、仅国补、无法国补的颜色提示。

* 库存管理 SN 库存清单新增“调整库位”操作，可将当前在库 SN 直接调整到同门店的启用标准库位。
* 库位调整不改变门店、库存数量和 SN 状态，并写入 SN 变更日志；后端增加门店归属、在库状态和启用库位校验。
* 本次暂不支持非 SN 商品数量库位调整和跨门店移动。

## 2026-07-16

* 新增调拨历史记录查询，支持全部调拨状态、调拨单号、状态和日期筛选。
* 调拨查询按角色分级：店员查看本人参与记录，店长查看授权门店全部记录，经销商其他角色查看本经销商全部门店记录，BOSS 查看全部记录。
* 修复调拨列表显式传入门店参数可能绕过服务端门店权限过滤的问题。

* 修复门店编辑选择店长后提示编辑失败的问题：门店新增/编辑现在在同一请求中保存店长，并校验店长为当前经销商下的在职员工，避免基本信息更新成功后店长更新请求失败造成误报。

* 修复商品 PN 主数据与商品导入、创建、编辑、厂商条码新增不同步的问题；新增/变更厂商 PN 时统一转字符串、去空格、过滤占位值，并在同一事务中写入 `T_PRODUCT_PN`。
* 优化销售归档 PN 校验：优先按订单商品 `product_id` 关联有效 PN 主数据，PN 比对统一规范化；移除仅凭 `manufacturer_code` 放行的兜底逻辑，PN 不存在或商品不匹配仍禁止归档。
* 加固商品批量导入：按规范化 PN 匹配已有商品，普通空格不再拆分 PN；同一 PN 匹配到多个商品时拒绝自动选择并记录导入失败，避免错误覆盖商品。
* 新增存量 PN 修复脚本，依据有效厂商条码安全补齐 `T_PRODUCT_PN`；本次已修复 4479 条，报告商品 `870017165` 已恢复归档所需的 PN 主数据。
* 对同一 PN 被多个商品占用、仅存在历史删除记录等无法自动判定的存量数据保留人工复核，避免错误转移库存归属。
* 完成重复商品收口：停用商品 `SP04584`、`SP02845` 的旧厂商条码不再参与批量导入匹配，并将有库存/SN 的 `F0HM00L8CD`、`ZAG60158CN` 分别归属到启用商品 `SP02907`、`SP00437`。
* 修复订单 `ORD202607151218081574` 明细 `121` 的商品 ID 错绑：PN `870017165` 原指向 `SP00686`，已按 PN 主数据、商品名称和门店库存修正为 `SP01819`。

## 2026-07-15

* 采购结算支持按采购商品和数量部分结算，个人垫付报销支持输入金额分次报销，原单保留剩余金额并显示部分结算/部分报销状态。
* 修复个人垫付报销审批通过生成报销结算单时因 `Settlement.supplier_id` 仍被模型强制要求非空而失败的问题；报销结算改为允许无供应商并保留员工收款方及来源信息。

## 2026-07-14

* 采购申请发票类型选项调整为“未税（收据或普票）”和“增专票（13%）”，移除6%选项。
* 采购订单新增“退单”入口，支持按门店和待入库单调整未入库数量；已入库数量继续通过库存退库处理。
* 新增采购调整单及明细迁移，保留原采购、入库和应付款历史；调整后待入库数量为0时入库单标记为已取消。
* 公司账期采购退单按实际应付单价生成正数或负数待付款调整，负数调整不能单独生成付款金额不大于0的正式结算单；个人垫付同步调整采购垫付报销单。
* 采购调整同步处理返利抵扣的按比例追加或退回，并记录返利账户流水。
* 修复生产环境 Vue Router History 路由直接访问或刷新时被 COS 按错误 Key 查找的问题；统一根路径资源构建，并限制 SPA fallback 不覆盖 API 和静态资源请求。
* 补充 CloudBase 静态托管的 SPA fallback、同源 API 回源和 CDN/回源路径配置要求。
* 统一前端 API base 路径规范化，去除环境变量中的多余尾斜杠，避免 API 地址重复拼接路径。
* 商品和定价批量上传新增文件格式、表头和数据行预校验。
* 商品和定价批量上传改为后台异步任务，前端展示排队、处理中、完成、部分失败和失败状态，避免大文件被请求超时误报为失败。
* 商品批量上传继续按厂商编码匹配已有商品并更新，未匹配商品继续新增；错误行进入失败清单，支持下载修改后重新导入。
* 定价批量导入改为批量预加载商品和现有价格，并按批次写入价格及价格变更记录，改善大数据量导入性能。
* 新增商品/定价导入任务表，保存来源文件、任务结果、失败行快照并支持服务重启后恢复任务。
* 修复小程序店员/店长发起调拨时因账号经销商归属字段与实际门店归属不一致而被误判无权的问题；调拨门店查询范围负责权限过滤，提交时继续校验两门店属于同一经销商和同一区域。

## 2026-07-13

* 新增通用审批管理中心：支持流程版本、审批实例、待办任务、串行签批、或签、审批日志和拒绝后重新提交。
* 审批人支持指定人员、门店店长、直属上级以及角色加组织范围。
* 员工新增直属上级配置，门店新增明确店长配置。
* 初始化、数据库迁移和测试业务数据清理脚本同步加入审批中心结构。

## 2026-07-12

* 供应商管理新增“是否服务商”和“非服务商每件毛利上浮金额”。
* 服务商订单毛利按产品定价计算，非服务商订单毛利按采购价加每件固定上浮金额计算。
* SN 入库和销售订单明细补充采购来源供应商信息；未归档订单使用最新供应商配置，已归档订单保留原毛利快照。
* 修复商品查询编辑页未返回 `need_sn` 等商品字段，导致需要 SN 的商品编辑页错误显示为关闭的问题。
* 更新一键清空业务数据脚本：补充库存批量申请、批次权益、Care 可用金和国补到账业务数据清理，并保留厂家返利政策、报销类型、采购货型及国补账户路由等配置。

## 2026-07-11

* 新增库存调拨流程改造：校验同经销商/同区域范围，调出确认时校验库存，记录出库与收货凭证照片、确认人及确认时间。

* 修复库存批量维护导入中文来源文件名被错误解码为乱码的问题。
* 修复非 SN 批量导入错误记录过大导致 `ERROR_JSON` 超长失败的问题；错误快照改为保存行号和原因，字段迁移为 `MEDIUMTEXT`。
* 登录页取消预填充测试账号和密码，账号及密码改为用户手动输入。
* 库存汇总悬浮明细增加“库型”分组标题。
* 优化大批量库存导入校验，增加批次内商品、门店和仓位缓存，并延长导入请求等待时间，避免大文件被误报为导入失败。
* 批量维护审批改为快速返回并由后台执行，执行过程保持单事务，支持执行中状态、失败原因记录和服务重启恢复。

## 2026-07-10

* 库存批量维护导入改为二级导入页面，批次级统一选择操作类型、SN/非SN导入类型、标准仓位和资源权益。
* 库存批量维护模板简化为行级字段：门店ID、商品编码、PN、SN或数量、提货价和备注，不再要求门店名称、商品名称、库位和权益逐行填写。
* 库存批量导入改为部分成功：正确行生成批量维护申请，失败行返回异常原因并支持下载后修正重导。
* 商品成本刷新口径调整：SN 商品按当前在库 SN 单台成本平均，非 SN 商品按当前剩余 FIFO 成本层加权平均。
* 商品价格批量导入不再包含生效时间，空价格按 0 立即生效，导入范围保持为定价、零售价和最低售价。

---

## 2026-07-09

* 库位管理调整为全局标准仓位：销售仓、样品仓、铺货仓、不可售仓、占用仓；配置后自动同步到权限范围内全部门店，新建门店自动生成标准仓位。
* 库存汇总的仓位展示与标准仓位关联，门店明细同时展示销售仓、样品仓、铺货仓、不可售仓、占用仓库存。
* 系统设置“库位管理”保留仓位启停和可销售配置；停用仓位不删除历史库存引用，批量维护、入库和SN清单继续引用门店仓位实例。
* 库存类型确认 `pending_qty` 显示为“占用仓”，`demo_qty` 显示为“样品仓”，与库存汇总标准仓位命名保持一致。
* 明确库存批量维护规则：Excel 导入先生成批量维护申请单，门店发起的批量库存操作必须由经销商总权限账号审批；SN 商品按 SN 清单维护，非 SN 商品按数量调整并支持批次资源权益，批量出库可按同一标准统一触发资源权益结果。
* 修复返利上账单模型字段与数据库表结构不一致的问题；启动迁移会自动为 `T_REBATE_POSTING_ORDER` 补齐 `SETTLEMENT_TYPE`、收款对象和来源单据信息字段，避免查询返利上账单时报 `Unknown column 'RebatePostingOrder.settlement_type'`。
* 商品管理页面新增 admin/BOSS 可用的批量删除能力，支持当前页勾选、多选后批量删除，并按商品返回成功和失败原因。

## 2026-07-07

* 商品价格管理新增“零售价”，零售价作为小程序及销售开单默认销售价；产品定价继续用于经营毛利商品成本，最低售价继续用于低价审批。
* 商品价格批量导入模板、校验、导入和变更日志支持零售价，并校验最低售价不得大于零售价。
* 小程序订单创建和订单详情中，非 SN 管理商品隐藏 SN 输入框，PN 行整行展示，并移除 PN 后多余必填星号。
* 小程序首页“采购申请”改为“采购及报销”，新增“费用管理”入口。
* 小程序采购申请新增付款方式：公司账期、个人垫付；申请记录改为采购信息标题右侧文字入口。
* 小程序新增费用管理页面，支持区域/门店、报销类型、费用发生方、支付方式、发票信息、金额日期和备注提交。
* 采购个人垫付提交后自动生成采购垫付报销单；采购审批通过后由领导审批报销单生成报销结算单。
* 费用财务对公提交后进入应付管理待付款清单；费用私人垫付提交后进入报销审批。
* 财务管理新增报销结算页签，个人垫付审批通过后生成报销结算单，提交后进入付款管理付款。
* 应付管理支持费用来源待付款；公司对公费用可从应付清单生成费用结算单，再提交到付款管理付款。
* 系统设置新增报销类型管理，支持报销类型新增、编辑、停用和删除。
* 后端新增费用/报销、费用类型、采购垫付、费用应付及报销结算相关数据库字段和迁移。

## 2026-07-06

* 供应商返利账户改为后台内部账户；返利上账时按供应商自动创建或恢复，不再要求用户提前配置结算账号。
* 系统设置的结算账号管理和财务账户中心不再展示供应商返利内部账户，返利余额继续在返利管理和采购抵扣页面展示。
* 收款方式默认账户及门店账户仅允许选择资金账户；Care可用金、供应商返利和政策补贴应收不能作为客户实收账户。
* 后端增加收款账户类型校验，自动迁移解除历史错误绑定，并补充内部账户隔离测试。
* 财务管理在“国补应收单”后新增独立“返利下账”页签，原“资源权益核销与成本调整”中的资源下账子页已移除。
* 返利下账清单统一展示销售使用、公司套回、厂家返利和手工返利记录，支持待处理优先排序。
* 新增按创建时间、SN、备注模糊查询、类型、来源、状态和供应商的组合筛选。
* 新增不关联SN的手工返利类型；录入供应商、金额和必填备注后形成待核销返利下账单。
* “返利管理”恢复返利上账并升级为正式上账单；上账单生效后立即增加供应商返利可用余额，采购可以随即抵扣。
* 返利下账改为关联返利上账单核销，不再重复增加返利余额，也不允许未上账返利直接下账入账。
* 新增返利上账与下账多对多核销分配，支持部分核销，并展示双方已核销金额和剩余待核销金额。
* 返利下账核销可按原因撤销，撤销只释放核销金额、不改变返利余额；上账单已有核销时禁止直接冲销。
* 供应商仍有采购占用返利时禁止冲销返利上账单，必须先完成采购退单；冲销保留原单并生成反向余额及账户流水。
* 返利余额流水移除直接冲销入口，finance、admin和BOSS继续拥有返利新增、核销、撤销及冲销权限。
* 新增返利上账单表、上账/下账核销分配表及已核销金额字段，补充并行流程自动化测试。
* 库存管理在“库存汇总”后新增“SN库存清单”，按单台展示 SN、PN、商品、门店、库位、资源状态、统一售价、SN特价、适用售价、库龄和备注。
* SN库存清单支持按SN/PN/商品、门店、库位、资源类型、资源状态、是否特价和库龄范围筛选，并在数据库层全局分页。
* 新增经销商SN销售特价和特价变更记录；特价按 `SN + 经销商` 隔离，商品统一调价不会覆盖，取消后回退最新统一售价。
* SN特价维护仅开放给本经销商admin及BOSS，服务端同时校验SN当前门店、经销商归属和账号门店权限。
* 销售开单选择SN后自动带入当前经销商适用特价；特价低于最低售价时继续触发现有低价审批。
* 新增SN特价优先级、统一调价隔离、库龄、权限范围和销售带价自动化测试。
* 用户应收调整为商品总额扣除普通折扣，国补和教育补贴作为国家及厂商承担的收入计入毛利，不再从用户应收中扣除。
* 毛利公式升级至 V5，系统启动时自动重算旧版本毛利快照，确保经营看板和员工业绩同步切换新口径。
* 订单经营毛利商品成本改为产品定价，公式统一为“用户应收－产品定价－应收税率费用－增值税＋补录净额”。
* 产品定价未设置时首次采购入库默认采用采购价，并允许在“商品管理－价格管理”人工调整；后续入库和成本刷新不覆盖非零定价。
* 订单毛利快照新增产品定价合计和明细，经营看板、员工业绩及订单查询毛利弹窗同步使用新口径。
* 增值税调整为 `max(开票金额－产品定价, 0) × 13%`。

## 2026-07-05

* 订单查询新增“毛利”入口，展示用户应收、产品定价、应收税率费用、增值税、补录净额及最终毛利的完整计算过程。
* 新增订单经营毛利快照表和订单补录明细表，保存公式版本、税率明细、商品成本明细及补录方向快照。
* 订单经营毛利调整为“用户应收－产品定价－应收税率费用－增值税＋补录净额”，增值税按正向开票差额的 13% 计算。
* 系统设置的收款方式管理新增默认税率配置；金额补录项目新增增加/减少毛利方向。
* 订单归档自动生成最终毛利快照，补录、开票金额或产品定价修改后同步重算，并保留归档时收款税率。
* 经营看板和员工业绩基础毛利改用订单经营毛利快照；商品及产品线按销售小计占比分摊订单毛利。
* 小程序金额补录由云数据库直写改为 ANY-ERP 后端事务接口，补录数据和毛利快照保持一致。
* 新增订单毛利公式、应收费用分配、负开票差额归零和政策补贴应收排除规则的自动化测试。
* 报表统计页面升级为“经营数据看板（结果层）”，保持 ERP 现有全局导航不变。
* 新增销售额、经营毛利、毛利率、成交单量、客单价和库存金额 KPI，以及同比、环比展示。
* 新增销售与毛利趋势、门店排名、员工业绩排名、产品线占比、产品 Top10、高毛利产品、重点产品和库存结构模块。
* 后端新增统一经营看板指标服务和实时 SQL 数据源接口，预留未来切换日汇总表的数据源实现。
* 经营看板统一只统计已归档订单，按订单创建时间和中国标准时间归属经营日期。
* 员工业绩支持主销售人与辅助销售人平均拆分销售额、基础毛利和已审批毛利调整。
* 新增员工业绩订单明细，展示参与身份、参与人数、分摊结果、毛利调整编号及产生原因。
* 辅助销售列表写入前增加去重，并排除主销售人重复参与。
* 商品主数据新增“重点产品”开关，商品管理列表可直接维护。
* 毛利和库存金额增加服务端角色权限控制，普通店员和采购角色不返回敏感金额。
* 新增经营看板查询组合索引和重点产品索引。
* 库存没有历史快照时不生成虚假同比、环比，界面明确提示当前无历史快照。
* 新增经营看板日期范围、零基数比较、辅助销售拆分和敏感指标权限自动化测试。
* 修复生产部署仍使用旧报表静态包：经营看板已接入正式 `frontend/web`，重新构建并同步到 `backend/public`。
* 新增生产 WEB 构建脚本，统一完成 `frontend/web` 构建、`frontend/dist-web` 输出和 `backend/public` 同步。
* 美化产品销售额 Top10、高毛利产品和重点产品排行榜，修复组件样式作用域问题，并按 BI 表格补充销量、毛利额、数字对齐及前三名视觉层级。
* 经营看板移除员工业绩订单明细；拆分后的订单级业绩明细调整到销售报表展示，保留参与身份、分摊金额和毛利产生原因。

## 2026-07-04

* 国补政策补贴应收改为“银行到账单 + 应收核销明细”流程，支持一笔到账核销多张应收及一张应收分次到账。
* 系统设置新增国补区域到账账户配置：成都默认四川银行（艾诺云）、重庆默认兴业银行（艾诺志兴），地市保持未配置并禁止跨区域兜底。
* 国补银行到账金额进入实际资金账户，超额部分保留为未分配到账款，支持后续继续核销或退款。
* 国补手续费和差额核销新增审批记录，必须由财务提交、admin/BOSS审批且申请人不得自审。
* 历史已下账国补记录保持原样，新流程保留到账账户快照、银行流水号、核销明细和退款流水。
* 库存序列号列表对未关联库位的 SN 明确显示“未指定库位”，不再展示为空白。
* 财务管理“商品资源成本定义”的商品搜索结果由“商品名称（商品编码）”调整为“商品名称（厂商编码）”。
* 日结明细清单调整为未下账优先，再按销售订单提交时间倒序展示。
* 统一销售、采购、商品审批、库存单据、退库、费用、应付、日结、资源下账、返利对账和毛利审批等业务清单的“待处理优先、时间倒序”规则。
* 业务清单排序统一在数据库分页前执行，并增加唯一 ID 倒序作为同时间稳定排序；基础主数据仍保留人工排序。

## 2026-07-03

* 修复销售订单二级来源、辅助销售人、云闪付订单号及各类照片未写入 MySQL、详情页无法回显的问题。
* 定金抵扣调整为订单提交时“已占用”、订单归档时“已核销”、订单作废时恢复“可使用”。
* 补充订单扩展字段数据库迁移和定金生命周期自动化测试。

## 2026-07-02

* 采购申请供应商列表调整为所有已登录账号可查询，不再要求采购角色或门店数据权限；供应商维护操作仍保留采购权限。
* 修复普通门店账号进入采购申请和提交采购申请时返回 403；申请查询与提交改为全员开放，仍按账号门店范围隔离。
* 修复订单归档补齐真实 SN ID 时被误判为“更换已锁定 SN”；只有数据库中确实存在该订单的锁定权益时才禁止更换 SN。
* 小程序订单归档改为单次后端事务请求，移除归档前保存、状态快照、兼容接口轮询和重复诊断日志；库存扣减、权益核销、成本计算与日结逻辑保持不变。
* 系统设置新增“货型配置”，支持货型模板和资源子内容的新增、编辑、删除（停用）及组合维护。
* 初始化“服务商全资源货”模板，并加入国补资格、教育补贴资格、厂商返利、销售红包和报号自购等资源子内容。
* WEB 与小程序采购申请改为读取动态货型；选择货型后自动带出资源子内容，并允许按商品调整。
* 商品资源成本定义等权益下拉框继续统一读取可配置资源子内容，新增或停用后自动生效。
* 后端新增腾讯云数据库营业时段心跳，按 `Asia/Shanghai` 每日 09:30（含）至 23:00（不含）每 7 分钟通过现有 Sequelize 连接池执行 `SELECT 1`。
* 数据库心跳开关、营业时间、时区、执行间隔和重试参数支持环境配置；非营业时间不发送心跳。
* 心跳失败最多按固定间隔重试 3 次，最终失败仅记录日志，不影响 HTTP 主服务。
* 本次不增加 warmup 接口，不修改小程序启动流程或前端提示。

## 2026-07-01

* 新建未归档销售订单取消 PN/SN 存在性校验，允许商品未建档或 PN/SN 尚不存在时先保存订单。
* 订单创建阶段只保存商品名称、PN、SN 文本，不再绑定 SN、不占用库存或锁定资源权益。
* 订单归档阶段统一校验 PN 对应商品、SN 与 PN 的对应关系、门店归属及可售状态，校验通过后再扣减库存和核销权益。
* 订单明细商品关联调整为可空，并新增历史库存占用兼容标记，避免未知商品被数据库外键或非空约束拦截。
* 国补 POS 收款自动拆分为“客户实收”和“政策补贴应收”，后者不计入当前客户实收校验。
* 订单详情收款信息补充显示定金抵扣，并兼容历史国补 POS 净收款记录的拆分展示。
* 国补客户实收和政策补贴应收分别进入日结明细，并支持按全局或门店配置不同下账账户。
* 新增“政策补贴应收”账户类型及默认账户，启动迁移自动补齐国补收款方式和未下账历史日结明细。
* 日结批量下账新增账户完整性校验，未配置下账账户的明细不允许直接标记为已下账。

## 2026-06-30

* 用户管理新增“重置密码”操作，admin/boss 可将管理范围内账号密码重置为该账号手机号后 6 位。
* 新增后端接口 `POST /api/v1/system/user/:staffId/reset-password`，复用现有用户管理范围校验，防止经销商管理员重置其他经销商或 BOSS 账号。

## 2026-06-28

* 定金提交成功后立即进入收款员工名下的客户定金库，不再需要二次归档。
* 销售订单支持按指定金额部分抵扣定金，剩余余额可继续使用。
* 后端同时兼容小程序 `depositItems` 抵扣格式和历史“定金收款方式”格式。
* 启动迁移新增订单定金抵扣快照字段，并自动将仍有余额的历史“已提交”定金转为可使用。
* 订单取消或审批拒绝时按该订单实际抵扣金额释放定金余额。

## 2026-06-27

* 系统设置的资源类别扩展为商品资源权益配置，支持权益类型、采购可选、销售触发、下账、销售个人 Care 可用金和销售者业绩毛利影响配置。
* 采购申请明细支持勾选多种资源权益，采购审批入库后按 SN 自动生成对应商品资源权益。
* 商品资源权益规则支持按商品、供应商和权益类型配置固定金额、库存成本比例、销售金额比例等算法。
* 库存资源权益页新增未销售在库 SN 的批量权益调整和按最新规则批量刷新能力；已归档销售单不被规则刷新改写。
* 销售归档时可按 SN 权益触发 PO奖励、待下账记录、销售个人 Care 可用金流水和可选的销售者业绩毛利调整。
* 套装资格作为内部标记保存，不参与销售校验，为后续组合销售提醒保留依据。

## 2026-06-26

* Backend startup no longer exits when cloud MySQL is sleeping or temporarily unreachable; the HTTP service starts first, while database activation and migrations continue in a bounded background recovery loop.
* API requests now trigger database activation before entering business routes when the backend has marked the database as unavailable.
* Transient database connection errors mark the database as unhealthy and start background recovery, so subsequent mini-program retries do not depend on manual database login.
* Added unauthenticated `GET /api/v1/health/db` for checking backend/database readiness separately.
* Environment templates now include `DB_ACTIVATION_RETRY_DELAYS_MS` and `DB_STARTUP_RECOVERY_RETRY_MS`.
* Sales deposit APIs now include mini-program compatibility routes for older endpoint names such as `deposit-list`, `deposit-create`, `deposit-available`, `archive-deposit`, and `refund-deposit`.
* Backend database access now uses explicit Sequelize/mysql2 pool settings with configurable max/min connections, acquire timeout, idle eviction, connect timeout, and bounded query retry.
* Backend startup warms up the database connection, runs migrations, and performs a post-migration warmup through recoverable background initialization instead of blocking HTTP startup.
* Transient database connection failures are returned as HTTP 503 so clients can distinguish recoverable cold-start or connection-lost cases from business validation errors.
* Web HTTP clients now retry idempotent requests on network errors and 500/502/503/504 responses, with 1s/2s backoff and no blind retry for write operations.
* Environment templates now include database pool and retry settings for cloud deployment tuning.

## 2026-06-25

* `一键清空业务数据.bat` now shows the full cloud business cleanup scope before running the production-launch cleanup script.

* Cloud cleanup now explicitly includes sales orders, inventory data, inbound documents, resource rights, transfers, split/assembly documents, purchase records, daily statements, expenses, pending payables, payables, rebates, and account center balance sources.

* Account center definitions are preserved, while balances are cleared by deleting `T_SETTLEMENT_ACCOUNT_TRANSACTION` and `T_SUPPLIER_REBATE`; preserved account tables with future stored balance columns will be backed up and reset to zero.

* Cloud cleanup scripts now load database settings from `cloud-db.env`, with `cloud-db.env.example` provided as a template and `cloud-db.env` ignored by git.

* 用户管理列表新增“停用/启用”快捷操作，停用账号后账号无法继续登录。
* 登录接口和全局鉴权统一返回“账号已停用”，停用账号已有 token 在下一次接口请求时失效。
* 账号信息和修改密码接口纳入同一鉴权链路，避免停用账号访问账号相关接口。
* 用户创建和更新接口增加账号状态校验，只允许启用或停用状态。
* 补充用户账号停用业务规则和决策记录，明确停用不删除历史业务归属。
* 重写正式启用前测试数据清理脚本：保留配置、用户权限、门店、供应商、商品基础资料和商品当前价格，清空库存、采购、销售、审批、财务流水、厂家政策、厂家价格历史、价格导入批次和价格变更历史。
* 清理脚本执行前会统计目标表数据量并备份将被清理的表，必须输入 `CLEAR_TEST_DATA` 才会真正删除。

## 2026-06-21

* 优化销售新建订单的商品明细区域：表格限制在弹窗内容区内，超宽列改为区域内横向滚动，并统一卡片、表头和边框样式。
* 用户管理操作栏恢复“分配门店”，支持为普通账号多选、全选或清空所属经销商门店。
* 新增账号与门店多对多权限表；新账号默认无门店，现有账号不自动迁移。
* 用户列表改为展示实际分配门店；BOSS 固定显示全部门店且不参与手工分配。
* 门店列表及销售、库存、采购、财务、报表查询按账号实际门店范围过滤，伪造未授权门店请求返回 403。
* 微信小程序切店列表只展示账号已分配门店；原门店权限失效后清除本地选择并要求重新选择。
* 无门店账号允许登录，但无法查询或操作门店业务。
* 修复后端尚未重启时分配门店弹窗只显示“全选”的兼容问题；旧接口响应自动回退到系统页门店列表。
* 分配门店失败时展示后端具体校验原因，不再统一隐藏为“分配失败”。
* 优化分配门店弹窗布局：全选与门店列表分行展示，并显示已选数量，避免复选框和门店名称挤在一起。
* 修复 BOSS 分配门店时只显示目标账号所属经销商门店的问题；BOSS 现在可查看并分配全部启用门店，经销商管理员仍保持本经销商限制。
* 修复销售管理宽表格撑开主内容区导致按钮移出屏幕、空状态“No Data”断行及页面疑似卡死的问题；销售列表增加中文空状态和加载反馈。
* 销售订单表格改为独立横向滚动容器，取消固定操作列并移除页面级横向裁剪，确保定金管理、新建订单、表头、订单内容和分页不会被移出或裁掉。
* 彻底移除销售订单主列表的 Element Plus 表格布局计算，改用稳定的原生表格渲染，解决加载已有订单后主线程卡死、菜单无法点击和列表停留在0条的问题。
* 修复销售页挂载时调用缺失的 `getResourceCategories` API 导致同步运行时异常、路由地址变化但页面不切换的问题；补齐资源类别与资源下账前后端路由，并增加全局 Vue/路由错误提示。

* 系统设置新增资源类别管理，支持名称、简称、销售使用、公司套回、默认到账账户、排序和启停配置。
* 国补、教育补贴和销量报号迁移为初始配置，销售订单与 SN 权益维护改为动态读取资源类别。
* 销售订单新增动态资源类别数组并兼容原三类历史字段；SN 无可用权益时前后端均禁止选择和提交。
* 新增资源待下账记录，销售使用、公司套回审批通过和厂商返利预估均先进入待下账状态。
* 财务资源权益页面新增“资源下账”，点击后才向目标账户生成入账流水。
* 结算账户扩展为统一账户中心，支持资金账户、供应商返利账户和 Care 可用金账户。
* 供应商返利账户要求绑定供应商；资源下账到该账户时同步增加现有供应商返利可抵扣余额。
* 厂商返利记录保存来源供应商，下账到返利账户类型时自动匹配对应供应商账户。
* 存量未到账厂商返利预估自动迁移为待下账记录；新建供应商返利账户时同步现有返利期初余额。
* Care 可用金作为受限额度账户，可在付款时手工选择用于延保等指定采购用途。
* 数据迁移自动创建默认“Care卡可用金”账户，资源类别可直接配置下账到该账户。
* 返利管理新增手工返利冲销，使用反向流水替代物理删除。
* 资源下账预留批次号，为后续批量上传和批量下账扩展做准备。

## 2026-06-20

* 修复新增角色分配菜单权限失败：统一菜单树节点字段，并移除无效的角色菜单关联查询。
* 菜单权限保存增加菜单有效性校验、父级菜单提交和数据库事务，避免部分保存。
* 用户管理支持一个账号选择多个角色，登录、路由、菜单、审批及数据权限统一按角色并集判断。
* 用户管理开放给经销商总权限账号和 BOSS；经销商按所属经销商隔离用户，BOSS 可管理全部用户。
* 用户列表改为展示角色名称及所属经销商下全部有效门店，不再显示角色代码或依赖“分配门店”弹窗查看。
* 新增角色的内部代码改为系统自动生成，角色管理界面完全隐藏角色代码。
* 启动迁移自动为仅有历史主角色的存量账号补齐员工角色关联数据。

* 新增库存资源权益管理，SN可分别维护国补、教育补贴和销量报号资格及独立状态。
* SN新增税务属性和货源性质，未税不再与资源资格混为单一货型。
* 新增资源套回申请及财务单级审批，禁止申请人自审，审批期间锁定对应权益。
* 新增商品+资源类型成本定义和产品资源成本流水，资源成本不自动影响销售结算成本。
* 销售开单选择SN后自动显示货品销售标签、可用权益和风险提醒，并按订单明细选择实际使用权益。
* 销售订单提交、归档、取消及审批拒绝分别联动权益锁定、核销和释放。
* 未税库存开票新增确认提示；后端要求记录操作人确认，避免绕过前端提示。
* 库存管理新增“库存资源权益”页签，财务管理新增“资源权益核销与成本调整”页签。
* 新增权益变更审计、来源幂等约束和产品成本不得小于零校验。

* 新增新建商品审批申请表、申请列表及通过/拒绝接口。
* 手工新建商品调整为先申请，审批通过后才在事务中创建正式商品。
* 网页端和小程序新建商品改为显式调用商品申请接口，防止旧创建接口绕过审批直接写入商品主表。
* 修复历史商品属性字段大小写不一时，品牌、系列、型号等值仅保存在扩展字段、商品列表和导出显示为空的问题。
* 商品管理新增“新建商品审批”页签，支持 finance、purchaser、admin、boss 审批。
* 小程序采购页同步商品申请状态，审批通过后才允许搜索商品发起采购。
* 采购及新建商品页面文本输入框与下拉框统一高度。
* 采购申请列表调整为待审批优先；待审批组内按创建时间倒序，其他状态不分组并统一按创建时间倒序。
* 员工业绩统计新增逐单毛利增加、减少调整申请。
* 所有账号可提交申请和证明附件，审批固定为财务初审、admin 复审。
* 申请人禁止审核自己的申请，仅最终审批通过的调整计入业绩。
* 员工业绩毛利改为使用订单归档销售毛利，不再按当前成本动态重算。
* 未保存归档毛利的历史订单保留旧成本口径兼容展示并给出重算提示，不自动改写历史数据。
* 业绩汇总改为按全部筛选结果计算，展示基础毛利、已审批调整和最终业绩毛利。
* 新增毛利调整申请及附件数据表，并为所有角色开放报表统计菜单。
* 修复 PowerShell 启动器将任意 Node 进程误判为后端已启动的问题，启动时按3000端口精准重启后端。

## 2026-06-19

* 付款管理待付款清单新增“立即付款”，支持全额和部分付款登记。
* 付款登记支持选择结算账户，自动生成单笔付款批次、付款记录和结算账户支出流水。
* 结算账户余额不足时显示负数余额提示，确认后仍可完成付款登记。
* 单笔付款与批量付款统一使用事务校验和账户锁，防止重复付款及并发余额覆盖。
* 待付款清单过滤剩余应付金额为0的历史结算单，并统一金额为两位小数显示。
* 待付款清单和付款批次增加加载状态，避免远程数据加载期间误显示为空。
* 新建商品取消“商品简称”输入，商品名称统一根据分类补充字段生成。
* 商品批量导入失败时显示具体错误原因，逐行错误补充 Excel 行号。

## 2026-06-13

* 商品价格批量导入模板移除商品名称，新增厂商编码、生效时间、调价原因和备注。
* 价格导入支持按商品编码或厂商编码匹配商品，厂商编码一对多时自动展开更新全部关联商品。
* 价格导入调整为上传文件后不立即校验，点击立即导入时校验并导入；异常记录跳过，正常记录入库。
* 价格导入异常记录支持页面提示和下载 Excel。
* 新增最低售价不得大于定价的校验，手工修改和批量导入均适用。
* 新增商品价格导入批次表和商品价格变更记录表，支持价格调整追溯。
* 批量导入支持预约生效时间，到期后由后端定时任务自动生效。
* 价格管理商品查询支持按厂商编码搜索。
* 价格管理列表新增价格历史查看入口。

## 2026-06-08

* 商品分类管理新增同级分类上移、下移排序能力，商品分类引用处按维护顺序展示。
* 财务管理结算账户新增上移、下移排序能力，结算账户列表、余额列表和下拉引用按维护顺序展示。
* 采购供应商管理新增上移、下移排序能力，供应商列表和采购、财务相关下拉引用按维护顺序展示。
* 供应商主数据新增排序字段，并加入启动迁移和初始化脚本。
* 商品管理新建/编辑商品弹窗取消固定商品属性填写区，商品属性统一通过分类补充字段维护。
* 商品字段标识匹配固定商品列时支持大小写不敏感，例如 `PROCESSOR` 可写入处理器字段。

## 2026-06-07

* 调整库存拆分规则：新拆分单支持部分成本剥离，拆出金额不再要求等于被拆商品成本。
* 拆分后被拆商品继续销售，SN 保持原有在库状态，原商品成本按拆出金额扣减。
* 拆装管理弹窗中被拆商品表格新增 PN 展示。
* 拆装管理金额输入去掉加减控制，拆出商品价格输入提示改为“请输入商品价格”。
* 拆出商品和组装成品选择目标商品后，默认带出目标商品成本价并允许手工修改。
* 拆装管理目标商品区域新增“新建商品”入口，创建成功后自动选中新商品。

## 2026-06-06

* 新增定金管理：支持定金单提交、归档、列表查询和退款记录。
* 正式销售订单支持选择“定金”收款方式并绑定定金单进行全额一次性抵扣。
* 定金核销限制为当前店员自己收取的定金单，并校验客户电话一致。
* 定金不参与毛利计算，正式订单归档后继续按现有销售结算成本口径计算毛利。
* 新增定金核销记录，正式订单取消或审批拒绝时释放定金并标记核销记录作废。
* 销售页新增定金管理入口和定金核销选择器。
* 新增定金相关数据库表、订单支付定金绑定字段和“定金”收款方式种子数据。
* 删除应付结算单“待确认”状态，草稿提交后直接进入待付款。
* 将付款管理从应付管理页签内部移出，调整为财务管理页面内的顶部标签页“付款管理”。
* 付款管理继续支持批量导出、付款结果导入、部分付款、账户扣款和付款批次撤销。
* 付款审批流程暂不固化，后续通过独立审批流程管理建设。
* 启动迁移时将历史待确认应付结算单归一为待付款。
* 新增库存拆分/组装转换单：支持拆分单、组装单、成本人工录入、成本守恒校验和转换明细追溯。
* 库房管理新增“拆装管理”标签页，支持创建拆分/组装、查看详情和冲销已完成转换单。
* 拆分/组装执行时维护库存聚合数量、SN状态、SN日志和转换来源关系。
* 组装服务成本计入组装成品库存成本。
* 新增库存转换单和转换明细数据库表，并加入启动迁移和初始化脚本。

## 2026-06-05

* 保留应付管理中的“待付款清单”。
* 将应付结算创建动作调整为“生成结算单”。
* 新增应付结算单管理列表与详情查看。
* 新增结算单状态流转：草稿、待确认、已确认、已作废。
* 新增付款状态预留字段：未付款、部分付款、已付款。
* 暂停应付结算付款功能入口，避免当前阶段误触发付款逻辑。
* 新增应付管理内的实际付款页面。
* 新增已确认结算单付款清单导出。
* 新增付款结果导入预校验和确认扣款。
* 新增付款批次、付款记录和账户流水扣款。
* 新增付款批次整批撤销和反向账户流水。
* 结算单详情新增付款记录查看。
* 厂家返利管理新增厂家政策维护字段，支持配置是否影响销售结算成本。
* 新增厂家价格历史表和厂家价格 Excel 导入入口。
* 库存 SN 和入库明细新增原始提货价字段。
* 销售单明细新增原始成本、原始提货价、销售时当前提货价、P差、成本调整、销售结算成本和销售毛利字段。
* 销售归档时自动生成返利预估记录和销售结算成本调整明细。
* 新增销售结算成本调整明细后台查看入口。
* 新增销售结算成本手工重算接口，避免历史订单自动重复计算。
* 执行入库弹窗移除库位选择，仅按库存类型和数量入库。

## 2026-07-17

* 采购申请商品明细增加商品名称、采购单价和数量的填写提示。
* 新增采购申请草稿保存、草稿编辑和草稿提交功能。
* 草稿提交后复用现有采购审批流程，不提前生成审批后业务单据。
* 采购申请列表增加提交人字段。
* 采购申请列表新增按提交人、商品名称/PN/商品编码、供应商和状态筛选查询。
* 采购申请门店分配增加库位分配，默认使用销售仓，并限制只能选择启用库位。
* 采购审批生成入库单时按门店和库位继承采购分配，执行入库自动使用指定库位。

## 2026-07-18

* 销售订单、采购申请列表增加提交人及审批/确认人员展示。
* 销售订单、采购申请详情增加审批字段和流程时间线。
* 调拨列表增加调出确认人、调入确认人及流程详情弹窗。
* 新增 `T_BUSINESS_ACTION_LOG` 业务操作审计日志表，记录销售、采购、调拨关键流程动作。
* 销售订单和采购申请补充提交时间、审批时间等审计字段，并通过启动迁移兼容历史数据库。
# 2026-08-17

* 销售退库完成后生成独立负向销售结算，按用户应收口径计算，原订单和原收款流水保持不变。
* 国补客户实收、政策补贴应收和教育补贴分别生成负向明细；国补后返 15% 沿用既有固定标准，不新增配置项。
* 已收款退单进入待退款，增加财务确认退款接口，支持原路或其他方式登记退款；已开票订单生成红字发票处理记录。
* 销售退单负向结算纳入销售订单导出和销售报表汇总，并增加退单结算幂等约束。

# 2026-08-19

* 销售订单导出按订单保存的辅助销售实际金额明细展示金额分配，无明细时显示 0。
* 销售退货导出以退货单号生成独立负向明细，数量和金额均按负数展示，并保留原销售订单关联。
* 采购申请增加申请单号查询；编辑草稿时恢复返利抵扣输入并保留原抵扣金额，提交时按当前余额校验。
* 入库详情增加单据级和商品级入库人员展示。
* Web 新建销售订单的 SN 字段改为按门店和 PN 查询结果下拉选择。
* 库存查询、SN库存清单和库存导出开放本经销商全部有效门店的只读范围，库存写入权限保持不变。
* 店长（`manager`、`store_manager`）经营数据看板、销售报表、库存报表和员工业绩统计开放本经销商全部有效门店查询。
* 新增只读门店选项接口，统一为库存查询和经营报表提供服务端校验后的门店范围。

# 2026-08-28

* SN 商品搜索库存统一按在库 SN 和销售仓库位统计，避免历史 `regular_qty` 聚合残留导致库存偏大。

# 2026-08-30

* 新建商品分类改为可选任意一级至四级，也支持不选分类后手工填写。
* 商品分类树层级自动映射为商品分类、品牌、系列、型号，并允许手工覆盖。
* 商品主表新增分类节点关联和历史路径兼容字段，旧路径数据启动时自动迁移。
* 商品申请、库存转换、商品导入、Web、小程序及销售/库存报表统一使用分字段商品维度。

# 2026-08-23

* 销售管理新增月度任务指定，支持门店和员工独立设置销售额、毛利及指定商品销量批次目标。
* 指定商品支持多个商品组合为一个批次，同月支持配置多个批次。
* 新增门店毛利目标手工分摊到员工的配置和达成口径。
* 报表管理新增月度任务实时达成查询，按授权范围展示门店和员工完成率并排序。
* 小程序员工销售内容后增加业务达成，展示门店和员工任务达成情况；不新增独立页面。
